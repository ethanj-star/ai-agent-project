# 阶段 11 异步生成进度和结果页完善

完成日期：2026-06-14

对应总控文档：

```text
阶段9-17开发规划与维护清单.md
```

## 1. 本阶段核心思想

第 11 阶段的核心思想是：把后台 `GenerationJob` 从“开发者能看懂的任务状态”升级成“用户能理解的生成过程”。

在前 9、10 阶段里，系统已经有正式前端页面和结构化需求表。  
但完整旅行规划生成是长耗时任务，如果用户点击生成后只看到一个简单状态，就很容易误以为页面卡住，或者不知道失败后该怎么处理。

本阶段围绕三个关键词推进：

```text
可等待
可恢复
可解释
```

也就是：

```text
用户能看见任务正在做什么
刷新页面后能找回任务
成功后能自动进入方案
失败后能知道原因和下一步
```

## 2. 本阶段解决的问题

本阶段主要解决了以下问题：

```text
生成任务运行时间较长，用户不知道系统是否真的在工作
页面刷新后只靠 localStorage，不能主动从后端找回最近任务
任务失败时提示不够清楚，用户不知道该修改需求还是稍后重试
成功后虽然能加载 plan，但进度区缺少明确的完成解释
前端维护了过多阶段枚举和进度映射，后端没有提供统一展示语义
```

## 3. 新增功能

### 3.1 任务展示字段

`GenerationJobResponse` 新增前端展示字段：

```text
statusLabel
stageLabel
stageDescription
progressPercent
actionHint
durationSeconds
terminal
recoverable
```

这些字段用于让前端直接展示：

```text
中文状态
中文阶段
阶段说明
进度条百分比
下一步建议
运行耗时
是否终态
失败后是否可恢复
```

### 3.2 最近任务列表包装响应

新增：

```text
GenerationJobListResponse
```

用于包装：

```text
sessionId
count
hasActiveJob
latestJobId
latestPlanId
jobs
```

这样 `GET /api/v1/jobs` 不再返回裸数组，而是返回一个更适合页面恢复的结构化对象。

### 3.3 前端任务进度体验

正式页面新增：

```text
任务摘要
阶段时间线
失败建议
最近任务列表
最近任务刷新按钮
```

### 3.4 页面刷新恢复任务

前端新增恢复逻辑：

```text
先从 localStorage 恢复当前 jobId
如果有 PENDING / RUNNING 任务，继续轮询
如果没有本地运行中任务，则调用 GET /api/v1/jobs?sessionId=xxx&limit=5
从后端最近任务中恢复运行中任务或最近成功任务
```

## 4. 修改的业务逻辑

### 4.1 任务查询接口语义增强

`GET /api/v1/jobs/{jobId}`：

```text
仍然查询单个任务详情
新增展示字段
不触发 Graph
不扣费
```

`GET /api/v1/jobs?sessionId=xxx&limit=5`：

```text
从返回 List<GenerationJobResponse>
改为返回 GenerationJobListResponse
用于页面刷新恢复和最近任务展示
不触发 Graph
不扣费
```

### 4.2 轮询逻辑增强

前端 `loadJobStatus(showErrors)` 增强为：

```text
统一调用 applyJobResponse(data)
终态后停止轮询
终态后刷新最近任务列表
成功后只自动加载一次 plan，避免重复闪烁
失败后显示 errorMessage 和 actionHint
```

### 4.3 任务恢复逻辑增强

新增 `recoverLatestJob()`：

```text
页面打开时执行
如果本地有运行中 job，继续轮询
否则查询最近任务列表
如果找到运行中任务，自动恢复并开始轮询
如果已有 planId，则自动加载 plan
```

## 5. 修改的前端内容

### 5.1 travel-planner.html

修改内容：

```text
静态资源版本从 20260614-4 更新为 20260614-5
生成进度区新增 jobMeta
生成进度区新增 jobTimeline
生成进度区新增 jobAdvice
新增 reloadRecentJobsButton
新增 recentJobs 最近任务列表容器
```

### 5.2 agent-app.js

新增状态：

```text
jobDetail
recentJobs
jobStartedAt
jobUpdatedAt
jobFinishedAt
jobErrorMessage
jobActionHint
planAutoLoadedForJob
```

新增恢复与渲染逻辑：

```text
loadRecentJobs(showErrors)
recoverLatestJob()
applyJobResponse(data)
applyJobListResponse(data)
selectRecentJob(jobId)
renderJobTimeline()
renderJobFailureAdvice()
renderRecentJobs()
renderJobMeta(detail)
jobStatusLabel(status)
jobStatusClass(status)
jobTerminal(status)
formatDuration(seconds)
formatJobTime(value)
```

增强逻辑：

```text
generatePlanAsync() 创建任务后加载最近任务列表
loadJobStatus(showErrors) 统一处理任务响应和终态
renderJobProgress() 使用后端 stageLabel / progressPercent / stageDescription
renderStatus() 显示中文任务状态和阶段
renderRequirementButtons() 新增最近任务按钮状态控制
persistState() 保存任务恢复需要的新字段
resetFlow() 清空任务恢复字段
renderDebug() 输出新增任务状态，方便开发调试
```

### 5.3 app.css

新增样式：

```text
job-status-badge
job-meta
job-timeline
job-step
job-step.done
job-step.active
job-step.failed
job-advice
recent-jobs
recent-job
recent-job.active
recent-job-main
```

样式目标：

```text
生成中状态清楚
任务阶段紧凑可扫读
失败建议不抢页面主体
最近任务列表在桌面和移动端都不溢出
```

## 6. 修改的后端接口

### 6.1 GET /api/v1/jobs/{jobId}

接口仍然存在，返回单个任务详情。

新增字段：

```text
statusLabel
stageLabel
stageDescription
progressPercent
actionHint
durationSeconds
terminal
recoverable
```

### 6.2 GET /api/v1/jobs

接口返回类型变化：

```text
旧：List<GenerationJobResponse>
新：GenerationJobListResponse
```

示例结构：

```json
{
  "sessionId": "planner-xxx",
  "count": 2,
  "hasActiveJob": true,
  "latestJobId": "job-xxx",
  "latestPlanId": "plan-xxx",
  "jobs": []
}
```

## 7. 修改的代码文件

本阶段修改：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/dto/GenerationJobResponse.java
AgentProjectTrip/src/main/java/com/travel/agent/web/GenerationJobController.java
AgentProjectTrip/src/main/resources/static/travel-planner.html
AgentProjectTrip/src/main/resources/static/agent-app.js
AgentProjectTrip/src/main/resources/static/app.css
第11阶段开发.md
阶段9-17开发规划与维护清单.md
```

本阶段新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/dto/GenerationJobListResponse.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/dto/GenerationJobResponseTest.java
阶段 11 异步生成进度和结果页完善.md
```

## 8. 新增的类

### 8.1 GenerationJobListResponse

路径：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/dto/GenerationJobListResponse.java
```

用途：

```text
包装某个 sessionId 下的最近任务列表
提供 hasActiveJob / latestJobId / latestPlanId 摘要
让前端刷新页面后更容易恢复任务
```

### 8.2 GenerationJobResponseTest

路径：

```text
AgentProjectTrip/src/test/java/com/travel/agent/ai/dto/GenerationJobResponseTest.java
```

用途：

```text
测试 GenerationJobResponse 新增展示字段
测试 GenerationJobListResponse 最近任务摘要
```

## 9. 删除或废弃的类

本阶段没有删除或废弃任何类。

## 10. 新增的方法

### 10.1 后端新增方法

`GenerationJobResponse` 新增或补充：

```text
statusLabel(...)
stageLabel(...)
stageDescription(...)
progressPercent(...)
actionHint(...)
durationSeconds(...)
isTerminal(...)
isRecoverable(...)
getStatusLabel()
setStatusLabel(...)
getStageLabel()
setStageLabel(...)
getStageDescription()
setStageDescription(...)
getProgressPercent()
setProgressPercent(...)
getActionHint()
setActionHint(...)
getDurationSeconds()
setDurationSeconds(...)
isTerminal()
setTerminal(...)
isRecoverable()
setRecoverable(...)
```

`GenerationJobListResponse` 新增：

```text
of(sessionId, jobs)
isActive(job)
findLatestPlanId(jobs)
getSessionId()
setSessionId(...)
getCount()
setCount(...)
isHasActiveJob()
setHasActiveJob(...)
getLatestJobId()
setLatestJobId(...)
getLatestPlanId()
setLatestPlanId(...)
getJobs()
setJobs(...)
```

### 10.2 前端新增方法

`agent-app.js` 新增：

```text
recoverLatestJob()
loadRecentJobs(showErrors)
applyJobResponse(data)
applyJobListResponse(data)
selectRecentJob(jobId)
renderJobMeta(detail)
renderJobTimeline()
renderJobFailureAdvice()
renderRecentJobs()
jobTerminal(status)
jobStatusLabel(status)
jobStatusClass(status)
formatDuration(seconds)
formatJobTime(value)
```

## 11. 删除或废弃的方法

本阶段没有删除或废弃方法。

## 12. 关键代码细节和用途

### 12.1 后端把展示语义集中在 DTO

`GenerationJobResponse.from(job)` 负责从核心任务状态派生展示字段：

```text
RUNNING -> 生成中
RUNNING_GRAPH -> 执行核心规划
RUNNING_GRAPH -> progressPercent 68
失败任务 -> actionHint 提示检查需求或稍后重试
```

这样前端不需要自己维护一套复杂映射，后续如果阶段名或说明变化，只需要改后端 DTO。

### 12.2 最近任务列表不再裸返回数组

`GenerationJobListResponse.of(sessionId, jobs)` 会同时计算：

```text
count
hasActiveJob
latestJobId
latestPlanId
```

这让前端可以快速判断：

```text
是否有运行中任务可以恢复
最近成功任务是否有 planId 可以加载
```

### 12.3 前端统一任务响应入口

`applyJobResponse(data)` 统一处理：

```text
jobId
jobStatus
jobStage
planId
remainingCredits
job 时间字段
errorMessage
actionHint
lastRawResponse
```

避免 `generatePlanAsync()`、`loadJobStatus()`、`selectRecentJob()` 各自写一套状态同步逻辑。

### 12.4 成功后只自动加载一次 plan

新增：

```text
planAutoLoadedForJob
```

用途：

```text
避免任务成功后轮询或重复渲染导致 plan 反复自动加载和页面闪动
```

### 12.5 最近任务切换

`selectRecentJob(jobId)` 支持用户点击最近任务：

```text
运行中任务：恢复并开始轮询
成功任务：加载对应 plan
失败任务：显示失败原因
```

## 13. 注释规范执行情况

本阶段按注释规范执行：

```text
GenerationJobResponse 类注释补充“展示字段是派生字段”
GenerationJobListResponse 类注释说明系统位置、职责、边界
GenerationJobListResponse.of(...) 注释说明输入和输出
GenerationJobController 注释明确两个 GET 接口只读、不扣费、不触发 Graph
agent-app.js 新增恢复逻辑前写了说明注释
```

前端 HTML / CSS 没有写多余说明性注释，避免页面资源臃肿。

## 14. 测试方式和测试结果

### 14.1 JavaScript 语法检查

```text
node --check AgentProjectTrip/src/main/resources/static/agent-app.js
通过
```

### 14.2 HTML / JS id 绑定检查

检查结果：

```text
绑定的 54 个 id 全部存在
```

### 14.3 Java 单元测试

```text
mvn test
通过，Tests run: 105, Failures: 0, Errors: 0, Skipped: 0
```

### 14.4 静态资源同步

```text
mvn process-resources
通过，静态资源已同步到 target/classes
```

### 14.5 target 静态资源检查

```text
node --check AgentProjectTrip/target/classes/static/agent-app.js
通过

target/classes/static/travel-planner.html 已包含：
20260614-5
jobTimeline
jobAdvice
recentJobs
reloadRecentJobsButton
```

### 14.6 人工测试

人工测试结果：

```text
用户已确认测试通过
```

## 15. 遗留问题

本阶段仍然遗留：

```text
没有新增取消任务接口，只兼容 CANCELLED 状态展示
没有接入真实航班分支
没有把 BranchDispatch 改成模型驱动
没有做任务 Trace 明细
没有做真正用户登录后的跨设备任务恢复
```

这些内容留给后续阶段继续推进。

## 16. 下一阶段衔接点

下一阶段建议进入：

```text
阶段 12：真实接入航班分支
```

原因：

```text
现在前端正式页面、需求表单、异步生成进度和任务恢复都已经能跑通。
接下来可以开始增强 Graph 的真实工具能力。
航班分支是旅行规划里最影响预算、路线和可执行性的外部工具能力，优先接入价值最高。
```

阶段 12 可以重点做：

```text
梳理 FlightTools / FlightService 当前能力
让 BranchDispatch 或兼容逻辑真正派发 FLIGHT 任务
让 BranchExecuteNode 能执行航班查询分支
缺少出发地或日期时返回明确不可查询原因
Planner 不再伪造具体航班号和价格
航班 API 失败时降级，不中断整个 Graph
```
