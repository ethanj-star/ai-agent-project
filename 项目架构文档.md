# 欧洲旅游 Agentic 系统架构文档

本文档根据当前代码状态重写，目标是同时说明三件事：

1. 当前项目已经实际完成了什么。
2. 当前实现与最终“像 Codex 一样自动识别、拆解、编排、反馈”的目标之间还缺什么。
3. 后续应该按什么顺序把 Spring AI 外围能力、LangGraph4j 核心编排、多模型分支工具链补齐。

## 1. 项目目标

项目定位为一个面向欧洲旅行的 Agentic 网站。用户可以用自然语言输入旅行偏好、预算、时间、目的地、约束条件，也可以直接闲聊。系统需要自动判断用户意图，并在简单问题上低成本快速回复，在复杂旅行规划任务上调用更强模型、RAG 知识库和外部工具完成多步推理。

最终目标不是单次 Chat Completion，而是一个可循环的多智能体工作流：

```text
用户输入
  -> 前台模型做意图识别
  -> 简单问题直接回复
  -> 复杂问题进入核心编排器
  -> 编排器拆解任务
  -> 调用天气、航班、酒店、景点、知识库等分支 Agent / Tools
  -> 汇总结果
  -> 校验预算、时间、可行性、信息缺口
  -> 必要时反复补查和修正
  -> 输出最终旅行方案
```

## 2. 当前技术栈

### 2.1 后端框架

- Spring Boot `3.3.13`
- Java `21`
- Spring AI `1.0.5`
- Spring Web
- Spring Cache + Redis
- Spring AI OpenAI-compatible ChatModel
- Spring AI Pinecone VectorStore

### 2.2 当前模型配置

模型配置集中在：

```text
AgentProjectTrip/src/main/java/com/travel/agent/config/MultiModelConfig.java
```

当前代码中已经手动装配了三类模型 Bean：

| Bean 名称 | 配置来源 | 当前用途 | 参数 |
|---|---|---|---|
| `gatekeeperChatModel` | `custom.ai.deepseek.*` | DeepSeek Flash，负责意图识别 | temperature `0.0`，maxTokens `512` |
| `coreChatModel` | `custom.ai.deepseek.*` | DeepSeek Pro，负责复杂规划回答 | temperature `0.7`，maxTokens `4096` |
| `branchChatModel` | `custom.ai.bailian.*` | Qwen 百炼，作为默认 `@Primary` 模型，负责旧版工具调用、ETL、分支回复 | temperature `0.7`，maxTokens `4096` |

注意：`application.properties` 里当前分支模型是：

```properties
custom.ai.bailian.model=qwen3.6-plus
```

文档和规划中如果写 `qwen3.6-max`，需要后续按真实可用模型名统一。

### 2.3 Embedding 与向量库

当前 Embedding 通过 Spring AI OpenAI 自动配置驱动，实际走阿里百炼 OpenAI 兼容地址：

```properties
spring.ai.openai.base-url=https://dashscope.aliyuncs.com/compatible-mode
spring.ai.openai.embedding.options.model=text-embedding-v1
spring.ai.vectorstore.pinecone.index-name=agentprojecttrip
```

Pinecone 已经接入，代码里有数据灌入和检索工具。

### 2.4 网络与超时

`AiClientConfig` 为 Spring `RestClient.Builder` 设置了：

- 连接超时：10 秒
- 读取超时：90 秒

这对大模型调用是必要的，因为规划和 ETL 提取可能明显超过普通 HTTP 默认超时。

## 3. 当前工程结构

实际主要代码结构如下：

```text
AgentProjectTrip
├── pom.xml
├── src/main/java/com/travel/agent
│   ├── TravelAgentApplication.java
│   ├── config
│   │   └── MultiModelConfig.java
│   ├── ai
│   │   ├── agents
│   │   │   └── MastermindAgent.java
│   │   ├── config
│   │   │   └── AiClientConfig.java
│   │   ├── dto
│   │   │   └── GatekeeperResponse.java
│   │   └── tools
│   │       ├── FlightTools.java
│   │       ├── WeatherTools.java
│   │       ├── PlacesTools.java
│   │       └── KnowledgeTools.java
│   ├── controller
│   │   └── TestController.java
│   ├── core
│   │   ├── GatekeeperAgent.java
│   │   ├── dto
│   │   │   ├── AttractionDTO.java
│   │   │   ├── FlightDTO.java
│   │   │   ├── HotelDTO.java
│   │   │   └── WeatherDTO.java
│   │   ├── etl
│   │   │   ├── DataPreProcessor.java
│   │   │   ├── XhsJsonlPreProcessorImpl.java
│   │   │   └── DataExtractionAgent.java
│   │   └── service
│   │       ├── FlightService.java
│   │       ├── WeatherService.java
│   │       ├── PlacesService.java
│   │       ├── KnowledgeBaseService.java
│   │       └── impl
│   │           ├── FlightServiceImpl.java
│   │           ├── WeatherServiceImpl.java
│   │           └── PlacesServiceImpl.java
│   └── web
│       ├── AgentController.java
│       ├── TravelController.java
│       ├── EtlController.java
│       └── KnowledgeController.java
└── src/main/resources
    ├── application.properties
    └── static/index.html
```

当前还没有 `src/test` 目录，也没有看到 LangGraph4j 相关类或 Maven 依赖。

## 4. 当前已实现能力

### 4.1 Spring Boot 应用骨架

`TravelAgentApplication` 已完成：

- Spring Boot 启动入口。
- `@EnableCaching` 启用缓存。
- 组件扫描覆盖当前所有 Controller、Service、Tool、Agent。

### 4.2 多模型手动装配

当前已经绕开了单一默认 ChatModel 的限制，手动构建了三个独立 ChatModel：

- Gatekeeper 使用 DeepSeek Flash。
- Core 使用 DeepSeek Pro。
- Branch 使用 Qwen 百炼，并标记为 `@Primary`。

这一步是正确的架构基础。它使 Spring AI 的自动注入不会把所有 Agent 锁死在同一个模型上。

### 4.3 Gatekeeper 意图识别

`GatekeeperAgent` 已实现：

- 使用 `gatekeeperChatModel` 单独构造 `ChatClient`。
- 用系统提示词约束模型只输出 JSON。
- 定义四类意图：
  - `DIRECT_CHAT`
  - `TOOL_WEATHER`
  - `TOOL_FLIGHT`
  - `PLAN_OR_RAG`
- 对空输入和模型调用失败提供兜底 JSON。
- 清理模型可能输出的 Markdown 代码块。

对应开发测试入口：

```text
GET /api/test/gatekeeper?message=...
```

需要注意：Gatekeeper prompt 要求输出 `direct_reply`，但 `GatekeeperResponse` DTO 当前只定义了 `intent` 和 `entities`，没有 `directReply` 字段。由于 `@JsonIgnoreProperties(ignoreUnknown = true)`，多出来的 `direct_reply` 会被忽略。

### 4.4 新版三擎编排入口

`MastermindAgent.handleUserWorkflow(String userMessage)` 已实现一个初版编排流：

```text
用户消息
  -> GatekeeperAgent.routeRequest
  -> ObjectMapper 解析 GatekeeperResponse
  -> switch(intent)
      DIRECT_CHAT -> 固定欢迎/引导回复
      TOOL_WEATHER / TOOL_FLIGHT -> Qwen 分支模型回复
      PLAN_OR_RAG -> DeepSeek Pro 规划回复
```

对应 HTTP 入口：

```text
GET /api/v1/agent/chat?message=...
```

这是当前最接近最终架构的入口，但它还只是“路由式编排”，不是完整 Agentic 工作流。

### 4.5 旧版单模型全工具入口

`MastermindAgent.chat(String userMessage)` 仍然保留，并由 `TravelController` 暴露：

```text
GET /api/v1/travel/chat?message=...
```

这个旧入口使用默认 `@Primary branchChatModel`，并一次性注册以下工具：

- `FlightTools`
- `WeatherTools`
- `PlacesTools`
- `KnowledgeTools`

当前前端 `index.html` 调用的仍然是这个旧入口：

```javascript
fetch(`/api/v1/travel/chat?message=${encodeURIComponent(text)}`)
```

这意味着：页面上的聊天体验目前走的是“Qwen + 全工具 Function Calling”模式，而不是新版 Gatekeeper + 三擎编排模式。

### 4.6 外部工具与业务服务

#### 航班

相关类：

- `FlightTools`
- `FlightService`
- `FlightServiceImpl`

能力：

- 通过 SerpApi 的 `google_flights` 引擎查询真实航班。
- 使用 `type=2` 查询单程票。
- 使用 `currency=EUR`。
- 最多返回 3 条航班。
- 使用 Redis 缓存，缓存 key 为 `origin:destination:date`。

#### 天气

相关类：

- `WeatherTools`
- `WeatherService`
- `WeatherServiceImpl`

能力：

- 通过 OpenWeatherMap Current Weather API 查询实时天气。
- 返回城市名、温度、天气描述、湿度。
- 查询失败时返回兜底 DTO，不向上抛异常。

#### 酒店与景点

相关类：

- `PlacesTools`
- `PlacesService`
- `PlacesServiceImpl`

能力：

- 酒店：SerpApi `google_hotels`，最多返回 3 条，Redis 缓存。
- 景点：SerpApi `google_local`，查询 `top attractions in {city}`，最多返回 3 条，Redis 缓存。

#### 私有知识库

相关类：

- `KnowledgeTools`
- `KnowledgeBaseService`
- `KnowledgeController`

能力：

- `KnowledgeTools.searchTravelGuide(query)` 通过 Pinecone `VectorStore.similaritySearch` 检索 topK=2。
- `KnowledgeBaseService.ingestRealKnowledge(fileName)` 从 `data/extracted/` 读取 JSONL，转换成 Spring AI `Document`，每 20 条一批写入 Pinecone。

### 4.7 小红书 ETL 管道

当前已经实现一条从原始 JSONL 到 Pinecone 的开发期数据管道。

#### 清洗阶段

入口：

```text
GET /api/v1/etl/clean-xhs?fileName=...&type=POST
GET /api/v1/etl/clean-xhs?fileName=...&type=COMMENT
```

相关类：

- `DataPreProcessor`
- `XhsJsonlPreProcessorImpl`
- `EtlController`

能力：

- 流式读取小红书 JSONL。
- POST 只保留点赞数大于等于 50 的主帖。
- COMMENT 保留非负点赞且内容长度大于等于 3 的评论。
- 输出到 `data/processed/`。

当前限制：

- 原始数据目录在 `EtlController` 中硬编码为 Windows 绝对路径：

```text
C:\Users\DJI\Desktop\red note mediacrawler\jsonl data\xhs\jsonl\
```

后续应改成配置项。

#### AI 提取阶段

入口：

```text
GET /api/v1/etl/extract?inputFileName=...&outputFileName=...
```

相关类：

- `DataExtractionAgent`

能力：

- 从 `data/processed/` 读取清洗后的 JSONL。
- 使用 Qwen 模型逐条提取结构化实体。
- 输出 `ROUTE`、`POI`、`TIP` 三类实体。
- 写入 `data/extracted/`。
- 异步触发，接口返回 `202 Accepted`。

#### 向量灌入阶段

入口：

```text
GET /api/v1/knowledge/ingest?fileName=france_italy_entities.jsonl
```

当前工作区已有数据：

- `data/processed/post_search_contents_2026-05-16.jsonl`：20 行。
- `data/processed/post_search_comments_2026-05-16.jsonl`：0 行。
- `data/extracted/france_italy_entities.jsonl`：144 行实体。

## 5. 当前接口清单

| 接口 | Controller | 用途 | 当前状态 |
|---|---|---|---|
| `GET /api/v1/travel/health` | `TravelController` | 健康检查 | 已实现 |
| `GET /api/v1/travel/flights` | `TravelController` | 直连航班 Service | 已实现 |
| `GET /api/v1/travel/chat` | `TravelController` | 旧版 Qwen + 全工具 Agent 入口 | 已实现，前端当前使用 |
| `GET /api/v1/agent/chat` | `AgentController` | 新版 Gatekeeper + 三擎编排入口 | 初版实现，未接入真实工具和 LangGraph |
| `GET /api/test/gatekeeper` | `TestController` | Gatekeeper 联调 | 已实现 |
| `GET /api/v1/etl/clean-xhs` | `EtlController` | 小红书 JSONL 清洗 | 已实现 |
| `GET /api/v1/etl/extract` | `EtlController` | LLM 实体提取 | 已实现 |
| `GET /api/v1/knowledge/ingest` | `KnowledgeController` | JSONL 灌入 Pinecone | 已实现 |

## 6. 当前架构中的关键差距

### 6.1 尚未引入 LangGraph4j

当前 `pom.xml` 没有 LangGraph4j / LangChain4j 依赖，代码中也没有：

- `TravelPlanState`
- `GraphInputRequest`
- `GraphResult`
- `LangGraphPlannerFacade`
- `StateGraph`
- Planner / Validator / Tool 节点
- 条件边、循环边、反思修正边

因此当前系统还不是图式工作流，只是 Spring Service 内部的 `switch-case` 路由。

### 6.2 新版三擎入口没有真正调用工具

`handleUserWorkflow` 中：

- `TOOL_WEATHER` / `TOOL_FLIGHT` 分支只是调用 Qwen 生成文字，没有 `.tools(weatherTools, flightTools)`。
- `PLAN_OR_RAG` 分支只是把地点、时间、关键词拼进 prompt，没有调用 `KnowledgeTools` 或 `VectorStore` 检索。

真实工具调用目前主要存在于旧入口 `chat()`。

### 6.3 前端仍然连接旧入口

`static/index.html` 调用的是：

```text
/api/v1/travel/chat
```

不是新版：

```text
/api/v1/agent/chat
```

所以用户在网页里体验到的是旧版“单模型全工具模式”，不是“三擎 + Gatekeeper 模式”。

### 6.4 RAG 已有工具，但尚未进入核心规划链

Pinecone 检索工具已实现，数据也已经有 144 条实体。但新版核心规划分支 `handlePlanOrRag` 没有检索知识库，也没有将检索结果注入 DeepSeek Pro。

当前只有旧版 `chat()` 在 `.tools(..., knowledgeTools)` 中给模型机会自主调用知识库。

### 6.5 缺少任务状态对象和多轮记忆

当前没有统一状态对象表达一次复杂旅行任务的生命周期。例如缺少：

- 用户原始输入
- Gatekeeper 结果
- 用户预算
- 日期范围
- 目的地列表
- 偏好与禁忌
- RAG 检索上下文
- 航班结果
- 天气结果
- 酒店结果
- 景点结果
- 子任务列表
- 规划草稿
- 校验错误
- 重试次数
- 最终答案

这正是 LangGraph4j 应该承载的核心状态。

### 6.6 缺少自动拆解、校验和循环

当前复杂规划是一次性让 DeepSeek Pro 回答，没有实现：

- 任务拆解。
- 多分支并行查询。
- 结果汇总。
- 预算校验。
- 日期和城市顺序校验。
- 信息不足时追问。
- 错误方案自动修正。
- 多轮反复调用。

### 6.7 缺少测试与评测集

当前没有 `src/test`。对 Agent 项目来说，至少需要：

- Gatekeeper 路由 JSON 解析测试。
- 外部 API Service 的 mock 测试。
- RAG 灌入与检索测试。
- Tool schema 是否可被 Spring AI 正确注册的测试。
- 端到端 Agent 场景测试。
- 一组固定旅行请求作为回归评测集。

### 6.8 生产化配置仍需整理

需要后续处理：

- Maven 源码编码显式设置为 UTF-8。
- ETL 原始数据目录改成配置项。
- GET 触发写操作的接口改为 POST。
- ETL 和 Knowledge 写入接口加鉴权。
- API Key 缺失时启动校验更明确。
- Redis 不可用时的降级策略。
- 前端 CDN 依赖的离线或构建方案。

## 7. 推荐目标架构

推荐采用“Spring AI 外围 + LangGraph4j 黑箱核心”的分层架构。

```text
Frontend
  -> AgentController
  -> MastermindAgent
  -> GatekeeperAgent
      -> DIRECT_CHAT：低成本直接回复
      -> TOOL_*：调用对应轻量工具分支
      -> PLAN_OR_RAG：进入 LangGraphPlannerFacade

LangGraphPlannerFacade
  -> 初始化 TravelPlanState
  -> RAG Retrieve Node
  -> Task Decompose Node
  -> Branch Dispatch Node
      -> Weather Branch Agent
      -> Flight Branch Agent
      -> Places Branch Agent
      -> Knowledge Branch Agent
  -> Synthesis Node
  -> Validator Node
      -> pass：Finalize Node
      -> fail：Refine / Re-dispatch Node
  -> 返回 GraphResult
```

Spring AI 继续负责：

- HTTP API。
- ChatModel Bean 装配。
- 外部 API Service。
- Redis 缓存。
- Pinecone VectorStore。
- Tool 方法暴露。
- 前端静态资源。

LangGraph4j 负责：

- 复杂任务状态流转。
- 节点编排。
- 条件分支。
- 自我校验循环。
- 多 Agent 子任务协作。
- 最终结果聚合。

## 8. 后续实施计划

### 阶段 0：先把当前工程收拢干净

目标：让现有能力稳定、入口清晰，避免继续在两个聊天入口之间分裂。

建议任务：

1. 在 `pom.xml` 增加统一编码配置：

```xml
<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
<maven.compiler.encoding>UTF-8</maven.compiler.encoding>
```

2. 将 `static/index.html` 的调用入口切换为新版：

```text
/api/v1/agent/chat
```

3. 暂时保留 `/api/v1/travel/chat` 作为 legacy/debug 入口，并在文档中明确。

4. 给 `GatekeeperResponse` 增加 `directReply` 字段，映射 `direct_reply`。

5. 将 `EtlController.INPUT_DIR` 从硬编码路径迁移到配置项：

```properties
travel.etl.xhs-input-dir=...
```

6. 将模型名统一写入文档和配置，确认实际使用 `qwen3.6-plus` 还是 `qwen3.6-max`。

7. 增加最小测试目录 `src/test`，先覆盖 Gatekeeper JSON 解析、DTO 映射、Service fallback。

### 阶段 1：让新版三擎入口具备真实工具能力

目标：`/api/v1/agent/chat` 不再只是路由，而是能在分支里真正调用工具和 RAG。

建议任务：

1. 在 `handleToolBranch` 中按意图注册对应工具：

```java
case "TOOL_WEATHER" -> branchChatClient.prompt()
    .system(...)
    .user(userMessage)
    .tools(weatherTools)
    .call()
    .content();

case "TOOL_FLIGHT" -> branchChatClient.prompt()
    .system(...)
    .user(userMessage)
    .tools(flightTools)
    .call()
    .content();
```

2. 在 `PLAN_OR_RAG` 中加入知识库检索。可以先用简单方式：

```java
String ragContext = knowledgeTools.searchTravelGuide(userMessage);
```

然后把 `ragContext` 注入 `coreChatClient` 的 system prompt。

3. 为地点/酒店/景点规划任务增加新的 intent，或者先让 `PLAN_OR_RAG` 内部可调用 `PlacesTools`。

4. 对 Gatekeeper 输出做二次校验：

- intent 不在枚举内时 fallback。
- locations/time/keywords 缺失时补空。
- JSON 解析失败时记录 rawResponse。

5. 把 `DIRECT_CHAT` 从固定回复升级为两档：

- 问候类：固定回复。
- 普通闲聊：低成本模型生成，但限制 maxTokens。

### 阶段 2：引入 LangGraph4j 黑箱编排

目标：把复杂规划从一次性 prompt 迁移到可循环状态图。

建议新增包：

```text
com.travel.agent.ai.graph
├── TravelPlanState.java
├── GraphInputRequest.java
├── GraphResult.java
├── LangGraphPlannerFacade.java
├── node
│   ├── RetrieveNode.java
│   ├── DecomposeNode.java
│   ├── BranchDispatchNode.java
│   ├── SynthesisNode.java
│   ├── ValidatorNode.java
│   └── FinalizeNode.java
└── branch
    ├── BranchTask.java
    ├── BranchResult.java
    ├── WeatherBranchAgent.java
    ├── FlightBranchAgent.java
    ├── PlacesBranchAgent.java
    └── KnowledgeBranchAgent.java
```

建议的 `TravelPlanState` 字段：

```java
public class TravelPlanState {
    private String userQuery;
    private GatekeeperResponse route;
    private List<String> destinations;
    private String travelTime;
    private String budget;
    private List<String> preferences;
    private String ragContext;
    private List<BranchTask> tasks;
    private Map<String, BranchResult> branchResults;
    private String draftPlan;
    private List<String> validationIssues;
    private int revisionCount;
    private String finalAnswer;
}
```

建议的图流程：

```text
START
  -> retrieve
  -> decompose
  -> branchDispatch
  -> synthesize
  -> validate
      -> if pass: finalize
      -> if fail and revisionCount < 2: branchDispatch or synthesize
      -> if fail and revisionCount >= 2: finalize_with_warnings
END
```

建议先做最小可用图：

1. `retrieve`：查 Pinecone，写入 `ragContext`。
2. `decompose`：让 DeepSeek Pro 输出结构化任务列表。
3. `synthesize`：根据 RAG 和用户输入生成草稿。
4. `validate`：检查时间、预算、地理顺序、是否遗漏用户约束。
5. `finalize`：输出 Markdown。

等最小图跑通后，再把天气、航班、酒店、景点分支接入 `branchDispatch`。

### 阶段 3：实现多分支 Agent 与工具分工

目标：把不同工具能力从“一个模型随便调”改成“明确的分支 Agent”。

建议分支：

| 分支 | 模型 | 工具 | 输入 | 输出 |
|---|---|---|---|---|
| WeatherBranchAgent | Qwen | `WeatherTools` | 城市、日期、上下文 | 天气摘要和风险提醒 |
| FlightBranchAgent | Qwen | `FlightTools` | 出发地、目的地、日期 | 航班候选与价格范围 |
| PlacesBranchAgent | Qwen | `PlacesTools` | 城市、入住日期、退房日期、兴趣偏好 | 酒店和景点候选 |
| KnowledgeBranchAgent | Qwen 或直接 VectorStore | `KnowledgeTools` | 目的地、关键词 | 防坑经验和 POI 背景 |
| Planner/Synthesis Agent | DeepSeek Pro | 无或只读上下文 | 所有 BranchResult | 最终方案 |

建议统一分支输入输出 DTO：

```java
public class BranchTask {
    private String taskId;
    private String type;
    private String objective;
    private Map<String, Object> params;
}

public class BranchResult {
    private String taskId;
    private String type;
    private boolean success;
    private String summary;
    private Object rawData;
    private List<String> warnings;
}
```

### 阶段 4：加入自我校验与反复调用

目标：实现“自动反馈和反复调用”。

Validator 节点至少检查：

- 用户要求的天数是否被满足。
- 预算是否被明确处理。
- 每天城市移动是否合理。
- 是否出现时间倒流或过期日期。
- 是否有缺失的关键参数，例如航班缺少出发机场。
- 是否把 RAG 防坑信息用于最终方案。
- 是否存在明显幻觉，例如引用了工具没有返回的数据。

Validator 输出建议结构：

```json
{
  "pass": false,
  "issues": [
    {
      "severity": "HIGH",
      "field": "budget",
      "message": "用户要求预算 1200 欧，但方案未估算交通和住宿总价"
    }
  ],
  "next_action": "REFINE_PLAN"
}
```

循环策略建议：

- 最多修正 2 次，避免无限调用。
- 高严重度问题回到 `branchDispatch` 或 `synthesize`。
- 低严重度问题进入 `finalize_with_warnings`。
- 如果缺少用户必要信息，返回追问而不是编造。

### 阶段 5：RAG 与数据管道生产化

目标：让知识库从开发期手动导入变成稳定可维护的资产。

建议任务：

1. 给实体 JSONL 增加稳定 ID，避免重复灌入 Pinecone。
2. Metadata 增加：

```text
source
note_id
entity_type
country
city
created_at
quality_score
```

3. 检索时按目的地做 metadata filter，避免问巴黎时混入瑞士内容。

4. 增加重建索引脚本或管理接口。

5. 把 ETL 写接口从 GET 改成 POST，并加开发期 token。

6. 建立数据质量检查：

- 空字段比例。
- 国家/城市识别准确率。
- POI/TIP/ROUTE 分类分布。
- 重复实体比例。

### 阶段 6：前端体验升级

目标：让用户真正感知 Agentic 工作流，而不是只看到等待中的聊天框。

建议任务：

1. 前端切换到 `/api/v1/agent/chat`。
2. 后端支持流式输出，优先考虑 Spring MVC `SseEmitter` 或 WebFlux。
3. 前端显示过程状态：

```text
正在识别意图
正在检索私有攻略
正在查询天气
正在查询航班
正在校验行程可行性
正在生成最终方案
```

4. 增加预算、日期、出发地、偏好等可选结构化输入控件，但仍保留自然语言输入为主。

5. 对最终行程使用更结构化的展示：

- 每日行程卡片。
- 交通段。
- 预算估算。
- 风险提醒。
- 可替代选项。

### 阶段 7：测试、观测与评测

目标：让 Agent 系统可迭代，而不是靠手感调 prompt。

建议测试：

- `GatekeeperAgent`：输入样例到 intent 的映射。
- `GatekeeperResponse`：`direct_reply` 反序列化。
- `FlightServiceImpl`：mock SerpApi 响应解析。
- `WeatherServiceImpl`：mock OpenWeatherMap 响应解析。
- `PlacesServiceImpl`：酒店和景点解析。
- `KnowledgeBaseService`：JSONL 到 Document 转换。
- `MastermindAgent`：不同 intent 的分支路由。
- `LangGraphPlannerFacade`：状态图最小闭环。

建议评测集：

```text
1. 闲聊：你是谁？
2. 天气：巴黎明天天气怎么样？
3. 航班：帮我查下 2026-06-10 都柏林飞巴黎的机票
4. RAG：巴黎有什么防偷防骗经验？
5. 简单规划：帮我安排巴黎 3 天
6. 复杂规划：1200 欧预算，10 天法意瑞，想避开人多景点
7. 信息缺失：我想下个月去欧洲，帮我安排
8. 多意图：查天气，再帮我安排罗马两天
```

建议观测日志：

- requestId
- userId/sessionId
- gatekeeper intent
- model name
- token usage
- tool calls
- graph node transitions
- validation issues
- total latency
- external API latency

## 9. 当前任务进度总结

### 已完成

- Spring Boot 项目骨架。
- Spring AI 多模型手动装配。
- DeepSeek Flash Gatekeeper 初版。
- DeepSeek Pro 复杂规划分支初版。
- Qwen 默认分支模型。
- 旧版 Qwen + 全工具 Function Calling 入口。
- 航班、天气、酒店、景点、知识库工具。
- SerpApi / OpenWeatherMap 外部 API 服务。
- Redis 缓存配置与部分服务缓存。
- Pinecone 向量库灌入与检索。
- 小红书 JSONL 清洗、LLM 实体提取、向量灌入管道。
- 静态聊天前端 MVP。
- Gatekeeper 测试接口。

### 部分完成

- 新版三擎编排入口：已能路由，但工具和 RAG 未真正接入分支。
- RAG：已能灌入和检索，但未进入新版核心规划链。
- 多模型架构：Bean 隔离完成，但 LangGraph4j 编排尚未完成。
- 工具调用：旧入口可用，新入口未完成。
- 前端：聊天 UI 可用，但仍接旧入口。

### 未完成

- LangGraph4j 依赖与状态图。
- `TravelPlanState` 等图状态 DTO。
- `LangGraphPlannerFacade` 黑箱门面。
- Planner / Validator / Branch / Finalize 节点。
- 多分支 Agent 的结构化输入输出。
- 自动任务拆解。
- 自动校验与修正循环。
- 多轮会话记忆。
- 预算计算。
- 日期范围和城市移动可行性校验。
- 流式响应。
- 系统化测试与评测集。
- 生产化鉴权、配置、部署方案。

## 10. 下一步优先级

建议不要马上大规模重构。最稳的顺序是：

1. 先让 `/api/v1/agent/chat` 接上真实工具和 RAG。
2. 前端切到 `/api/v1/agent/chat`。
3. 补 `GatekeeperResponse.directReply` 和最小测试。
4. 引入 LangGraph4j，只做最小 `retrieve -> plan -> validate -> finalize` 图。
5. 再把天气、航班、酒店、景点作为分支 Agent 接入图。
6. 最后做循环修正、流式状态、预算和多轮记忆。

按这个路线，项目可以从“已有工具的 Spring AI Chat 应用”平滑升级为“真正有状态、有分工、有校验循环的 Agentic 旅行规划系统”。
