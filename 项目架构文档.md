# 欧洲旅游 Agentic 项目架构文档

更新时间：2026-06-04

本文档记录当前项目在第八阶段后的真实架构、已完成能力、核心流程、技术债和后续计划。当前项目名称为 `AgentProjectTrip`，目标是构建一个允许用户通过自然语言或结构化表单生成欧洲旅行方案的 Agentic 网站。

## 1. 项目目标

项目最终目标：

```text
用户用自然语言描述旅行需求
  -> 系统自动识别意图
  -> 简单问题低成本直接回答
  -> 复杂规划进入核心 Agent 工作流
  -> 抽取并确认旅行需求表
  -> 调用 RAG、天气、景点、航班等分支能力
  -> 核心模型生成初稿
  -> 风险推理和自动修订
  -> 输出可执行旅行计划
  -> 用户继续用自然语言修改计划
  -> 系统保存版本和用户记忆
```

这个项目不是普通聊天机器人，而是“Spring AI 外围能力 + Agent Graph 核心编排 + 数据库持久化 + 前端可视化调试”的组合系统。

## 2. 当前技术栈

后端：

```text
Java 21
Spring Boot 3.3.13
Spring AI 1.0.5
Spring Web
Spring JDBC
Spring Cache
Redis
MySQL / MAMP
Pinecone VectorStore
OpenAI-compatible ChatModel
```

外部服务：

```text
DeepSeek：Gatekeeper / Core Planner
Qwen / 百炼：分支模型、ETL、部分工具 Agent
OpenWeatherMap：天气
SerpApi：航班、酒店、景点
Pinecone：私有旅行知识库
MySQL：持久化需求、计划、记忆、任务、额度
```

前端：

```text
Spring Boot static HTML
agent-lab.html 调试台
原生 JavaScript
HTTP 轮询异步任务状态
```

## 3. 当前项目规模

```text
main Java 文件：121 个
ai.graph 相关 Java 文件：63 个
Graph 节点：16 个
test Java 文件：29 个
Maven 测试：103 个通过
MySQL 表：7 张
```

当前已经不是早期 Demo，而是一个具备完整任务链路的 Agentic MVP。

## 4. 当前工程结构

```text
com.travel.agent
├── web
│   ├── AgentController
│   ├── TravelController
│   ├── RequirementController
│   ├── PlanController
│   ├── MemoryController
│   ├── GenerationJobController
│   ├── EtlController
│   ├── KnowledgeController
│   └── DebugController
├── core
│   ├── dto
│   ├── service
│   └── etl
└── ai
    ├── config
    ├── agents
    ├── dto
    ├── tools
    ├── generation
    ├── memory
    └── graph
        ├── model
        ├── node
        ├── store
        └── LangGraphPlannerFacade
```

当前包结构已经完成了一次重要收拢：

- HTTP 入口统一在 `web`。
- 传统 Java 服务统一在 `core`。
- Agent、模型、Graph、记忆、异步生成统一在 `ai`。

## 5. 多模型架构

当前多模型配置集中在：

```text
com.travel.agent.ai.config.MultiModelConfig
com.travel.agent.ai.AiModelBeanNames
```

当前模型角色：

| 角色 | Bean | 用途 |
|---|---|---|
| Gatekeeper | `gatekeeperChatModel` | 低成本意图识别、简单直答 |
| Core | `coreChatModel` | 复杂规划、初稿、修订、风险推理 |
| Branch | `branchChatModel` | 分支任务、工具 Agent、ETL 抽取 |

架构判断：

- 多模型 Bean 隔离已经完成。
- Gatekeeper / Core / Branch 三类模型职责已经明确。
- 还没有引入模型调用成本统计和 token 级审计。

## 6. 当前已完成阶段

### 第一阶段：直线 Graph 流程

已完成：

- `LangGraphPlannerFacade` 初版。
- `TravelPlanState` 状态对象。
- `GraphInputRequest` / `GraphResult`。
- `InitStateNode`。
- `RetrieveKnowledgeNode`。
- `PlanDraftNode`。
- `ValidateDraftNode`。
- `FinalizeAnswerNode`。

意义：

- 复杂规划不再只是 Controller 中一次性 prompt。
- 初步形成“输入 -> RAG -> 初稿 -> 校验 -> 输出”的工作流。

### 第二阶段：前置澄清和 pending 状态

已完成：

- `PreClarifyCheckNode`。
- `ClarifyQuestionNode`。
- `MergeClarificationNode`。
- `ConversationStateStore`。
- `InMemoryConversationStateStore`。
- `JdbcConversationStateStore`。

意义：

- 用户输入过于模糊时，系统可以先追问。
- 支持保存 pending 状态。
- 用户补充信息后可以恢复原任务。

### 第三阶段：分支任务派发

已完成：

- `BranchDispatchNode`。
- `BranchExecuteNode`。
- `BranchTask`。
- `BranchResult`。
- `BranchTaskType`。
- `BranchAgentFacade`。

已接入分支：

```text
KNOWLEDGE
WEATHER
PLACES
FLIGHT
```

意义：

- 核心 Agent 可以把任务分发给分支能力。
- 分支 Agent 调用工具后把结果回填给 Planner。
- 这是核心 Agent 与分支 Agent 反复交互的第一版基础。

### 第四阶段：风险推理和自动修订

已完成：

- `TripRiskReasoningNode`。
- `RiskAssessment`。
- `RiskIssue`。
- `RiskIssueType`。
- `RiskSeverity`。
- `PlanRevisionNode`。

能力：

- 输出前审查天气、预算、交通、拥挤、行程过载、目的地不匹配等风险。
- 发现可修正问题时自动进入修订。
- 修订后再做二次审查。

意义：

- 系统已经不只是生成答案，而是会在输出前“想一遍”。
- 这是从普通 Planner 走向 Agentic Reasoning 的关键一步。

### 第五阶段：结构化需求表和生成门控

已完成：

- `RequirementExtractionAgent`。
- `TravelRequirementSpec`。
- `RequirementValidationNode`。
- `RequirementStatus`。
- `RequirementController`。
- `RequirementDraftRequest` / `RequirementDraftResponse`。
- `RequirementConfirmRequest`。
- `RequirementGenerateResponse`。
- `CreditService`。
- `InMemoryCreditService`。
- `JdbcCreditService`。

能力：

- 用户自然语言先抽取成表单。
- 表单完整后才能确认。
- 确认后完整生成消耗 1 次额度。
- 避免用户乱写一句话就触发高成本 Graph。

意义：

- 极大降低无效生成。
- 为后续正式付费和生产级风控打基础。

### 第六阶段：计划版本化和自然语言修改

已完成：

- `TravelPlanRecord`。
- `TravelPlanVersion`。
- `TravelPlanStore`。
- `InMemoryTravelPlanStore`。
- `JdbcTravelPlanStore`。
- `PlanController`。
- `PlanModificationAgent`。
- `PlanModificationDecision`。
- `PlanModificationIntent`。
- `PlanLocalRevisionNode`。
- `RequirementPatchNode`。

能力：

- 生成成功后保存 `planId`。
- 用户可以针对已有计划自然语言修改。
- 局部修改会生成新版本。
- 核心需求变化会回到需求表确认流程。

意义：

- 旅行规划从“一次性文本”变成“可持续协作对象”。

### 阶段 6.5：前端调试台

已完成：

- `agent-lab.html`。
- 自然语言输入。
- 需求表编辑。
- 需求确认。
- 规划结果展示。
- 自然语言修改计划。
- 原始 JSON 查看。

意义：

- 当前开发可以直接在页面中测试完整系统。
- 不再完全依赖浏览器地址栏和 Postman。

### 第七阶段：MySQL 持久化和用户记忆

已完成：

- MySQL 数据库 `agent_project_trip`。
- `schema-mysql.sql`。
- `conversation_sessions`。
- `travel_requirements`。
- `travel_plans`。
- `travel_plan_versions`。
- `user_memories`。
- `credit_accounts`。
- `generation_jobs`。
- JDBC Store 和内存 Store 双实现。
- `UserMemory`。
- `UserMemoryStore`。
- `JdbcUserMemoryStore`。
- `UserMemoryService`。
- `MemoryController`。

能力：

- 需求、计划、版本、会话、记忆、额度都可以落 MySQL。
- 用户确认需求后自动同步偏好记忆。
- Graph 规划前可以加载用户记忆上下文。

意义：

- 系统有了长期状态。
- 开始具备“越用越懂用户”的基础。

### 阶段 7.5：前端接入用户记忆

已完成：

- `agent-lab.html` 新增用户记忆区域。
- 查询记忆。
- 新增记忆。
- 禁用记忆。
- 确认需求后自动刷新记忆。

意义：

- 用户记忆不再只是数据库能力，已经能在页面调试和观察。

### 第八阶段：异步生成任务与进度可视化

已完成：

- `GenerationJob`。
- `GenerationJobStatus`。
- `GenerationJobStage`。
- `GenerationJobStore`。
- `InMemoryGenerationJobStore`。
- `JdbcGenerationJobStore`。
- `PlanGenerationService`。
- `AsyncPlanGenerationService`。
- `PlanGenerationOutcome`。
- `GenerationJobCreateResult`。
- `GenerationJobController`。
- `GenerationJobCreateResponse`。
- `GenerationJobResponse`。
- `AsyncGenerationConfig`。
- 前端异步生成和轮询状态。

能力：

- 生成完整规划不再阻塞 HTTP 请求。
- 前端先拿 `jobId`。
- 后端后台执行 Graph。
- 前端轮询 `/api/v1/jobs/{jobId}`。
- 成功后自动加载 `planId`。
- 重复点击同一需求时返回已有运行中任务，降低重复扣费风险。

意义：

- 系统从开发 Demo 进一步接近真实产品体验。
- 为后续取消、重试、队列、任务审计和支付流水打基础。

## 7. 当前核心业务流程

### 7.1 表单式完整规划流程

```text
用户自然语言
  -> RequirementController.draft
  -> RequirementExtractionAgent
  -> TravelRequirementSpec
  -> RequirementValidationNode
  -> 前端展示表单
  -> 用户人工确认
  -> RequirementController.confirm
  -> UserMemoryService.syncFromConfirmedRequirement
  -> RequirementController.generateAsync
  -> GenerationJob
  -> AsyncPlanGenerationService
  -> PlanGenerationService
  -> LangGraphPlannerFacade
  -> TravelPlanStore
  -> GenerationJob SUCCEEDED
  -> 前端加载 planId
```

### 7.2 Graph 规划流程

```text
GraphInputRequest
  -> InitStateNode
  -> UserMemoryService
  -> PreClarifyCheckNode
  -> RetrieveKnowledgeNode
  -> BranchDispatchNode
  -> BranchExecuteNode
  -> PlanDraftNode
  -> ValidateDraftNode
  -> TripRiskReasoningNode
  -> PlanRevisionNode
  -> FinalizeAnswerNode
  -> GraphResult
```

### 7.3 计划修改流程

```text
用户修改自然语言
  -> PlanController.modify
  -> PlanModificationAgent
      -> LOCAL_REVISION
          -> PlanLocalRevisionNode
          -> TravelPlanStore.addVersion
      -> REQUIREMENT_CHANGE
          -> RequirementPatchNode
          -> RequirementValidationNode
          -> 回到需求表确认
      -> CLARIFICATION
          -> 追问用户
      -> DIRECT_COMMENT
          -> 不修改计划
```

## 8. 当前 API 清单

主要接口：

| 接口 | 用途 |
|---|---|
| `GET /api/v1/agent/chat` | 主 Agent 聊天入口 |
| `GET /api/v1/travel/chat` | 旧版旅行聊天入口 |
| `POST /api/v1/requirements/draft` | 自然语言抽取需求表 |
| `PUT /api/v1/requirements/{requirementId}` | 保存需求表 |
| `POST /api/v1/requirements/{requirementId}/confirm` | 确认需求表 |
| `POST /api/v1/requirements/{requirementId}/generate` | 同步调试生成 |
| `POST /api/v1/requirements/{requirementId}/generate-async` | 异步生成完整规划 |
| `GET /api/v1/jobs/{jobId}` | 查询异步生成任务 |
| `GET /api/v1/jobs?sessionId=...` | 查询会话最近任务 |
| `GET /api/v1/plans/{planId}` | 查询当前计划 |
| `GET /api/v1/plans/{planId}/versions/{version}` | 查询指定版本 |
| `POST /api/v1/plans/{planId}/modify` | 自然语言修改计划 |
| `GET /api/v1/memories` | 查询用户记忆 |
| `POST /api/v1/memories` | 新增用户记忆 |
| `DELETE /api/v1/memories/{memoryId}` | 禁用用户记忆 |
| `GET /api/v1/etl/clean-xhs` | 小红书 JSONL 清洗 |
| `GET /api/v1/etl/extract` | LLM 实体提取 |
| `GET /api/v1/knowledge/ingest` | 知识库灌入 |

当前主要测试页面：

```text
http://localhost:8080/agent-lab.html
```

## 9. 数据库架构

当前持久化模式：

```properties
travel.persistence.type=jdbc
```

当前 MySQL：

```text
host: localhost
port: 3306
database: agent_project_trip
user: root
```

表结构：

```text
conversation_sessions
travel_requirements
travel_plans
travel_plan_versions
user_memories
credit_accounts
generation_jobs
```

当前数据库已经支撑：

- 会话 pending 状态。
- 结构化需求表。
- 计划主记录。
- 计划版本。
- 用户记忆。
- 生成额度。
- 异步生成任务。

## 10. LangGraph4j 完成度判断

必须准确区分两个概念：

### 10.1 真实 LangGraph4j 框架接入

当前完成度：

```text
0%-5%
```

原因：

- `pom.xml` 里没有 `langgraph4j` 依赖。
- 当前没有使用 `StateGraph`。
- 没有使用框架级节点、边、条件边、图编译。
- 当前图流程由 `LangGraphPlannerFacade` 手动串联。

### 10.2 Agent Graph 编排能力

当前完成度：

```text
55%-65%
```

原因：

已经完成：

- 状态对象。
- 节点拆分。
- RAG。
- 分支任务。
- 分支工具执行。
- 前置澄清。
- pending 恢复。
- 风险推理。
- 自动修订。
- 计划版本。
- 用户记忆。
- 异步任务。

但尚未完成：

- 标准图 DSL。
- 条件边统一抽象。
- 并发分支。
- 节点级 trace。
- token 成本统计。
- 节点重试。
- 任务取消。
- 分布式队列。
- 多用户权限。

### 10.3 生产级 Agent 系统

当前完成度：

```text
35%-40%
```

原因：

当前已经能跑复杂规划，但生产级还需要：

- 登录和权限。
- 支付和订单。
- 成本审计。
- Prompt 版本管理。
- 评测集。
- 观测系统。
- 工具调用超时和重试。
- 数据质量体系。
- 前端正式产品 UI。

## 11. 当前主要技术债

### 11.1 LangGraphPlannerFacade 会继续膨胀

现在所有节点调用顺序仍在一个 Java Service 里手动控制。节点越多，这个类越容易变成“隐形状态机”。

建议：

- 下一阶段设计真正的图结构。
- 把当前流程迁移到 LangGraph4j 或自定义 Graph DSL。
- 用条件边表达澄清、修订、失败、完成。

### 11.2 分支任务还不是真正并发

当前分支能力已经有，但还不是成熟的并行 Agent 执行框架。

建议：

- 给 `BranchExecuteNode` 加超时控制。
- 分支结果引入置信度和来源。
- 后续支持并发执行。

### 11.3 工具失败和数据质量仍会影响最终方案

外部 API 可能 404、522、超时、限流。当前系统能降级，但还不够细。

建议：

- 给每个工具结果标记 `source`、`freshness`、`success`、`error`。
- Planner 必须显式区分“已查到”和“未查到”。

### 11.4 预算和可行性校验还不够硬

当前风险推理能发现很多问题，但预算计算、交通耗时、营业时间、天气对行程影响还没有强约束算法。

建议：

- 建立预算估算模块。
- 建立交通段模型。
- 建立天气风险规则。
- 建立每日负载评分。

### 11.5 前端仍是调试台

`agent-lab.html` 很适合开发测试，但不是正式用户产品。

建议：

- 下一步可以做正式需求表页面。
- 把任务进度、计划卡片、版本修改做成真实 UI。

## 12. 下一阶段建议

不建议马上继续堆功能。当前最值得做的是把 Graph 编排标准化。

推荐第九阶段目标：

```text
第九阶段：标准化 Graph 执行框架与节点可观测性
```

第九阶段建议任务：

1. 调研并引入真正 `langgraph4j`，或先写项目内 `StateGraph` 抽象。
2. 把当前 `LangGraphPlannerFacade` 的手写流程表达成图：

```text
START
  -> INIT
  -> PRE_CLARIFY
  -> RETRIEVE
  -> BRANCH_DISPATCH
  -> BRANCH_EXECUTE
  -> PLAN_DRAFT
  -> VALIDATE
  -> RISK_REASONING
  -> REVISION
  -> FINALIZE
  -> END
```

3. 把条件逻辑改成边：

```text
PRE_CLARIFY -> CLARIFY or RETRIEVE
VALIDATE -> CLARIFY or RISK_REASONING
RISK_REASONING -> REVISION or FINALIZE
REVISION -> RISK_REASONING or FINALIZE
```

4. 增加节点执行 trace：

```text
nodeName
status
startedAt
finishedAt
durationMs
inputSummary
outputSummary
errorMessage
modelName
```

5. 把 trace 写入 `generation_jobs.result_json` 或新增 `generation_job_events` 表。

6. 前端展示节点执行轨迹，而不仅是粗粒度 job stage。

## 13. 当前项目评价

当前项目已经完成了 Agentic 系统最难的一部分雏形：不是调用一次模型，而是把用户需求、工具、RAG、风险审查、记忆、版本、异步任务串成一个闭环。

当前最准确的定位是：

```text
一个基于 Spring AI 的旅行 Agentic MVP，
内部已经实现类 LangGraph 工作流，
但尚未迁移到真正 LangGraph4j 框架。
```

下一步应当从“继续加能力”转向“把已验证能力工程化成标准图框架”。这样项目后续才不会在节点越来越多时失控。
