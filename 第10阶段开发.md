# 第10阶段开发：需求表单体验完善

创建日期：2026-06-14

对应总控文档：

```text
阶段9-17开发规划与维护清单.md
```

当前状态：

```text
已完成，人工测试通过
```

## 0.1 当前开发记录

### 2026-06-14：人工测试通过，阶段完成

用户已在正式前端页面完成手动测试，并确认第 10 阶段测试通过。

本阶段正式归档文档：

```text
阶段 10 需求表单体验完善.md
```

### 2026-06-14：第 10 阶段代码第一版

本轮已按人工确认的产品策略完成第一版代码：

```text
新增 POST /api/v1/requirements：同意，已完成
新增 TravelRequirementSpec.specialNotes：同意，已完成
预算金额缺失：blocking，已完成
出发地缺失：warning，沿用并明确，已完成
预算是否包含国际机票为空：warning，已完成
```

后端完成内容：

```text
TravelRequirementSpec 新增 specialNotes 字段，并在 setter 中清洗空白值
RequirementExtractionAgent 支持模型抽取 specialNotes
RequirementExtractionAgent 规则兜底识别带老人、带孩子、少走路、不想开车、不要太累等特殊要求
RequirementValidationNode 将国际机票预算边界从 blocking 改为 warning
RequirementValidationNode 保持预算金额和预算币种为 blocking
RequirementController 新增 POST /api/v1/requirements，用于手动填表创建需求草稿
InitStateNode 将 specialNotes 写入关键词，供分支派发和兼容逻辑参考
PlanDraftNode 将 specialNotes 注入 Planner prompt，确保最终方案遵守特殊要求
RequirementPatchNode 复制需求表时保留 specialNotes
RequirementValidationNodeTest 已同步新规则
```

前端完成内容：

```text
travel-planner.html 将需求表分成基础信息、预算信息、旅行偏好
新增 specialNotesInput 特殊要求输入框
新增字段级 hint 容器
新增 confirmReason 确认原因提示
agent-app.js 支持没有 requirementId 时直接 POST /api/v1/requirements 创建草稿
agent-app.js 新增 requirementDirty 状态，防止未保存修改直接确认
agent-app.js 新增字段级缺失 / warning 提示渲染
agent-app.js 新增本地轻量校验和币种大写归一化
app.css 新增字段分组、字段缺失、字段 warning、确认原因样式
静态资源版本更新为 20260614-4
```

已执行验证：

```text
node --check AgentProjectTrip/src/main/resources/static/agent-app.js
通过

HTML / JS id 绑定检查
通过，绑定的 49 个 id 全部存在；progressBox 是纯样式容器，不需要绑定

mvn test
通过，Tests run: 103, Failures: 0, Errors: 0, Skipped: 0
```

## 0. 本阶段一句话目标

第 10 阶段的目标是：把第 9 阶段已经能跑通的“需求确认表”，打磨成真正可靠的生成前入口。

用户应该可以：

```text
自然语言输入 -> 自动填表 -> 看懂缺什么 -> 手动补齐 -> 系统即时提醒风险 -> 确认后再生成
```

系统应该避免：

```text
用户随便一句话就进入高成本 Graph
关键信息缺失却仍然扣费生成
用户以为表单改了，但实际上没有保存
模型误解时间、天数、预算、人数、机票是否包含等关键字段
```

## 1. 本阶段核心思想

第 10 阶段不急着增加更多 Agent 分支，也不迁移 LangGraph4j。

本阶段先把“生成前的信息质量”做好。

原因很简单：

```text
Agent 规划质量 = 用户输入质量 + 结构化需求质量 + 工具结果质量 + 模型推理质量
```

现在第 9 阶段已经有正式页面和完整流程，但需求表还比较基础。
如果需求表质量不够，后面的 Graph、BranchDispatch、RiskReasoning 再强，也会被错误输入拖偏。

所以第 10 阶段要把 `TravelRequirementSpec` 变成产品入口的“唯一真相源”。

## 2. 本阶段不做什么

为了控制阶段边界，第 10 阶段明确不做：

```text
不接入真实支付
不做登录注册
不做航班分支真实接入
不把 BranchDispatch 改成模型驱动
不新增酒店 / 签证 / 预算分支 Agent
不迁移 LangGraph4j
不大改数据库 schema，除非现有字段无法表达需求
不做复杂多旅客画像建模
```

本阶段可以做少量后端字段和校验增强，但必须围绕“需求表单体验”服务。

## 3. 当前代码基础

### 3.1 当前前端入口

```text
AgentProjectTrip/src/main/resources/static/travel-planner.html
AgentProjectTrip/src/main/resources/static/app.css
AgentProjectTrip/src/main/resources/static/agent-app.js
```

当前页面已经有这些字段：

```text
destinations
departureCity
startDateText
startDate
durationDays
travelerCount
budgetAmount
budgetCurrency
budgetIncludesInternationalFlight
travelStyle
accommodationPreference
transportPreference
preferences
avoidances
```

### 3.2 当前后端核心模型

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelRequirementSpec.java
```

当前已经包含：

```text
requirementId
sessionId
originalMessage
destinations
departureCity
startDateText
startDate
durationDays
travelerCount
budgetAmount
budgetCurrency
budgetIncludesInternationalFlight
preferences
avoidances
travelStyle
accommodationPreference
transportPreference
status
missingFields
warnings
```

### 3.3 当前需求抽取和校验入口

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/RequirementExtractionAgent.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/RequirementValidationNode.java
AgentProjectTrip/src/main/java/com/travel/agent/web/RequirementController.java
```

### 3.4 当前结论

现有字段已经能支撑第 10 阶段第一版。

所以本阶段默认不新增：

```text
TravelerProfile.java
BudgetPreference.java
```

只有当代码实现时发现 `TravelRequirementSpec` 明显变臃肿，才考虑拆分。

## 4. 当前问题

### 4.1 表单字段还像“数据录入”

现在表单能用，但还不够像产品。

例如：

```text
目的地只是普通文本输入
偏好和避开项只是逗号分隔
预算币种需要用户自己输入
机票是否包含只有简单下拉
没有字段级解释
没有字段级缺失提示
没有字段级风险提示
```

### 4.2 缺失项提示还不够可操作

当前有 `validationBox`，但用户看到的是整体提示。

更理想的是：

```text
哪个字段缺失，就在那个字段附近提示
哪个字段有风险，就在那个字段附近提示
确认按钮为什么不能点，要说清楚
```

### 4.3 用户手动填表入口还不够清晰

第 9 阶段主要流程是“先自然语言输入，再自动填表”。

但正式产品里用户也应该可以：

```text
不输入自然语言，直接填表
填完以后保存为一张需求表
再确认并生成
```

这一步目前还不够顺。

### 4.4 预算和时间风险没有提前暴露

例如：

```text
10天玩法意，预算1200欧，不含国际机票
欧洲多国20天，预算3000人民币
下个月去欧洲，但是没有具体目的地和天数
```

这类信息不一定阻塞生成，但应该提前提醒用户：

```text
预算可能偏低
时间表达仍然模糊
目的地范围太宽
天数和目的地数量可能不匹配
预算是否包含国际机票会严重影响方案
```

## 5. 本阶段最终用户流程

目标流程：

```text
1. 用户打开 travel-planner.html
2. 用户可以选择：
   A. 输入自然语言，让系统整理需求
   B. 直接手动填写需求表
3. 页面把需求字段分成几个清晰区域
4. 用户填写或修改字段
5. 页面即时显示字段缺失、字段风险和确认条件
6. 用户点击保存
7. 后端重新校验
8. 页面展示后端校验结果
9. 用户确认需求
10. 确认后表单锁定
11. 用户生成完整方案
```

## 6. 本阶段功能拆分

### 6.1 需求表分组

当前所有字段在一个表单网格里。

第 10 阶段建议分组为：

```text
基础信息
  目的地
  出发地
  出行时间
  开始日期
  旅行天数
  人数

预算信息
  预算金额
  预算币种
  是否包含国际机票

旅行偏好
  旅行风格
  住宿偏好
  交通偏好
  喜欢的内容
  想避开的内容
```

好处：

```text
用户更容易理解自己正在填什么
后续增加酒店、签证、航班字段时有自然位置
字段级提示可以就近显示
```

### 6.2 字段级缺失提示

新增前端函数：

```text
renderFieldHints()
```

职责：

```text
读取 state.requirementValidation.missingFields
读取 state.requirementSpec.missingFields
把缺失提示显示到对应字段附近
给对应 label 增加缺失样式
```

第一版字段映射：

```text
destinations -> 目的地
durationDays -> 旅行天数
budgetAmount -> 预算金额
budgetCurrency -> 预算币种
budgetIncludesInternationalFlight -> 是否包含国际机票
```

是否把 `startDateText` 或 `startDate` 作为阻塞项，需要以后端校验为准。

### 6.3 字段级风险提示

新增前端函数：

```text
renderFieldWarnings()
```

职责：

```text
把预算偏低、时间模糊、目的地过宽、天数偏短等 warning 展示到表单区域
```

第一版可以先使用整体 warning 列表，不强行做复杂 NLP 匹配。

建议展示方式：

```text
表单顶部显示总体风险
关键字段下方显示轻量提示
确认按钮旁边说明“有警告但仍可确认”或“缺失字段不能确认”
```

### 6.4 直接手动建需求表

当前前端保存需求依赖 `requirementId`。

第 10 阶段要补齐：

```text
用户没有输入自然语言时，也可以先填表
点击“保存修改”时，如果没有 requirementId，前端走一个创建草稿流程
创建成功后拿到 requirementId
再继续确认
```

实现方式有两个选择：

```text
方案 A：前端用表单内容拼出 originalMessage，调用 /api/v1/requirements/draft
方案 B：后端新增 POST /api/v1/requirements 接口，直接保存手动填写的 TravelRequirementSpec
```

推荐方案：

```text
方案 B
```

原因：

```text
手动填表不应该伪造成自然语言输入
后端直接保存 TravelRequirementSpec 更干净
后续产品里“填表生成”和“聊天生成”本来就是两个入口
```

预计新增接口：

```text
POST /api/v1/requirements
```

语义：

```text
创建一张手动填写的需求草稿
不调用 LLM
不扣费
保存后返回 RequirementDraftResponse
```

### 6.5 前端即时校验

新增前端函数：

```text
validateFormLocally()
```

职责：

```text
用户还没点保存时，先做浏览器本地校验
本地校验只负责明显错误，不代替后端最终校验
```

第一版本地规则：

```text
目的地不能为空
旅行天数必须大于 0
人数必须大于 0
预算金额不能为负数
预算金额存在时必须有币种
币种统一转成大写
目的地、偏好、避开项去重和去空格
```

### 6.6 输入控件优化

建议改造：

```text
预算币种：文本框 -> select
旅行风格：文本框 -> select + 可输入扩展暂不做
住宿偏好：文本框 -> select
交通偏好：文本框 -> select
喜欢内容 / 避开内容：先保留逗号输入，但增加提示和自动清洗
```

第一版可选值：

```text
budgetCurrency:
  EUR
  CNY
  USD
  GBP

travelStyle:
  慢游
  小众
  深度游
  高舒适度
  低预算
  亲子

accommodationPreference:
  青旅
  民宿
  经济型酒店
  中档酒店
  高舒适度酒店

transportPreference:
  火车
  公共交通
  少换乘
  自驾
  飞机优先
```

注意：

```text
如果后端已经抽取出不在选项里的自由文本，前端不能直接丢失。
第一版可以保留文本输入，只在旁边加快捷按钮。
如果直接改 select，需要支持“其他”。
```

推荐第一版：

```text
不把所有文本框硬改成 select。
先增加快捷按钮 / datalist。
```

原因：

```text
旅行偏好非常开放，硬下拉容易限制用户表达。
```

### 6.7 确认按钮解释

当前确认按钮禁用时，用户可能不知道为什么不能点。

第 10 阶段要增加：

```text
确认按钮旁边显示原因
如果缺字段：请先补齐目的地 / 天数 / 预算等
如果只有 warning：可以确认，但建议检查提醒
如果已确认：需求已锁定，如需修改请点击继续编辑
```

### 6.8 表单脏状态

新增状态：

```text
requirementDirty
```

含义：

```text
用户修改了表单，但还没有保存。
```

用途：

```text
如果表单有未保存修改，不允许直接确认
如果表单已确认后点继续编辑，进入 dirty=false 的草稿态
用户改动字段后，显示“有未保存修改”
```

这能避免：

```text
用户改了表单，但忘记点保存，就直接生成旧需求。
```

## 7. 后端计划

### 7.1 RequirementController

预计修改文件：

```text
AgentProjectTrip/src/main/java/com/travel/agent/web/RequirementController.java
```

预计新增方法：

```text
createManualDraft(...)
```

接口：

```text
POST /api/v1/requirements
```

职责：

```text
接收前端手动填写的 TravelRequirementSpec
补齐 requirementId、sessionId、status
调用 RequirementValidationNode 校验
保存到 RequirementStore
返回 RequirementDraftResponse
```

失败策略：

```text
请求体为空 -> 400
sessionId 为空 -> 后端可兜底生成或使用 default-session，具体沿用现有规则
字段不完整 -> 仍保存草稿，但 readyToConfirm=false
```

### 7.2 RequirementValidationNode

预计修改文件：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/RequirementValidationNode.java
```

增强方向：

```text
字段缺失原因更清晰
warning 更适合前端展示
预算 / 天数 / 目的地数量做基础一致性检查
```

第一版规则建议：

```text
目的地为空 -> blocking
天数为空或 <=0 -> blocking
预算为空 -> blocking 或 warning，需要根据产品策略决定
预算币种为空但预算金额存在 -> blocking
预算是否包含国际机票为空 -> warning
目的地只有“欧洲”这种大区域 -> warning
目的地数量多但天数很少 -> warning
预算明显偏低 -> warning
```

注意：

```text
第 10 阶段不要把规则写得过死。
预算偏低只能 warning，不要随便阻塞用户。
```

### 7.3 TravelRequirementSpec

预计修改文件：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelRequirementSpec.java
```

当前字段暂时够用。

可能新增字段：

```text
specialNotes
```

用途：

```text
保存用户不能归类到固定字段的特殊要求。
例如：不想开车、带老人、每天不要超过2个景点、需要无障碍交通。
```

是否新增该字段，需要代码实现前再确认。

推荐：

```text
本阶段新增 specialNotes。
```

原因：

```text
当前没有“特殊要求”字段，很多用户需求只能塞进 preferences 或 avoidances。
specialNotes 可以保持表单开放性，又不需要立刻拆复杂画像类。
```

### 7.4 RequirementExtractionAgent

预计修改文件：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/RequirementExtractionAgent.java
```

增强方向：

```text
Prompt 要求模型抽取 specialNotes
规则兜底也识别“带老人、带孩子、不想开车、少走路、不要太累”等表达
```

## 8. 前端计划

### 8.1 travel-planner.html

预计修改：

```text
把需求表按基础信息 / 预算信息 / 旅行偏好分组
增加 specialNotes 输入框
给每个字段增加 hint 容器
增加“直接填写表单”路径说明
增加确认按钮原因提示
```

### 8.2 app.css

预计修改：

```text
字段分组样式
字段缺失样式
字段 warning 样式
快捷选项按钮样式
表单脏状态提示样式
```

### 8.3 agent-app.js

预计新增或修改函数：

```text
createManualRequirement()
validateFormLocally()
renderFieldHints()
renderConfirmReason()
markRequirementDirty()
normalizeFormValues()
renderQuickChoices()
```

预计修改函数：

```text
bindElements()
bindEvents()
saveRequirement()
confirmRequirement()
fillRequirementForm()
readSpecFromForm()
renderRequirementButtons()
renderValidation()
persistState()
restoreState()
resetFlow()
```

## 9. 数据流设计

### 9.1 自然语言入口

```text
用户输入自然语言
-> draftRequirement()
-> POST /api/v1/requirements/draft
-> RequirementExtractionAgent
-> RequirementValidationNode
-> RequirementStore.save()
-> 前端 fillRequirementForm()
-> 用户检查和补充
```

### 9.2 手动填表入口

```text
用户直接填写表单
-> saveRequirement()
-> 如果没有 requirementId：
   POST /api/v1/requirements
-> 如果已有 requirementId：
   PUT /api/v1/requirements/{requirementId}
-> RequirementValidationNode
-> RequirementStore.save()
-> 前端显示缺失和 warning
```

### 9.3 确认入口

```text
用户点击确认需求
-> 前端检查 requirementDirty
-> 如果有未保存修改，提示先保存
-> POST /api/v1/requirements/{requirementId}/confirm
-> 后端最终校验
-> 校验通过：status=CONFIRMED，表单锁定
-> 校验失败：保持 DRAFT，显示缺失项
```

## 10. 注释规范要求

本阶段如果修改 Java / HTML / JavaScript，必须遵守：

```text
C:\Users\DJI\Desktop\agent spring ai\注释规范.md
```

重点：

```text
RequirementController 新接口必须写清楚 HTTP 语义、是否扣费、失败状态
RequirementValidationNode 必须说明哪些规则是 blocking，哪些只是 warning
TravelRequirementSpec 新字段必须说明业务含义和空值策略
agent-app.js 的异步流程必须说明状态映射、保存、确认、dirty 状态和错误处理
```

## 11. 预计修改文件

### 11.1 前端

```text
AgentProjectTrip/src/main/resources/static/travel-planner.html
AgentProjectTrip/src/main/resources/static/app.css
AgentProjectTrip/src/main/resources/static/agent-app.js
```

### 11.2 后端

```text
AgentProjectTrip/src/main/java/com/travel/agent/web/RequirementController.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelRequirementSpec.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/RequirementValidationNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/RequirementExtractionAgent.java
```

### 11.3 文档

```text
第10阶段开发.md
阶段9-17开发规划与维护清单.md
```

第 10 阶段完成后再新增：

```text
阶段 10 需求表单体验完善.md
```

## 12. 预计新增类

默认不新增 Java 类。

暂不新增：

```text
TravelerProfile.java
BudgetPreference.java
```

原因：

```text
当前字段规模还没有复杂到必须拆对象。
过早拆分会增加 Controller、Store、Prompt 和前端映射复杂度。
```

可能新增字段而非新增类：

```text
TravelRequirementSpec.specialNotes
```

## 13. 预计删除内容

本阶段不删除 Java 类。

本阶段不删除 `agent-lab.html`。

## 14. 验收标准

### 14.1 自然语言填表

```text
输入：国庆去法国和意大利玩10天，预算1200欧，不含国际机票，想避开人多的地方
预期：
目的地填入法国、意大利
天数填入10
预算填入1200 EUR
机票包含选择“不包含”
避开项包含人多
页面可保存、可确认
```

### 14.2 直接手动填表

```text
不输入自然语言
直接填写目的地、天数、预算、人数等字段
点击保存
预期：
系统创建 requirementId
页面显示校验结果
可继续确认
```

### 14.3 缺失字段提示

```text
只填“欧洲”
点击保存或确认
预期：
页面提示目的地过宽或关键信息不足
确认按钮说明为什么不可确认
不会扣费
不会生成完整方案
```

### 14.4 未保存修改保护

```text
整理需求后修改预算
不点击保存，直接点确认
预期：
页面提示有未保存修改，请先保存
不会确认旧需求
```

### 14.5 warning 不阻塞

```text
填写法国、意大利、10天、1200欧、不含国际机票
预期：
可能提示预算偏紧或时间需要确认
但只要关键字段齐全，仍允许确认
```

### 14.6 生成前门控

```text
需求未确认时
预期：
生成完整方案按钮不可用
```

确认后：

```text
生成完整方案按钮可用
```

## 15. 测试计划

### 15.1 前端静态检查

```text
node --check AgentProjectTrip/src/main/resources/static/agent-app.js
```

### 15.2 静态资源同步

```text
mvn process-resources
```

### 15.3 Java 测试

如果本阶段新增或修改后端 Java：

```text
mvn test
```

### 15.4 手动页面测试

访问：

```text
http://localhost:8080/travel-planner.html?stage10=test
```

手动测试重点：

```text
自然语言填表
手动填表
缺失字段提示
warning 提示
未保存修改保护
确认后锁定
生成按钮门控
```

## 16. 本阶段风险

### 16.1 风险：表单越做越复杂

控制方式：

```text
只围绕生成前关键字段做优化
不做完整 CRM / 用户画像系统
复杂字段留给后续阶段
```

### 16.2 风险：前端和后端校验重复

控制方式：

```text
前端只做即时体验校验
后端仍然是最终确认门控
```

### 16.3 风险：下拉选项限制用户表达

控制方式：

```text
优先使用 datalist 或快捷按钮
保留自由文本能力
```

### 16.4 风险：新增接口影响旧流程

控制方式：

```text
新增 POST /api/v1/requirements 只服务手动建表
不改变 /draft、PUT、/confirm、/generate-async 既有语义
```

## 17. 开发顺序建议

### Step 1：前端字段分组和 specialNotes

```text
修改 travel-planner.html
修改 app.css
修改 agent-app.js 的 fill/read 映射
```

### Step 2：后端 specialNotes 字段

```text
修改 TravelRequirementSpec
修改 RequirementExtractionAgent
修改相关 Prompt / fallback 规则
```

### Step 3：手动创建需求接口

```text
RequirementController 新增 POST /api/v1/requirements
复用 RequirementValidationNode
复用 RequirementStore
```

### Step 4：本地校验和 dirty 状态

```text
agent-app.js 新增 requirementDirty
监听表单 input/change
保存成功后 dirty=false
修改后 dirty=true
确认前检查 dirty
```

### Step 5：字段级提示和确认原因

```text
renderFieldHints()
renderConfirmReason()
字段缺失和 warning 可视化
```

### Step 6：测试、同步资源、更新文档

```text
node --check
mvn test
mvn process-resources
更新第10阶段开发.md
完成后新增 阶段 10 需求表单体验完善.md
更新阶段9-17开发规划与维护清单.md
```

## 18. 需要你审查的问题

请重点审查这几个选择：

```text
1. 是否同意新增 POST /api/v1/requirements，用于手动填表创建需求草稿？
2. 是否同意本阶段给 TravelRequirementSpec 新增 specialNotes 字段？
3. 预算金额缺失时，是 blocking 还是 warning？
4. 出发地缺失时，是 blocking 还是 warning？
5. 预算是否包含国际机票为空时，是 blocking 还是 warning？
```

我的默认建议：

```text
新增 POST /api/v1/requirements：同意
新增 specialNotes：同意
预算金额缺失：blocking
出发地缺失：warning
预算是否包含国际机票为空：warning
```
