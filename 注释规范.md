# Java 注释规范（初学者友好的流程注释版）

本文档记录当前项目推荐的注释风格。后续新增或修改代码时，优先参考 `LangGraphPlannerFacade.plan()` 的写法：注释要帮助初学者看懂“这段代码在系统流程里为什么存在、前后状态如何变化、失败时如何兜底”，而不是逐行翻译代码。

## 1. 总目标

注释要回答读者真正会卡住的问题：

1. 这个类或方法在系统架构里处于什么位置。
2. 当前步骤为什么要放在这里。
3. 读了哪些状态、写了哪些状态。
4. 是否调用模型、工具、数据库、缓存或外部 API。
5. 异常、空值、旧数据、重复请求等边界情况如何处理。
6. 哪些返回值只是“本次请求处理成功”，并不代表完整业务任务已经完成。

不要把注释写成代码复读机。简单赋值、普通 getter/setter、显而易见的循环，不需要为了凑数量加注释。

## 2. 类级 Javadoc

每个生产代码类都建议有类级 Javadoc。类注释必须回答三件事：

1. 这个类是什么。
2. 它在系统架构中的位置。
3. 它承担哪些职责，尤其是外部调用、状态转换、异常兜底、是否参与 Agent / Graph 工作流。

推荐格式：

```java
/**
 * Planner 草案生成节点（Graph 层 - LLM 规划核心）。
 *
 * <p>系统架构位置：LangGraphPlannerFacade -> <b>PlanDraftNode</b> -> DeepSeek Pro</p>
 *
 * <p>职责：
 * <ul>
 *   <li>读取 TravelPlanState 中的用户输入、Gatekeeper 实体、RAG 上下文和分支结果。</li>
 *   <li>调用核心模型生成结构化 PlannerDraft。</li>
 *   <li>模型输出不合法时降级为可读草案，避免整个 Graph 中断。</li>
 * </ul>
 * </p>
 */
```

DTO、record、enum 也要写清楚业务含义。简单 DTO 不需要长篇大论，但至少要让读者知道它在哪个接口或节点之间流转。

## 3. 字段注释

字段注释只写有业务含义、架构含义或边界含义的字段。尤其要说明：

- 状态字段代表的生命周期。
- 标志位为 `true` / `false` 时分别意味着什么。
- 缓存、仓库、模型客户端、工具类依赖的用途。
- 兜底常量和默认值为什么存在。

推荐：

```java
/** 用户未传 sessionId 时使用的兜底会话 ID，保证 pending 状态仍有稳定 key。 */
private static final String DEFAULT_SESSION_ID = "default-session";

/** 第二阶段保存 pending 状态的会话仓库，用于“追问 -> 用户补充 -> 续跑”闭环。 */
private final ConversationStateStore conversationStateStore;
```

不推荐：

```java
/** 字符串 */
private String name;
```

## 4. 构造器注释

构造器注释需要说明注入了什么依赖，以及为什么这样注入。存在测试兼容构造器、可选依赖或 setter 注入时，必须解释原因。

推荐：

```java
/**
 * 构造器注入当前规划工作流需要的全部节点。
 *
 * <p>后续迁移到 LangGraph4j StateGraph 时，对外仍保持 plan() 方法不变。</p>
 *
 * @param initStateNode 初始化 Graph 状态的节点
 * @param planDraftNode 调用核心模型生成草稿的节点
 */
```

## 5. 方法级 Javadoc

公开方法必须写方法级 Javadoc。复杂私有方法也要写。注释要包含：

- 输入是什么。
- 输出是什么。
- 核心流程是什么。
- 失败、降级或暂停策略是什么。
- 对状态对象的读写影响是什么。

推荐：

```java
/**
 * 执行支持澄清和分支 Agent 的旅行规划工作流。
 *
 * <p>处理流程：</p>
 * <ol>
 *   <li>根据 sessionId 查询是否存在上一轮 pending 状态。</li>
 *   <li>存在 pending 状态时，把当前用户输入合并进旧任务；否则初始化新状态。</li>
 *   <li>执行 RAG、BranchDispatch、BranchExecute、Planner、Validator、RiskReasoning。</li>
 *   <li>如果需要澄清，则生成追问并保存 pending 状态。</li>
 *   <li>如果信息足够，则进入 Finalizer 输出最终答案，并清理 pending 状态。</li>
 * </ol>
 *
 * @param request Graph 入口请求，包含用户原始输入、Gatekeeper 路由结果和 sessionId
 * @return Graph 执行结果；失败时包含面向用户的降级答案
 */
```

## 6. 方法内部注释

方法内部注释要按业务阶段分段，使用“Step + 目的”或“边界条件 + 原因”的写法。读者应该能顺着注释理解整条流程。

推荐：

```java
// Step 1：先判断这是不是“上一轮追问后的补充回答”。
//
// 例如：
//   第一轮：用户说“帮我规划欧洲旅行”
//   系统追问：“你想去几天？”
//   第二轮：用户说“10天”
//
// 第二轮的“10天”不能当作新任务处理，必须合并回上一轮 pendingState。
TravelPlanState state = conversationStateStore.findPendingState(sessionId)
        .map(pendingState -> mergeClarificationNode.merge(pendingState, safeRequest))
        .orElseGet(() -> initStateNode.init(safeRequest));
```

推荐解释语义陷阱：

```java
// 这里的 success 表示“本次请求被成功处理并生成追问”，不是最终旅行方案已经完成。
return GraphResult.success(state.getFinalAnswer(), state.getValidationIssues());
```

不推荐：

```java
// 调用 check 方法
state = preClarifyCheckNode.check(state);
```

## 7. 异常处理注释

异常注释必须说明异常类型代表什么，以及为什么不继续抛出。只写“捕获异常”是不够的。

推荐：

```java
} catch (RestClientException e) {
    // 网络故障、HTTP 4xx、5xx 等 RestClient 层面的错误。
    // 不向上传播，避免整个 Agent 推理链被外部 API 故障打断。
    return buildFallback(city);
}
```

## 8. Agent / Graph 代码额外规则

Agent 和 Graph 工作流代码必须额外写清楚：

- 这个节点读哪些 `TravelPlanState` 字段。
- 这个节点写哪些 `TravelPlanState` 字段。
- 它是否调用模型、工具、数据库、缓存或外部 API。
- 模型输出解析失败时如何降级。
- 为什么这个阶段暂停、续跑、清理 pending 状态或跳过某个节点。
- 为什么旧测试构造器、可选节点或空依赖可以安全跳过。

推荐：

```java
// Dispatch 只负责“决定要做哪些分支任务”，例如天气、景点、航班。
TravelPlanState dispatchedState = branchDispatchNode.dispatch(state);

// Execute 才真正调用 BranchAgentFacade / Tools，并把结果写回 state。
return branchExecuteNode.execute(dispatchedState);
```

## 9. Controller / Service / Store 代码额外规则

Controller 注释要说明 HTTP 语义：

- 入口 URL 用来做什么。
- `200 OK`、`202 ACCEPTED`、`404 NOT_FOUND` 等状态分别代表什么。
- 接口是否同步返回最终结果，还是只创建后台任务。

Service 注释要说明业务事务和编排边界：

- 什么时候创建记录、什么时候复用旧记录。
- 是否异步执行。
- 失败状态如何回写。

Store 注释要说明持久化策略：

- 内存实现是否只适合开发和测试。
- JDBC 实现如何序列化复杂对象。
- key 的组成和覆盖策略。

## 10. DTO / Enum / Record 注释规则

DTO、record、enum 注释以“业务含义”为主：

- DTO 是哪个接口的请求或响应。
- enum 每个值代表哪个状态或阶段。
- record 字段如果会被前端、数据库或节点消费，要解释含义。

简单字段不必过度解释，但状态类、任务类、结果类必须说明生命周期。

## 11. 测试注释规则

测试代码不需要像生产代码一样密集，但每个测试类建议说明测试对象和边界。测试方法名应尽量自解释，场景复杂时补 Javadoc 或方法内注释。

推荐：

```java
/**
 * InitStateNode 的单元测试。
 *
 * <p>重点验证 GatekeeperResponse 到 TravelPlanState 的字段搬运和 null-safe 行为。</p>
 */
class InitStateNodeTest {
}
```

复杂测试推荐写清楚 given / when / then：

```java
// given：构造一个缺少出行天数的状态，模拟用户输入过宽。

// when：执行前置澄清检查。

// then：应该进入 NEEDS_CLARIFICATION，而不是继续调用 Planner。
```

## 12. 前端静态页面注释规则

HTML / JavaScript 注释只写流程和边界，不解释普通 DOM 语法。适合补注释的位置：

- 页面入口状态初始化。
- 调用后端接口的函数。
- 轮询、重试、取消、错误提示等异步流程。
- 前端状态和后端字段的映射关系。

## 13. 落地检查清单

补注释时按文件逐个检查，不跳过某个类或某个源码文件：

- 生产 Java：类级 Javadoc、复杂公开方法、关键私有方法、异常兜底、状态流转。
- 测试 Java：测试类目的、复杂场景的 given / when / then。
- 静态页面：后端接口调用、异步流程、状态映射。
- 已有注释乱码、过时或与代码不一致时，优先修正。
- 不改业务逻辑；注释变更不应改变测试结果。

## 14. 总原则

- 注释写“为什么”“何时暂停/续跑/兜底”“状态怎么变”，少写“代码正在做什么”。
- 类级注释要有系统位置。
- 方法级注释要有流程和异常策略。
- 方法内部注释要按阶段解释关键决策。
- DTO 字段要解释业务含义。
- 不为了注释而注释简单 getter/setter。
