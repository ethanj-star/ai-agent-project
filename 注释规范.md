# Java 注释规范（WeatherServiceImpl Few-Shot）

本文档记录当前项目推荐的 Java 注释风格。后续新增代码时，优先参考 `WeatherServiceImpl` 的写法：注释解释“这个类在系统中承担什么责任、为什么这样设计、异常时如何兜底”，而不是逐行翻译代码。

## 1. 类级 Javadoc

类注释必须回答三件事：

1. 这个类是什么。
2. 它在系统架构中的位置。
3. 它承担哪些职责，尤其是外部调用、状态转换、异常兜底等关键行为。

推荐格式：

```java
/**
 * 天气业务服务实现类（Service 层 - 数据获取核心）
 *
 * <p>系统架构位置：Tools 层 → <b>Service 层</b> → 外部 OpenWeatherMap API</p>
 *
 * <p>职责：
 * <ul>
 *   <li>通过 OpenWeatherMap 的 Current Weather Data 接口获取实时天气数据。</li>
 *   <li>解析 JSON 响应，提取温度、湿度、天气描述等关键字段，封装为 WeatherDTO。</li>
 *   <li>对所有外部调用加装完整的异常防护，确保 API 故障不会向上传播。</li>
 * </ul>
 * </p>
 */
```

## 2. 字段注释

字段注释只写有业务含义或架构含义的字段，不要给所有普通变量机械注释。

推荐：

```java
/** 查询失败时返回的兜底天气描述 */
private static final String FALLBACK_DESCRIPTION = "获取天气失败";

/** 从环境变量 OPENWEATHER_API_KEY 读取的 API 密钥，由 application.properties 桥接注入 */
private final String apiKey;
```

不推荐：

```java
/** 字符串 */
private String name;
```

## 3. 构造器注释

构造器注释需要说明注入了什么依赖，以及为什么这样注入。

推荐：

```java
/**
 * 构造器注入 RestClient 和 OpenWeatherMap 密钥。
 *
 * <p>使用 RestClient.Builder 而非直接注入 RestClient，
 * 是为了允许 Spring Boot 自动配置在构建阶段生效。</p>
 *
 * @param builder Spring Boot 自动配置提供的 RestClient.Builder
 * @param apiKey  从配置项 openweathermap.api-key 注入的 API 密钥
 */
```

## 4. 方法级 Javadoc

公开方法必须写方法级 Javadoc。注释要包含：

- 输入是什么。
- 输出是什么。
- 核心流程是什么。
- 异常或降级策略是什么。

推荐：

```java
/**
 * 查询指定城市的实时天气。
 *
 * <p>处理流程：
 * <ol>
 *   <li>通过 UriBuilder 构造 OpenWeatherMap 请求。</li>
 *   <li>将响应体反序列化为 JsonNode。</li>
 *   <li>从固定路径提取城市、温度、湿度和天气描述。</li>
 *   <li>发生网络或解析异常时返回兜底 WeatherDTO。</li>
 * </ol>
 * </p>
 *
 * @param city 城市英文名
 * @return 天气 DTO；失败时返回兜底对象
 */
```

## 5. 方法内部注释

方法内部注释只写关键步骤、坑点和边界条件。

推荐：

```java
// 通过 UriBuilder 安全构建带查询参数的请求 URL，避免手动拼接引发的注入或编码问题
JsonNode root = restClient.get()
        .uri(uriBuilder -> uriBuilder ...)
        .retrieve()
        .body(JsonNode.class);

// OpenWeatherMap 正常情况下不会返回空体，但做防御性判空
if (root == null) {
    return buildFallback(city);
}
```

不推荐：

```java
// 设置 cityName
String cityName = ...
```

## 6. 异常处理注释

异常注释必须说明异常类型代表什么，以及为什么不继续抛出。

推荐：

```java
} catch (RestClientException e) {
    // 网络故障、HTTP 4xx、5xx 等 RestClient 层面的错误
    // 不向上传播，避免整个 Agent 推理链被外部 API 故障打断
    return buildFallback(city);
}
```

## 7. Agent / Graph 代码额外规则

Agent 工作流代码要额外写清楚：

- 这个节点读哪些状态。
- 这个节点写哪些状态。
- 它是否调用模型、工具或外部 API。
- 模型输出解析失败时如何降级。
- 为什么这个阶段不做循环或复杂分支。

推荐：

```java
/**
 * Planner 草案生成节点（Graph 层 - LLM 规划核心）
 *
 * <p>系统架构位置：LangGraphPlannerFacade → <b>PlanDraftNode</b> → DeepSeek Pro</p>
 *
 * <p>职责：
 * <ul>
 *   <li>读取 TravelPlanState 中的用户输入、Gatekeeper 实体和 RAG 上下文。</li>
 *   <li>调用核心模型生成结构化 PlannerDraft。</li>
 *   <li>模型没有返回合法 JSON 时，降级为 Markdown 草案，保证流程不中断。</li>
 * </ul>
 * </p>
 */
```

## 8. 测试注释规则

测试代码不需要像生产代码一样密集，但每个测试类建议说明测试对象和边界。测试方法名应尽量自解释，只在场景复杂时补 Javadoc。

推荐：

```java
/**
 * InitStateNode 的单元测试。
 *
 * <p>重点验证 GatekeeperResponse 到 TravelPlanState 的字段搬运和 null-safe 行为。</p>
 */
class InitStateNodeTest {
}
```

## 9. 总原则

- 注释写“为什么”和“边界”，少写“代码正在做什么”。
- 类级注释要有系统位置。
- 方法级注释要有流程和异常策略。
- 内联注释要解释坑点、兜底、外部系统约束。
- DTO 字段要解释业务含义。
- 不为了注释而注释简单 getter/setter。
