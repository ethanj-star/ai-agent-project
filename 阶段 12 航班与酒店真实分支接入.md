# 阶段 12 航班与酒店真实分支接入

完成日期：2026-06-14

对应总控文档：

```text
阶段9-17开发规划与维护清单.md
```

阶段开发过程文档：

```text
第12阶段开发.md
```

## 1. 本阶段核心思想

第 12 阶段的核心思想是：把“底层已经写好，但 Graph 工作流没有真正使用”的航班和酒店能力接入正式规划链路。

在本阶段之前，系统已经有：

```text
FlightServiceImpl.searchFlights(...)
FlightTools.searchFlights(...)
PlacesServiceImpl.searchHotels(...)
PlacesTools.searchHotels(...)
```

但是正式 Graph 流程里，航班分支仍然走 fallback，酒店也没有独立的 `HOTEL` 分支类型。  
所以 Planner 最终拿到的仍然主要是模型常识，而不是真实工具返回的航班和酒店参考。

本阶段完成后，系统可以从结构化需求表里读取出发地、目的地、日期、天数、预算和住宿偏好，然后派发 `FLIGHT` / `HOTEL` 分支，并把真实查询结果或明确失败原因写回 `BranchResult`。

## 2. 本阶段解决的问题

本阶段解决了这些问题：

```text
Graph 派发 FLIGHT 后不能真实查航班。
酒店查询底层工具存在，但 Graph 不会派发 HOTEL。
Planner 容易编造实时航班、酒店价格或库存。
外部 API 失败时，Graph 缺少明确的失败分支结果。
结构化需求表里的 startDate / durationDays / departureCity 没有完整传入分支任务。
```

## 3. 新增功能

### 3.1 航班真实分支

`BranchAgentFacade` 新增真实航班执行逻辑：

```text
BranchTaskType.FLIGHT
 -> FlightSearchParamResolver.resolve(...)
 -> FlightTools.searchFlights(...)
 -> BranchResult
```

当参数完整时，系统会真实调用航班工具。  
当缺少出发地、日期或目的地无法映射机场代码时，分支返回失败型 `BranchResult`，但不会中断整个 Graph。

### 3.2 酒店真实分支

新增 `HOTEL` 分支类型：

```text
BranchTaskType.HOTEL
 -> HotelSearchParamResolver.resolve(...)
 -> PlacesTools.searchHotels(...)
 -> BranchResult
```

第一版酒店分支最多查询 2 个代表城市，避免一次规划触发过多外部 API 调用。

### 3.3 分支任务携带更多结构化字段

`BranchTask` 新增这些字段：

```text
startDate
durationDays
departureCity
accommodationPreference
budgetIncludesInternationalFlight
```

这些字段让分支 Agent 不必只从自然语言里猜参数，而可以直接消费第 10 阶段形成的结构化需求表。

### 3.4 不伪造实时数据约束

`PlanDraftNode` 的提示词新增约束：

```text
没有工具返回时，不要编造实时航班号、实时票价、酒店实时价格、酒店库存。
```

这样即使外部 API 暂时失败，最终答案也会说明“实时数据未成功获取”，而不是硬编一个看起来完整但不可验证的结果。

### 3.5 前端横向溢出修复

在第 12 阶段手动测试时发现正式页面会被长文本、任务 ID 或方案内容撑出屏幕右侧。  
本阶段顺手修复了 `travel-planner.html` 的横向滚动问题，让页面保持在视口宽度内。

## 4. 修改的业务逻辑

本阶段主要修改了以下业务逻辑：

```text
1. BranchDispatchNode 可以派发 HOTEL 分支。
2. BranchDispatchNode 在结构化需求完整时，可以派发 FLIGHT / HOTEL。
3. “不含国际机票”只作为预算边界，不会单独触发航班查询。
4. “不用查机票 / 不查机票”等表达会抑制 FLIGHT 派发。
5. FLIGHT 分支从占位 fallback 改为真实调用 FlightTools。
6. HOTEL 分支真实调用 PlacesTools.searchHotels。
7. 航班和酒店工具失败时返回失败型 BranchResult，不抛异常打断 Graph。
8. 如果预算不包含国际机票，航班结果只能作为路线参考，不能计入用户预算。
```

## 5. 修改的前端内容

本阶段没有新增前端业务功能，但修复了正式页面布局问题：

```text
AgentProjectTrip/src/main/resources/static/app.css
AgentProjectTrip/src/main/resources/static/travel-planner.html
```

主要变化：

```text
限制 body / 主容器 / 面板最大宽度，避免页面横向撑开。
给 grid、panel、answer-box、debug-panel、recent-job 等区域补充 min-width: 0。
对长文本、JSON、任务 ID、方案内容增加 overflow-wrap / ellipsis。
更新 travel-planner.html 的静态资源版本号，避免浏览器继续使用旧 CSS。
```

## 6. 修改的后端接口

本阶段没有新增 HTTP 接口。

已有正式页面和生成接口继续沿用原流程：

```text
需求整理 / 确认
 -> 创建异步生成任务
 -> Graph 内部派发 FLIGHT / HOTEL
 -> 前端通过任务状态和 planId 展示结果
```

也就是说，航班和酒店接入发生在 Graph 内部，不要求前端额外调用新的接口。

## 7. 修改的代码文件

后端核心文件：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTaskType.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTask.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/FlightSearchRequest.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/HotelSearchRequest.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/FlightSearchParamResolver.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/HotelSearchParamResolver.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/BranchDispatchNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/BranchAgentFacade.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/PlanDraftNode.java
```

测试文件：

```text
AgentProjectTrip/src/test/java/com/travel/agent/ai/agents/BranchAgentFacadeTest.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/node/BranchDispatchNodeTest.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/node/FlightSearchParamResolverTest.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/node/HotelSearchParamResolverTest.java
```

前端静态文件：

```text
AgentProjectTrip/src/main/resources/static/app.css
AgentProjectTrip/src/main/resources/static/travel-planner.html
```

## 8. 新增的类

### 8.1 FlightSearchRequest

用途：

```text
封装航班查询参数。
表达当前分支是否可以查询。
如果不能查询，记录缺失原因。
```

它让 `BranchAgentFacade` 不需要在方法里堆多个局部变量，也让失败原因可以清晰传递给 Planner。

### 8.2 HotelSearchRequest

用途：

```text
封装酒店查询参数。
记录 city、checkInDate、checkOutDate。
如果不能查询，记录缺失原因。
```

### 8.3 FlightSearchParamResolver

用途：

```text
把 BranchTask 里的中文地点、目的地、日期解析成 FlightTools 可用参数。
```

它只负责参数推导，不调用外部 API。  
第一版内置有限城市 / 国家到机场代码的映射，例如上海 -> PVG、法国 -> CDG、意大利 -> FCO。

### 8.4 HotelSearchParamResolver

用途：

```text
把 BranchTask 里的目的地、开始日期和旅行天数解析成酒店查询参数。
```

它只负责城市和日期推导，不调用外部 API。  
第一版最多返回 2 个酒店查询请求，控制 SerpApi 调用成本。

## 9. 删除或废弃的类

本阶段没有删除类。

原有航班 fallback 思路被实际航班查询替代，但没有作为独立类删除。

## 10. 新增的方法

`BranchAgentFacade`：

```text
executeFlight(BranchTask task)
executeHotel(BranchTask task)
formatFlight(FlightDTO flight)
formatHotel(HotelDTO hotel)
```

`BranchDispatchNode`：

```text
shouldDispatchHotel(TravelPlanState state)
looksLikeHotelNeed(TravelPlanState state)
shouldDispatchFlight(TravelPlanState state)
hasStructuredFlightSkeleton(TravelPlanState state)
```

`FlightSearchParamResolver`：

```text
resolve(BranchTask task)
resolveAirportCode(String place)
```

`HotelSearchParamResolver`：

```text
resolve(BranchTask task, int maxQueries)
resolveHotelCity(String place)
```

## 11. 删除或废弃的方法

本阶段没有删除公开方法。

`FLIGHT` 分支不再依赖旧的占位式 fallback 行为；后续如果代码里仍保留兼容兜底方法，应在阶段 13 或阶段 14 清理时统一检查。

## 12. 关键代码细节和用途

### 12.1 BranchTaskType.HOTEL

新增独立枚举值，让酒店查询不再塞进 `PLACES`。

这样做的好处是：

```text
景点推荐和酒店价格查询可以分开计费、分开失败、分开进入 Planner prompt。
```

### 12.2 BranchTask 结构化字段

`BranchTask` 不再只携带 query / destinations / time，而是补充结构化需求表字段。

这样分支工具可以优先使用可靠字段，而不是每次都从自然语言里重新猜：

```text
startDate -> 航班出发日期 / 酒店入住日期
durationDays -> 酒店退房日期
departureCity -> 航班出发地
budgetIncludesInternationalFlight -> 预算边界
accommodationPreference -> 酒店派发和摘要参考
```

### 12.3 FlightSearchParamResolver

该类把自然语言地点转换成 IATA 机场代码。

它当前是有限映射，不是完整机场库。  
后续如果要做生产级航班查询，应把映射迁移到数据库表、配置文件或专门的 `AirportResolver`。

### 12.4 HotelSearchParamResolver

该类把目的地转换为酒店查询城市，并根据 `startDate + durationDays` 算出退房日期。

它的失败策略是返回不可查询请求，而不是抛异常。  
这样即使酒店参数不足，Graph 仍然可以继续生成常规行程，只是必须标注实时酒店数据缺失。

### 12.5 BranchAgentFacade

`BranchAgentFacade` 成为分支工具调用入口：

```text
FLIGHT -> FlightTools.searchFlights
HOTEL -> PlacesTools.searchHotels
```

它负责把工具返回值压缩为 Planner 能读懂的 `BranchResult.summary`。  
它也负责捕获工具异常，避免外部 API 抖动影响整个规划流程。

### 12.6 PlanDraftNode

Planner prompt 明确要求：

```text
如果分支工具没有返回实时数据，不能编造实时价格、库存或航班号。
```

这是第 12 阶段非常关键的安全边界。

## 13. 注释规范执行情况

本阶段新增和修改的 Java 代码已按项目注释规范补充说明，重点覆盖：

```text
类在 Graph / Agent 链路中的位置。
分支任务读取哪些字段。
参数解析类只负责推导，不负责 API 调用。
外部工具失败时为什么返回失败型 BranchResult，而不是继续抛异常。
为什么航班和酒店不能伪造实时数据。
```

参考规范：

```text
C:\Users\DJI\Desktop\agent spring ai\注释规范.md
```

## 14. 测试方式和测试结果

### 14.1 自动化测试

已执行：

```powershell
cd AgentProjectTrip
mvn test
```

结果：

```text
Tests run: 114, Failures: 0, Errors: 0, Skipped: 0
```

### 14.2 静态资源构建验证

修复前端横向溢出后，已执行：

```powershell
cd AgentProjectTrip
mvn process-resources
```

结果：

```text
通过，静态资源已同步到 target/classes/static。
```

### 14.3 人工测试

用户已确认第 12 阶段手动测试通过。

核心测试内容：

```text
从上海出发，2026年10月1日去法国和意大利玩10天，预算2000欧，预算包含国际机票，想住经济型酒店，避开人多的地方。
```

期望验证点：

```text
Graph 日志出现 FLIGHT 和 HOTEL 分支。
FLIGHT 分支真实调用 FlightTools。
HOTEL 分支真实调用 PlacesTools.searchHotels。
最终方案中航班和酒店部分有真实查询参考，或在工具失败时有明确说明。
正式页面不会再被长文本横向撑出屏幕。
```

## 15. 遗留问题

本阶段完成了航班和酒店的真实分支接入，但仍有这些遗留问题：

```text
BranchDispatchNode 仍主要依赖 Java 硬规则，不是模型智能派发。
航班 / 酒店参数映射仍是有限映射，不是完整机场库或城市库。
酒店查询第一版只查代表城市，不能覆盖复杂多城市住宿拆分。
没有接入千问作为分支模型，当前分支仍是 Java 调工具并整理结果。
火车、城际交通、签证、预算精算、餐厅、门票、开放时间、未来天气、安全提醒仍未真实接入。
```

## 16. 下一阶段衔接点

下一阶段是：

```text
第 13 阶段：模型驱动 BranchDispatch
```

第 13 阶段要解决的问题是：

```text
当前系统能执行 WEATHER / PLACES / KNOWLEDGE / FLIGHT / HOTEL 分支，
但“该派发哪些分支”仍主要由 Java 写死规则决定。
```

下一步应升级为：

```text
DeepSeek Pro 根据需求上下文提出分支任务建议。
Java Guard 校验任务类型、数量、参数和成本边界。
旧 BranchDispatchNode 作为模型失败时的兜底。
```

这样系统才会更接近成熟 agent 的任务流编排方式。
