# 第9阶段开发：正式前端 MVP 页面

创建日期：2026-06-14

对应总控文档：

```text
阶段9-17开发规划与维护清单.md
```

当前状态：

```text
已完成
```

用户已审查通过本文档，当前进入代码开发阶段。

## 0. 用户已确认的开发决策

用户已确认：

```text
允许拆分 app.css 和 agent-app.js。
保留原始 JSON 调试面板，但默认折叠。
新增 travel-planner.html 作为正式入口，保留 agent-lab.html 作为开发调试页。
```

因此第 9 阶段实际开发文件优先选择：

```text
AgentProjectTrip/src/main/resources/static/travel-planner.html
AgentProjectTrip/src/main/resources/static/app.css
AgentProjectTrip/src/main/resources/static/agent-app.js
```

`agent-lab.html` 本阶段原则上不修改，继续保留为开发调试台。

## 0.1 当前开发记录

### 2026-06-14：正式入口第一版

已新增 `travel-planner.html`、`app.css`、`agent-app.js`，形成第 9 阶段正式前端 MVP 页面。

本轮实现内容：

```text
新增正式入口页面 travel-planner.html
新增独立样式文件 app.css
新增独立前端交互文件 agent-app.js
保留 agent-lab.html 作为开发调试页
保留 JSON 调试面板，并默认折叠
```

本轮细节修正：

```text
在前端状态中新增 latestVersion 字段
currentVersion 专门表示用户当前正在查看的方案版本
latestVersion 专门表示该计划已经生成到的最新版本
避免用户点击历史版本后，版本列表误收缩到旧版本
修正浏览器 sessionId 生成逻辑，兼容不支持 crypto.randomUUID 的环境
```

### 2026-06-14：版本切换问题修复

用户测试发现：点击历史版本 `v1` 后，`v2` 会从版本列表中消失。

问题原因：

```text
前端最初把 currentVersion 同时当成“正在查看的版本”和“最新版本”
点击历史版本后，currentVersion 会变成旧版本号
版本列表如果依赖 currentVersion 渲染，就会把后续版本隐藏
浏览器也可能继续缓存旧的 agent-app.js，导致修复代码没有立即生效
```

本次修复：

```text
版本列表统一根据 latestVersion、planRecord.currentVersion、currentVersion 的最大值渲染
所有版本号进入前端状态前统一转成数字
loadPlan、modifyPlan、loadVersion 都不允许 latestVersion 被旧版本覆盖
travel-planner.html 给 app.css 和 agent-app.js 增加版本参数，避免浏览器缓存旧静态资源
```

### 2026-06-14：第 9 阶段完成收尾

本阶段已完成正式前端 MVP 页面，并生成阶段归档文档：

```text
阶段 9 正式前端 MVP 页面.md
```

最终收尾内容：

```text
确认 travel-planner.html 作为正式入口
保留 agent-lab.html 作为开发调试页
新增顶部生成额度状态
确认需求表在 CONFIRMED 后锁定
复制方案增加浏览器兼容兜底
静态资源版本更新到 20260614-3
通过 node --check 检查
通过 HTML id 与 JS 绑定检查
通过 mvn process-resources 同步静态资源
```

涉及文件：

```text
AgentProjectTrip/src/main/resources/static/travel-planner.html
AgentProjectTrip/src/main/resources/static/app.css
AgentProjectTrip/src/main/resources/static/agent-app.js
```

## 1. 本阶段目标

第 9 阶段的目标是：把当前偏开发调试性质的 `agent-lab.html`，升级成一个用户可以完整体验旅行规划流程的正式 MVP 页面。

用户不应该理解这些内部技术词：

```text
requirementId
jobId
planId
GenerationJob
TravelRequirementSpec
GraphResult
```

页面要把这些技术细节藏到内部状态里，让用户只感受到：

```text
我输入旅行想法
 -> 系统帮我整理需求
 -> 我确认需求
 -> 系统生成方案
 -> 我看到方案
 -> 我继续提出修改
```

## 2. 本阶段不做什么

为了避免第 9 阶段膨胀，本阶段明确不做：

```text
不改 Agent 核心规划逻辑
不改 LangGraphPlannerFacade
不接入真正 LangGraph4j
不改 BranchDispatchNode
不接入真实航班分支
不新增酒店 / 预算 / 签证分支
不做登录支付系统
不大改数据库 schema
```

本阶段只做前端 MVP 页面和必要的前端状态整理。如果发现后端接口有明显小缺口，可以做最小范围补充，但必须先在开发前说明。

## 3. 当前问题

当前 `agent-lab.html` 已经能测试很多功能，但它更像开发调试台：

```text
页面信息密度高
技术字段暴露较多
用户需要理解 requirementId / jobId / planId
流程感不够强
生成状态和结果展示还不够产品化
修改计划、版本历史、记忆偏好还像工具区
错误提示偏开发语气
```

所以本阶段要解决的是“可完整体验”，不是“新增更多后端智能”。

## 4. 本阶段最终用户流程

用户完整流程应该是：

```text
1. 打开页面
2. 输入一句自然语言旅行需求
3. 点击“整理需求”
4. 页面展示结构化需求表
5. 用户修改表单
6. 用户点击“确认需求”
7. 用户点击“生成完整方案”
8. 页面展示生成进度
9. 生成成功后自动显示旅行方案
10. 用户输入“我想把第3天改轻松一点”
11. 页面生成新版本方案
12. 用户可以查看版本历史
```

## 5. 页面信息架构

正式 MVP 页面建议分成 6 个区域。

### 5.1 顶部状态栏

用途：

```text
显示当前阶段
显示需求是否已确认
显示是否正在生成
显示剩余额度
显示当前 planId 的短 ID
```

用户看到的是自然语言状态，不看到内部对象名。

示例状态：

```text
正在整理需求
需求待确认
正在生成方案
方案已完成
修改已生成新版本
生成失败，请检查需求
```

### 5.2 需求输入区

用途：

```text
让用户输入自然语言旅行想法
调用 POST /api/v1/requirements/draft
把返回的 spec 填入表单
```

控件：

```text
多行文本框
整理需求按钮
清空按钮
示例需求按钮
```

示例需求：

```text
国庆去法国和意大利玩10天，预算1200欧，不含国际机票，想避开人多的地方
```

### 5.3 需求表单区

用途：

```text
展示 TravelRequirementSpec
允许用户编辑
调用 PUT /api/v1/requirements/{requirementId}
调用 POST /api/v1/requirements/{requirementId}/confirm
```

字段第一版保持当前后端已有字段，不在第 9 阶段扩展复杂字段。

建议展示：

```text
目的地
出发地
出行时间文字
开始日期
旅行天数
人数
预算金额
预算币种
是否包含国际机票
旅行风格
住宿偏好
交通偏好
偏好列表
避开项列表
特殊备注
```

状态：

```text
草稿
缺少信息
可以确认
已确认
正在生成
已生成
```

### 5.4 生成进度区

用途：

```text
调用 POST /api/v1/requirements/{requirementId}/generate-async
拿到 jobId
调用 GET /api/v1/jobs/{jobId}
轮询任务状态
成功后自动调用 GET /api/v1/plans/{planId}
```

显示内容：

```text
任务状态
当前阶段
错误信息
是否已扣费
是否已退款
成功后的 planId
```

用户文案映射：

```text
CREATED -> 已创建生成任务
VALIDATING_REQUIREMENT -> 正在检查需求表
CHARGING_CREDIT -> 正在扣除生成额度
RUNNING_GRAPH -> 正在规划旅行方案
SAVING_PLAN -> 正在保存方案
REFUNDING_CREDIT -> 生成失败，正在退回额度
FINISHED -> 任务已结束
```

### 5.5 方案展示区

用途：

```text
展示最终 Markdown 方案
展示当前版本号
展示复制按钮
展示重新加载按钮
展示风险提醒和需要确认的信息
```

当前后端返回的方案主要是 Markdown 文本。本阶段前端可以先做轻量 Markdown 渲染：

```text
保留标题层级
保留列表换行
保留加粗语义
不引入复杂 Markdown 库，除非现有项目已经有依赖
```

### 5.6 修改和版本区

用途：

```text
调用 POST /api/v1/plans/{planId}/modify
展示修改结果
加载指定历史版本
```

控件：

```text
修改输入框
提交修改按钮
版本列表
查看版本按钮
```

用户例子：

```text
把第3天安排轻松一点
预算再压低一些
我不想住青旅，改成经济型酒店
```

### 5.7 记忆偏好区

用途：

```text
调用 GET /api/v1/memories
调用 POST /api/v1/memories
调用 DELETE /api/v1/memories/{memoryId}
```

第一版不需要做复杂管理，只需要：

```text
展示当前记忆
新增一条偏好
禁用一条记忆
```

## 6. 前端状态设计

页面内部需要维护一个统一状态对象。建议命名为：

```javascript
state
```

核心字段：

```text
sessionId
requirementId
requirementSpec
requirementValidation
requirementConfirmed
jobId
jobStatus
jobStage
planId
planRecord
currentVersion
memories
loading
errorMessage
lastAssistantMessage
pollingTimer
```

状态设计目标：

```text
前端不要靠 DOM 反查业务状态
所有按钮可用性都从 state 推导
所有后端返回都先写入 state，再统一 render
```

## 7. 前端函数规划

本阶段如果继续维护单文件 `agent-lab.html`，建议整理出这些函数。

### 7.1 基础工具函数

```text
api(path, options)
setLoading(value, label)
showMessage(target, text, type)
extractErrorMessage(body)
escapeHtml(value)
renderMarkdown(value)
```

用途：

```text
统一调用后端
统一错误处理
统一 loading 状态
统一文本渲染
避免每个按钮事件里重复写 fetch 和 try/catch
```

### 7.2 状态和渲染函数

```text
renderAll()
renderStatus()
renderRequirementForm()
renderValidation()
renderJobProgress()
renderPlan()
renderVersions()
renderMemories()
```

用途：

```text
把 state 映射到页面
避免多个函数各自修改 DOM 后互相打架
```

### 7.3 业务动作函数

```text
draftRequirement()
saveRequirement()
confirmRequirement()
generatePlanAsync()
loadJobStatus()
startJobPolling()
stopJobPolling()
loadPlan()
modifyPlan()
loadVersion(version)
loadMemories()
saveMemory()
deactivateMemory(memoryId)
resetFlow()
```

用途：

```text
每个函数对应一个明确用户动作或轮询动作
函数名直接表达业务含义
```

## 8. 后端接口使用计划

本阶段优先复用已有接口。

### 8.1 整理需求

```text
POST /api/v1/requirements/draft
```

请求：

```json
{
  "sessionId": "front-session-001",
  "message": "国庆去法国和意大利玩10天，预算1200欧，不含国际机票，想避开人多"
}
```

页面处理：

```text
保存 requirementId
保存 spec
保存 validation
填充表单
显示 assistantMessage
```

### 8.2 保存需求表

```text
PUT /api/v1/requirements/{requirementId}
```

页面处理：

```text
从表单读取 spec
发送给后端保存
用返回结果刷新表单和校验提示
```

### 8.3 确认需求表

```text
POST /api/v1/requirements/{requirementId}/confirm
```

页面处理：

```text
成功后标记 requirementConfirmed = true
显示可以生成完整方案
如果失败，展示缺失项
```

### 8.4 创建异步生成任务

```text
POST /api/v1/requirements/{requirementId}/generate-async
```

页面处理：

```text
保存 jobId
启动轮询
禁用重复生成按钮
```

### 8.5 查询任务状态

```text
GET /api/v1/jobs/{jobId}
```

页面处理：

```text
更新 jobStatus 和 jobStage
SUCCEEDED 时停止轮询并加载 plan
FAILED 时停止轮询并展示错误
```

### 8.6 查询方案

```text
GET /api/v1/plans/{planId}
```

页面处理：

```text
保存 planRecord
展示当前版本方案
展示版本列表
```

### 8.7 修改方案

```text
POST /api/v1/plans/{planId}/modify
```

页面处理：

```text
如果返回新版本，则刷新 plan
如果需要澄清，则展示追问
如果核心需求变化，则提示用户回到需求表确认
```

### 8.8 记忆管理

```text
GET /api/v1/memories
POST /api/v1/memories
DELETE /api/v1/memories/{memoryId}
```

页面处理：

```text
展示记忆列表
新增偏好
禁用偏好
```

## 9. 具体开发步骤

### Step 1：备份和梳理当前 agent-lab.html

目标：

```text
确认当前页面已有功能
决定是继续单文件实现，还是拆出 app.css / agent-app.js
```

预期动作：

```text
阅读 agent-lab.html
整理当前已有函数
保留可复用 API 调用逻辑
标记需要删除或弱化的调试区
```

审查点：

```text
是否允许拆分 CSS / JS 文件
是否保留原始 JSON 调试面板
```

### Step 2：重建页面布局

目标：

```text
把页面改成正式 MVP 工作台
```

预期区域：

```text
顶部状态栏
左侧需求输入和表单
右侧生成进度和方案结果
底部修改和记忆区域
```

审查点：

```text
页面结构是否符合用户使用习惯
是否保留开发者调试信息
```

### Step 3：统一前端 state

目标：

```text
把散落的 DOM 状态和临时变量收敛到统一 state 对象
```

预期结果：

```text
所有按钮状态从 state 推导
所有接口返回先写入 state
所有渲染由 renderAll 或局部 render 函数触发
```

审查点：

```text
state 字段是否够用
命名是否易懂
是否避免暴露过多后端技术名给用户
```

### Step 4：打通需求整理和表单确认

目标：

```text
用户输入自然语言后，页面展示可编辑需求表
```

预期功能：

```text
draftRequirement()
saveRequirement()
confirmRequirement()
renderRequirementForm()
renderValidation()
```

审查点：

```text
缺失项提示是否清楚
确认按钮是否只在可确认时启用
确认后是否能重新编辑
```

### Step 5：打通异步生成和进度展示

目标：

```text
用户确认需求后，可以创建异步生成任务并看到进度
```

预期功能：

```text
generatePlanAsync()
startJobPolling()
loadJobStatus()
stopJobPolling()
renderJobProgress()
```

审查点：

```text
轮询间隔是否合理
重复点击是否被阻止
失败时是否能停止轮询
刷新页面恢复任务是否本阶段实现
```

说明：

```text
刷新恢复可以先做轻量版本：使用 localStorage 保存 sessionId、requirementId、jobId、planId。
```

### Step 6：打通方案展示

目标：

```text
生成成功后自动加载并展示旅行方案
```

预期功能：

```text
loadPlan()
renderPlan()
renderVersions()
copyAnswer()
```

审查点：

```text
Markdown 展示是否可读
版本号是否清楚
复制按钮是否工作
```

### Step 7：打通自然语言修改

目标：

```text
用户可以基于已有方案继续提出修改
```

预期功能：

```text
modifyPlan()
loadVersion(version)
```

审查点：

```text
局部修改成功后是否刷新当前版本
核心需求变化时是否提示回到需求表
需要澄清时是否显示问题
```

### Step 8：打通记忆偏好区

目标：

```text
用户可以查看、新增、禁用偏好记忆
```

预期功能：

```text
loadMemories()
saveMemory()
deactivateMemory(memoryId)
renderMemories()
```

审查点：

```text
记忆功能是否放在不干扰主流程的位置
用户是否能理解记忆会影响后续规划
```

### Step 9：错误提示、空状态和加载状态

目标：

```text
让页面在失败、空数据、加载中时都有清楚反馈
```

预期内容：

```text
接口失败提示
空计划提示
空记忆提示
生成中提示
按钮禁用状态
```

审查点：

```text
错误文案是否适合普通用户
是否保留必要的开发调试详情
```

### Step 10：测试和文档维护

目标：

```text
确认页面主流程可用，并记录实际修改内容
```

预期动作：

```text
手动测试完整流程
检查浏览器控制台无明显错误
如修改 Java 后端，运行 Maven test
根据实际代码变更维护本文件
阶段完成后更新 阶段9-17开发规划与维护清单.md
```

审查点：

```text
是否达到阶段 9 验收标准
是否进入阶段 10
```

## 10. UI 风格要求

本阶段不是营销官网，而是一个可操作的旅行规划工具页面。

设计方向：

```text
清爽
稳定
步骤清楚
少装饰
少大段说明文
移动端不崩
桌面端信息密度适中
```

避免：

```text
大面积纯装饰渐变
复杂动画
看起来像宣传页但不能操作
把所有区域都做成大卡片堆叠
把调试 JSON 放在最显眼位置
```

优先：

```text
步骤条
表单
状态徽标
清晰按钮
结果预览
版本列表
可折叠调试区
```

## 11. 注释规范要求

本阶段如果修改 HTML / JavaScript，必须遵守：

```text
C:\Users\DJI\Desktop\agent spring ai\注释规范.md
```

前端注释重点：

```text
解释异步流程为什么这样写
解释 state 字段和后端字段的映射
解释轮询何时开始和停止
解释失败时为什么降级或停止
不要解释普通 DOM 语法
```

如果新增 Java 代码：

```text
类级 Javadoc 必须说明系统位置和职责
公开方法必须说明输入、输出、流程和失败策略
Agent / Graph 相关代码必须说明读写哪些状态字段
Controller 必须说明 HTTP 语义
```

## 12. 预计修改文件

本阶段主要预计修改：

```text
AgentProjectTrip/src/main/resources/static/agent-lab.html
```

可能新增：

```text
AgentProjectTrip/src/main/resources/static/app.css
AgentProjectTrip/src/main/resources/static/agent-app.js
```

是否拆分文件由代码审查前决定：

```text
如果 agent-lab.html 已经过大，优先拆分 CSS / JS。
如果为了减少阶段风险，也可以继续单文件实现，但需要在完成文档中说明原因。
```

本阶段原则上不修改：

```text
LangGraphPlannerFacade.java
BranchDispatchNode.java
BranchAgentFacade.java
PlanDraftNode.java
TripRiskReasoningNode.java
数据库 schema
```

## 13. 测试计划

### 13.1 手动测试用例

测试用例 1：完整生成

```text
输入：国庆去法国和意大利玩10天，预算1200欧，不含国际机票，想避开人多
期望：需求表自动填充，确认后可以生成，生成成功后展示 plan
```

测试用例 2：缺少信息

```text
输入：我想下个月去欧洲玩，帮我安排
期望：页面提示目的地过宽、需要补充天数或预算等信息
```

测试用例 3：生成失败

```text
模拟后端返回 FAILED 或接口错误
期望：页面停止轮询，显示错误，允许用户回到需求表
```

测试用例 4：修改方案

```text
输入：把第3天安排轻松一点
期望：生成新版本或返回需要澄清的问题
```

测试用例 5：记忆管理

```text
新增偏好：我不喜欢赶路
期望：记忆列表出现该偏好，可以禁用
```

### 13.2 浏览器检查

```text
页面无明显布局错位
移动端宽度下不横向溢出
按钮 loading 状态正确
浏览器 console 无明显 JS 报错
```

### 13.3 后端测试

如果本阶段只修改静态前端：

```text
不强制运行 Maven test
```

如果修改 Java 后端：

```text
必须运行 Maven test
```

## 14. 验收标准

第 9 阶段通过标准：

```text
用户可以从自然语言输入开始，完整生成一次旅行计划。
用户不需要手动理解 requirementId / jobId / planId。
生成过程中页面有明确进度，不会假死。
生成成功后自动展示方案。
生成失败后能看到可理解错误。
用户可以继续输入自然语言修改已有方案。
用户可以查看至少当前版本信息。
用户可以查看和维护基础记忆偏好。
```

## 15. 审查流程

本阶段严格按审查点推进。

流程：

```text
1. 用户审查本文档
2. 用户确认后，开始第 9 阶段代码开发
3. 完成一个小步骤后，向用户说明结果并询问是否进入下一步
4. 所有代码完成后，进行代码审查
5. 代码审查通过后，按照实际修改内容维护本文档
6. 更新 阶段9-17开发规划与维护清单.md
```

每次审查时必须问：

```text
是否进行下一步？
```

## 16. 当前待用户确认的问题

在开始写代码前，需要用户确认：

```text
1. 第 9 阶段是否只修改 agent-lab.html，还是允许拆分 app.css 和 agent-app.js？
2. 是否保留原始 JSON 调试面板？建议保留，但折叠到“开发者调试”区域。
3. 页面名称是否继续使用 agent-lab.html，还是新增正式入口 travel-planner.html？
```

建议选择：

```text
允许拆分 app.css 和 agent-app.js。
保留原始 JSON 调试面板，但默认折叠。
新增 travel-planner.html 作为正式入口，保留 agent-lab.html 作为开发调试页。
```
