# 第14阶段开发：RAG 知识库运营与 Adaptive RAG

创建日期：2026-06-21

对应总控文档：

```text
阶段9-17开发规划与维护清单.md
```

当前状态：

```text
代码第二轮开发中
```

计划阶段归档文档：

```text
阶段 14 RAG 知识库运营与 Adaptive RAG.md
```

## 0.1 当前开发记录

### 2026-06-21：第 14 阶段 Adaptive RAG 第一版

本轮优先实现 Adaptive RAG 查询层，不先实现 MediaCrawler 和景点主数据全链路。

已完成目标：

```text
新增 RAG 查询类型：FACT_BASED / EXPLORATORY / COMPARATIVE / INSTRUCTIONAL / MULTI_HOP。
新增 RAG 检索策略：PRECISE_KEYWORD / SEMANTIC_EXPANSION / COMPARATIVE_MULTI_SOURCE / STRUCTURED_GUIDE_FIRST / MULTI_STAGE_CONTEXT。
新增知识来源类型：REQUIREMENT_SPEC / PINECONE_PRIVATE_GUIDE / POI_CATALOG / STRUCTURED_GUIDE / ONLINE_FACT_CHECK。
新增 AdaptiveRagDecision，用于记录 queryType、retrievalStrategy、sourceTypes、plannedQueries、topK 和 fallback 原因。
新增 RagRetrievalResult，用于记录最终 ragContext、executedQueries、hitCount 和 sourceSummaries。
新增 RagQueryClassifierAgent，用规则优先判断 RAG 查询类型。
新增 AdaptiveRagService，根据查询类型选择策略并构造不同检索 query。
新增 AdaptiveRagNode，把决策和结果写回 TravelPlanState。
LangGraphPlannerFacade 优先调用 AdaptiveRagNode，未注入或失败时回退 RetrieveKnowledgeNode。
KnowledgeTools 新增内部 topK 检索和 Document 返回能力，旧 @Tool 方法保持不变。
KnowledgeController 新增 POST /api/v1/knowledge/adaptive-rag/preview 调试接口。
PlanDraftNode 注入 RAG 检索轨迹，让核心 Planner 知道上下文来自哪种检索策略。
```

本轮暂未完成，后续 TODO：

```text
TODO(stage14-media-crawler-keywords)：接入 MediaCrawler 工程，根据 MySQL 景点表和旅行风格生成 base_config.py / KEYWORDS。
TODO(stage14-poi-catalog)：建立 travel_countries / travel_pois / poi_collection_profiles 等景点主数据表。
TODO(stage14-rag-ingestion-pipeline)：导入 MediaCrawler 原始内容，完成 raw -> clean -> chunk -> embedding -> Pinecone upsert 链路。
TODO(stage14-adaptive-rag-metadata-filter)：KnowledgeTools 支持 country、city、poi、styleTags、sourceType 等 metadata filter。
TODO(stage14-adaptive-rag-reranker)：在 Pinecone 召回后增加 reranker，避免只靠向量相似度排序。
TODO(stage14-adaptive-rag-model-fallback)：RagQueryClassifierAgent 接入 DeepSeek Flash 作为规则不确定时的模型兜底分类。
TODO(stage14-rag-trace-db)：把 rag_query_logs / rag_retrieval_traces 落库，供前端调试和阶段 17 Eval 使用。
TODO(stage14-frontend-rag-panel)：在正式前端调试面板展示 queryType、strategy、hitCount、executedQueries。
TODO(stage16-online-fact-check)：开放时间、闭馆日、签证政策、极端天气等强事实仍交给阶段 16 联网事实核查。
```

### 2026-06-22：Adaptive RAG 第一版人工测试通过，继续 POI 与关键词链路

用户已完成第 14 阶段第一轮 Adaptive RAG 手动测试，结果通过。

本轮继续推进“景点主数据 -> MediaCrawler KEYWORDS 候选”链路：

```text
schema-mysql.sql 新增 travel_countries / travel_pois / poi_collection_profiles。
schema-mysql.sql 种入法国、意大利、瑞士少量代表 POI。
新增 TravelCountryDTO / TravelPoiDTO。
新增 PoiCatalogService。
新增 JdbcPoiCatalogService，从 MySQL 读取 POI 主数据。
新增 InMemoryPoiCatalogService，便于 memory 模式临时排查。
新增 MediacrawlerKeywordBuilder，根据 POI、城市、标签和旅行风格生成小红书关键词候选。
KnowledgeController 新增 GET /api/v1/knowledge/pois/countries。
KnowledgeController 新增 GET /api/v1/knowledge/pois。
KnowledgeController 新增 POST /api/v1/knowledge/mediacrawler/keywords/preview。
新增关键词生成器和内存 POI 服务单元测试。
```

本轮暂未完成，后续 TODO：

```text
TODO(stage14-media-crawler-config-writer)：把审核后的 KEYWORDS 安全写入 C:\Users\DJI\Desktop\red note mediacrawler\base_config.py。
DONE(stage14-crawl-task-db)：已建立 crawl_tasks / crawl_task_keywords，并完成任务创建、关键词保存和查询接口。
TODO(stage14-media-crawler-runner)：后续再决定是否由 Java 后端触发 MediaCrawler；第一版暂不自动运行爬虫。
TODO(stage14-rag-ingestion-pipeline)：MediaCrawler 结果导入 raw_travel_documents，再清洗成 clean_travel_documents。
TODO(stage14-poi-admin-ui)：后续在前端提供 POI 覆盖状态和关键词预览面板，当前只提供后端接口。
```

### 2026-06-22：MediaCrawler 采集任务与配置预览链路

本轮继续把一次性关键词预览升级为可追踪的采集任务。

已完成：

```text
schema-mysql.sql 新增 crawl_tasks。
schema-mysql.sql 新增 crawl_task_keywords。
新增 CrawlTaskStatus，描述 DRAFT / READY_FOR_CONFIG / RUNNING / CRAWLED / INGESTED / FAILED。
新增 CrawlTaskDTO / CrawlTaskKeywordDTO。
新增 CrawlTaskService。
新增 JdbcCrawlTaskService，把任务和关键词保存到 MySQL。
新增 InMemoryCrawlTaskService，便于 memory 模式调试。
新增 MediacrawlerConfigWriter，把关键词渲染成 MediaCrawler base_config.py 可用的 KEYWORDS 配置片段。
KnowledgeController 新增 POST /api/v1/knowledge/mediacrawler/tasks。
KnowledgeController 新增 GET /api/v1/knowledge/mediacrawler/tasks/{taskId}。
新增配置片段和任务服务单元测试。
```

当前安全边界：

```text
本轮仍然不会自动写入 C:\Users\DJI\Desktop\red note mediacrawler\MediaCrawler-main\config\base_config.py。
本轮仍然不会自动运行 MediaCrawler。
后端只返回 configPreview，用户可以人工审查和复制。
```

本轮暂未完成，后续 TODO：

```text
TODO(stage14-media-crawler-config-writer-apply)：真正写 base_config.py 前，需要做路径白名单、原文件备份、diff 预览和人工确认开关。
TODO(stage14-crawl-task-status-flow)：补充 APPROVED / APPLIED_TO_CONFIG 等更细粒度状态，区分“已生成预览”和“已写入配置”。
TODO(stage14-media-crawler-runner)：后续再决定是否由 Java 后端触发 MediaCrawler；第一版暂不自动运行爬虫。
TODO(stage14-crawl-result-import)：把 MediaCrawler 的 jsonl 输出导入 raw_travel_documents。
```

## 0. 本阶段一句话目标

第 14 阶段要把当前“固定检索一次 Pinecone”的 RAG，升级成两个能力：

```text
1. 知识库可以长期维护：
   用数据库管理法国、意大利、瑞士的景点和攻略覆盖情况，
   后续基于这些景点定期生成 MediaCrawler 小红书关键词，
   将采集内容清洗、切片、写入 Pinecone。

2. 查询时可以自适应检索：
   根据用户问题类型选择不同 RAG 策略，
   不是所有问题都走同一套 similarity search。
```

## 1. 为什么现在做第 14 阶段

前 9-13 阶段已经完成了正式前端、结构化需求表、异步生成、真实机票/酒店工具、模型驱动分支派发。

现在系统已经能“调用工具并生成方案”，但 RAG 还偏弱：

```text
当前 RAG 更像一个静态资料库。
Pinecone 里的知识没有长期维护机制。
某个景点有没有攻略、攻略是否过期，目前没有数据库记录。
RetrieveKnowledgeNode 基本是固定检索流程。
事实问题、路线规划问题、比较问题、小众推荐问题都可能走同一种检索方式。
```

所以第 14 阶段要补上“知识更新”和“检索策略”这两个底座。

## 2. 本阶段不做什么

为了控制范围，第 14 阶段不做这些事：

```text
不做无限制大规模爬取。
不自动长期后台运行爬虫。
不把小红书内容当作强事实来源。
不在本阶段完成实时开放时间、天气灾害、签证变化的联网事实核查。
不在本阶段迁移标准 LangGraph4j。
不在本阶段做完整 Agent Eval。
```

这些分别放在后续阶段：

```text
阶段 15：标准 LangGraph4j 状态图迁移
阶段 16：联网事实核查与输出前强校验
阶段 17：Agent Eval 自动评估体系
```

## 3. 总体流程设计

第 14 阶段完成后，RAG 相关流程应变成：

```text
离线 / 半自动知识维护流程：

MySQL 景点主数据
 -> 根据国家、城市、景点、旅行风格生成 MediaCrawler KEYWORDS
 -> 少量运行 MediaCrawler
 -> 导入原始内容
 -> 清洗和去重
 -> 切片
 -> embedding
 -> upsert Pinecone
 -> 记录 MySQL 审计信息

在线规划检索流程：

用户需求 / 结构化需求表
 -> AdaptiveRagNode
 -> RagQueryClassifierAgent 判断查询类型
 -> AdaptiveRagService 选择检索策略
 -> KnowledgeTools / MySQL / Pinecone 执行检索
 -> 记录 rag trace
 -> 写回 TravelPlanState.ragContext
 -> BranchDispatch / PlanDraft / RiskReasoning 使用上下文
```

## 4. Adaptive RAG 的第一版类型

第一版先支持 5 类查询。

### 4.1 FACT_BASED：事实型

例子：

```text
欧洲有几个国家？
卢浮宫是什么类型的景点？
巴黎适合第一次欧洲旅行吗？
```

第一版检索策略：

```text
优先精确关键词
优先结构化数据和高可信摘要
topK 较小
生成时要求简洁回答
不做复杂多阶段推理
```

注意：

```text
开放时间、闭馆日、门票价格这类强事实，第一版可以检索已有知识，
但最终权威校验放到阶段 16。
```

### 4.2 EXPLORATORY：探索推荐型

例子：

```text
法国有哪些小众海边城市？
意大利适合慢游的地方有哪些？
瑞士有什么不那么游客化的路线？
```

第一版检索策略：

```text
语义扩展检索
使用风格标签过滤，例如小众、慢游、拍照、亲子、美食、徒步
topK 中等
结果要尽量多样化，避免全部命中同一个城市或同一类景点
```

### 4.3 COMPARATIVE：比较型

例子：

```text
巴黎和尼斯哪个更适合亲子？
法国和意大利哪个更适合预算有限的旅行？
罗马和佛罗伦萨哪个更适合第一次去意大利？
```

第一版检索策略：

```text
识别比较对象
分别检索每个对象的攻略和结构化信息
返回平衡上下文
生成时按维度比较：预算、交通、景点密度、人流、适合人群、风险
```

### 4.4 INSTRUCTIONAL：操作指导型

例子：

```text
法国申根签证怎么准备？
怎么买卢浮宫门票？
欧洲火车票怎么买比较便宜？
```

第一版检索策略：

```text
优先攻略步骤、FAQ、结构化说明
降低纯游记内容权重
生成时输出步骤、注意事项和风险提醒
```

注意：

```text
签证规则可能变化，本阶段只做已有知识检索和提醒。
实时政策强校验放到阶段 16。
```

### 4.5 MULTI_HOP：多跳规划型

例子：

```text
从都柏林出发，2026年7月1日去法国和瑞士玩5天，预算1500欧，预算包含国际机票。
国庆去法国和意大利玩10天，预算1200欧，不含国际机票，想避开人多。
```

第一版检索策略：

```text
拆成多阶段检索：
1. 目的地概览
2. 景点与城市推荐
3. 交通串联
4. 预算和住宿经验
5. 人流、季节、风险提示

把多阶段结果压缩成一个规划上下文包，再交给 PlanDraftNode。
```

## 5. 本阶段代码任务拆分

### 5.1 任务一：数据库景点主数据和 RAG 维护表

目标：

```text
在 MySQL 里建立 RAG 知识运营需要的基础表。
```

计划新增或修改：

```text
AgentProjectTrip/src/main/resources/schema-mysql.sql
```

候选表：

```text
travel_countries
travel_pois
poi_collection_profiles
crawl_tasks
crawl_task_keywords
raw_travel_documents
clean_travel_documents
rag_knowledge_chunks
pinecone_upsert_records
rag_source_profiles
rag_query_logs
rag_retrieval_traces
```

第一版种子数据：

```text
France
Italy
Switzerland
```

景点数据先少量即可，例如每个国家 5-10 个代表景点，验证流程优先。

### 5.2 任务二：POI 和采集任务服务

目标：

```text
让 Java 后端可以管理景点、查看攻略覆盖情况、创建采集任务。
```

计划新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/core/service/PoiCatalogService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/CrawlTaskService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/JdbcPoiCatalogService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/JdbcCrawlTaskService.java
```

职责：

```text
PoiCatalogService：
读取国家、城市、景点、标签、启用状态、RAG 覆盖状态。

CrawlTaskService：
创建采集任务，记录关键词、状态、错误信息和执行结果。
```

### 5.3 任务三：MediaCrawler 关键词生成

目标：

```text
根据景点表和旅行风格生成适合小红书搜索的 KEYWORDS。
```

计划新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/core/etl/MediacrawlerKeywordBuilder.java
AgentProjectTrip/src/main/java/com/travel/agent/core/etl/MediacrawlerConfigWriter.java
```

关键词生成例子：

```text
巴黎 小众 旅行 攻略
巴黎 避开人群 景点
卢浮宫 门票 避坑
佛罗伦萨 深度游 攻略
瑞士 少女峰 预算 交通
```

第一版安全策略：

```text
只生成关键词和配置候选。
不自动无限运行 MediaCrawler。
需要用户手动确认或后端手动触发。
```

### 5.4 任务四：原始内容导入、清洗、切片

目标：

```text
把 MediaCrawler 导出的内容导入本项目，再清洗成适合 RAG 的 chunk。
```

计划新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/core/etl/TravelContentCleaner.java
AgentProjectTrip/src/main/java/com/travel/agent/core/etl/TravelContentChunker.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/RagKnowledgeMaintenanceService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/PineconeKnowledgeMaintenanceService.java
```

清洗规则第一版：

```text
去掉空内容
去掉明显广告和无意义短文本
保留标题、正文摘要、目的地、景点、标签、发布时间、来源
同一来源重复内容做去重
按 500-1000 中文字左右切片
为 chunk 增加 metadata：country、city、poi、styleTags、sourceType、publishedAt、collectedAt
```

### 5.5 任务五：Adaptive RAG 模型对象

目标：

```text
建立查询类型、检索策略、检索结果和决策轨迹的数据结构。
```

计划新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/RagQueryType.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/RagRetrievalStrategy.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/AdaptiveRagDecision.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/RagRetrievalResult.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/KnowledgeSourceType.java
```

代码框架示意：

```java
public enum RagQueryType {
    FACT_BASED,
    EXPLORATORY,
    COMPARATIVE,
    INSTRUCTIONAL,
    MULTI_HOP
}
```

```java
public enum RagRetrievalStrategy {
    PRECISE_KEYWORD,
    SEMANTIC_EXPANSION,
    COMPARATIVE_MULTI_SOURCE,
    STRUCTURED_GUIDE_FIRST,
    MULTI_STAGE_CONTEXT
}
```

### 5.6 任务六：查询类型识别 Agent

目标：

```text
根据用户需求和结构化需求表判断 RAG 查询类型。
```

计划新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/RagQueryClassifierAgent.java
```

第一版策略：

```text
规则优先：
  有“哪个更适合 / 对比 / 比较” -> COMPARATIVE
  有“怎么买 / 如何 / 怎么准备” -> INSTRUCTIONAL
  有多目的地 + 天数 + 预算 + 时间 -> MULTI_HOP
  有“推荐 / 小众 / 适合 / 有哪些” -> EXPLORATORY
  其他短问题 -> FACT_BASED

规则不确定时，再调用小模型或 DeepSeek Flash 做分类。
```

这样做的原因：

```text
节省 token。
提高稳定性。
避免所有分类都依赖模型。
```

### 5.7 任务七：AdaptiveRagService

目标：

```text
根据 RagQueryType 选择检索策略，调用 MySQL / Pinecone / KnowledgeTools，返回统一 RAG 上下文。
```

计划新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/core/service/AdaptiveRagService.java
```

第一版策略映射：

```text
FACT_BASED -> PRECISE_KEYWORD
EXPLORATORY -> SEMANTIC_EXPANSION
COMPARATIVE -> COMPARATIVE_MULTI_SOURCE
INSTRUCTIONAL -> STRUCTURED_GUIDE_FIRST
MULTI_HOP -> MULTI_STAGE_CONTEXT
```

输出内容：

```text
AdaptiveRagDecision：记录系统为什么这样检索。
RagRetrievalResult：记录最终上下文、来源列表、命中 chunk、过滤条件。
```

### 5.8 任务八：Graph 接入

目标：

```text
用 AdaptiveRagNode 替代或包装 RetrieveKnowledgeNode。
```

计划新增或修改：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/AdaptiveRagNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/RetrieveKnowledgeNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelPlanState.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/LangGraphPlannerFacade.java
```

接入方式第一版：

```text
保留 RetrieveKnowledgeNode 作为 fallback。
新增 AdaptiveRagNode。
LangGraphPlannerFacade 优先调用 AdaptiveRagNode。
如果 AdaptiveRagNode 失败，则回退 RetrieveKnowledgeNode。
```

TravelPlanState 计划新增字段：

```text
adaptiveRagDecision
ragRetrievalResult
ragTraceSummary
```

### 5.9 任务九：调试接口和前端显示

目标：

```text
方便人工测试 Adaptive RAG 是否真的选择了不同策略。
```

计划修改：

```text
AgentProjectTrip/src/main/java/com/travel/agent/web/KnowledgeController.java
AgentProjectTrip/src/main/resources/static/agent-app.js
AgentProjectTrip/src/main/resources/static/app.css
```

第一版可以先加后端调试接口：

```text
POST /api/v1/knowledge/adaptive-rag/preview
```

输入：

```json
{
  "message": "巴黎和尼斯哪个更适合亲子？",
  "destinations": ["巴黎", "尼斯"],
  "durationDays": 5,
  "budgetAmount": 1500,
  "budgetCurrency": "EUR"
}
```

输出：

```json
{
  "queryType": "COMPARATIVE",
  "strategy": "COMPARATIVE_MULTI_SOURCE",
  "sources": ["PINECONE_UGC", "POI_CATALOG"],
  "trace": "分别检索巴黎和尼斯，并按亲子、预算、交通、人流维度组织上下文"
}
```

前端可以先只在调试面板展示：

```text
RAG 类型
检索策略
命中文档数量
数据来源
```

## 6. 手动测试计划

### 6.1 测试景点主数据

目标：

```text
确认数据库中存在 France / Italy / Switzerland 和少量景点。
```

测试方式：

```text
启动 Spring Boot。
调用景点列表接口，检查返回国家和景点。
```

### 6.2 测试关键词生成

目标：

```text
确认系统可以根据景点和风格生成 MediaCrawler KEYWORDS。
```

示例：

```text
国家：France
景点：Paris / Louvre Museum
风格：小众、避坑、亲子
```

预期：

```text
生成类似：
巴黎 小众旅行 攻略
卢浮宫 门票 避坑
巴黎 亲子 景点 推荐
```

### 6.3 测试 Adaptive RAG 分类

测试问题一：

```text
巴黎和尼斯哪个更适合亲子？
```

预期：

```text
queryType = COMPARATIVE
strategy = COMPARATIVE_MULTI_SOURCE
```

测试问题二：

```text
法国有哪些小众海边城市？
```

预期：

```text
queryType = EXPLORATORY
strategy = SEMANTIC_EXPANSION
```

测试问题三：

```text
从都柏林出发，2026年7月1日去法国和瑞士玩5天，预算1500欧，预算包含国际机票。
```

预期：

```text
queryType = MULTI_HOP
strategy = MULTI_STAGE_CONTEXT
```

### 6.4 测试完整规划

输入：

```text
从都柏林出发，2026年7月1日去法国和瑞士玩5天，预算1500欧，预算包含国际机票，想住经济型酒店，尽可能完整参观著名热门景点。
```

预期：

```text
需求整理正常。
Graph 日志中出现 AdaptiveRagNode。
日志中显示 queryType 和 retrievalStrategy。
最终方案仍能生成。
方案中能体现 RAG 提供的目的地、景点或攻略上下文。
```

## 7. 验收标准

第 14 阶段完成时，需要满足：

```text
MySQL 中可以维护法国、意大利、瑞士的国家和景点主数据。
后端可以根据景点和旅行风格生成 MediaCrawler KEYWORDS。
可以导入少量 MediaCrawler 或模拟采集内容。
可以清洗、切片，并记录待写入 Pinecone 的 chunk。
可以将 chunk upsert 到 Pinecone，或在无 Pinecone 配置时给出清晰 fallback。
Adaptive RAG 可以识别 5 类查询类型。
不同查询类型能选择不同检索策略。
Graph 规划流程能使用 Adaptive RAG 输出的 ragContext。
日志或调试接口可以看到 queryType、strategy、source、topK 和命中文档数量。
旧 RetrieveKnowledgeNode fallback 保留，Adaptive RAG 失败不影响主规划流程。
```

## 8. 第一轮代码优先级

如果用户审核通过，建议按这个顺序写代码：

```text
1. 先写 RagQueryType / RagRetrievalStrategy / AdaptiveRagDecision 等模型对象。
2. 写 RagQueryClassifierAgent 的规则分类第一版。
3. 写 AdaptiveRagService 的策略选择框架，先复用现有 KnowledgeTools 检索。
4. 写 AdaptiveRagNode，并接入 LangGraphPlannerFacade。
5. 写调试接口，先验证不同问题是否走不同策略。
6. 再写景点主数据表和 MediaCrawler KEYWORDS 生成。
7. 最后做原始内容导入、清洗、切片、Pinecone upsert。
```

这样做的原因：

```text
Adaptive RAG 查询层能最快验证核心想法。
MediaCrawler 和 Pinecone 维护链路更长，可以第二轮补齐。
先保证主规划流程不被破坏。
```

## 9. 审核问题

请重点确认这几个问题：

```text
1. 第 14 阶段是否允许先做 Adaptive RAG 查询层，再做 MediaCrawler 维护链路？
2. 第一版是否接受“规则优先 + 模型兜底”的查询类型识别？
3. 是否同意先提供调试接口，再把 Adaptive RAG 结果展示到正式前端？
4. MediaCrawler 第一版是否只生成 KEYWORDS 和导入结果，不自动长期运行爬虫？
```

你审核通过后，我再开始写第 14 阶段代码。
