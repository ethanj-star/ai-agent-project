# 阶段6.5前端可视化调试台

本文档是阶段 6.5 的实施计划。它不是正式产品前端，而是一个面向开发和人工测试的 Agent 可视化调试台，用来快速验证第五阶段“结构化需求确认与生成门控”和第六阶段“计划版本化与自然语言修改闭环”。

阶段 6.5 的核心思想是：

```text
用一个简单网页串起 draft -> confirm -> generate -> plan modify 的完整链路
```

第一版建议直接放在 Spring Boot 静态资源目录：

```text
AgentProjectTrip/src/main/resources/static/agent-lab.html
```

启动后访问：

```text
http://localhost:8080/agent-lab.html
```

这样可以避免前端工程化、CORS、Vite、React、npm 依赖等额外复杂度，让我们先把 Agent 工作流测顺。

## 1. 阶段 6.5 目标

完成后，系统应该具备以下能力：

1. 用户可以在网页中输入自然语言旅行需求。
2. 点击按钮后调用 `POST /api/v1/requirements/draft`，显示结构化需求表。
3. 用户可以在网页中编辑需求表字段。
4. 点击确认后调用 `POST /api/v1/requirements/{requirementId}/confirm`。
5. 点击生成后调用 `POST /api/v1/requirements/{requirementId}/generate`。
6. 页面显示完整规划结果、`requirementId`、`planId`、当前版本号和剩余额度。
7. 用户可以输入自然语言修改已有计划。
8. 点击修改后调用 `POST /api/v1/plans/{planId}/modify`。
9. 页面根据返回状态展示新版本、更新后的需求表、追问问题或普通回复。
10. 页面有 loading、错误提示、原始 JSON 查看区域，方便调试。

阶段 6.5 暂时不做：

- 正式用户登录。
- 真正支付页面。
- 复杂 UI 组件库。
- React / Vue / Vite 工程。
- 响应式移动端精修。
- Markdown 高级渲染库。
- 数据持久化。
- 计划版本对比和回滚 UI。

## 2. 为什么先做调试台

当前系统已经不是单个 GET URL 就能测完，而是一个多阶段链路：

```text
需求抽取 -> 需求确认 -> 完整生成 -> 保存 planId -> 自然语言修改 -> 版本更新
```

如果只用浏览器地址栏或 PowerShell，会有几个问题：

1. 每次都要手动复制 `requirementId` 和 `planId`。
2. 不方便编辑结构化需求表。
3. 不容易观察不同状态返回。
4. 完整生成耗时长，缺少 loading 状态时很难判断是否卡住。
5. 修改计划有多种分支结果，命令行测试体验差。

调试台的作用是让我们能像使用真实产品一样测试 Agent 流程，但不提前投入正式前端工程成本。

## 3. 页面入口和文件位置

推荐文件：

```text
AgentProjectTrip/src/main/resources/static/agent-lab.html
```

访问地址：

```text
http://localhost:8080/agent-lab.html
```

原因：

- Spring Boot 默认会暴露 `static` 目录。
- 页面和 API 同源，不需要处理 CORS。
- 一个 HTML 文件即可包含 HTML、CSS、JavaScript。
- 适合当前阶段快速调试。

后续正式产品前端可以再迁移到：

```text
React / Vue / Next.js / 独立前端项目
```

## 4. 页面结构设计

页面建议分为 5 个主区域。

### 4.1 顶部状态栏

显示当前会话和关键 ID：

```text
Session ID: test-006
Requirement ID: req-...
Plan ID: plan-...
Current Version: v1 / v2
Remaining Credits: 2
```

按钮：

```text
新建会话
清空页面状态
复制 planId
```

第一版状态可以只存在浏览器内存中，不需要 localStorage。后续可加 localStorage，方便刷新后保留 ID。

### 4.2 自然语言需求输入区

控件：

```text
textarea: 用户自然语言旅行需求
button: 整理需求表
button: 使用示例
```

示例文本：

```text
国庆去法国和意大利玩10天，预算1200欧，不含国际机票，2个人，从上海出发，想避开人多的地方
```

调用接口：

```http
POST /api/v1/requirements/draft
```

请求：

```json
{
  "sessionId": "test-006",
  "message": "..."
}
```

成功后：

- 保存 `requirementId`
- 保存 `spec`
- 保存 `validation`
- 渲染需求表编辑区
- 渲染原始 JSON

### 4.3 需求表编辑区

显示 `TravelRequirementSpec` 的核心字段。

第一版表单字段：

```text
destinations                      目的地，逗号分隔
departureCity                     出发城市
startDateText                     出行时间文本
durationDays                      行程天数
travelerCount                     旅行人数
budgetAmount                      预算金额
budgetCurrency                    预算币种
budgetIncludesInternationalFlight 是否包含国际机票
preferences                       偏好，逗号分隔
avoidances                        避开内容，逗号分隔
travelStyle                       旅行风格
accommodationPreference           住宿偏好
transportPreference               交通偏好
```

按钮：

```text
保存需求表
确认需求表
生成完整规划
```

对应接口：

```http
PUT /api/v1/requirements/{requirementId}
POST /api/v1/requirements/{requirementId}/confirm
POST /api/v1/requirements/{requirementId}/generate
```

交互规则：

- `保存需求表`：只更新字段并重新校验，不进入生成。
- `确认需求表`：字段完整才会变为 `CONFIRMED`。
- `生成完整规划`：只有确认后才应该点击；生成成功后返回 `planId`。

如果后端返回缺失字段：

```text
missingFields: ["departureCity", "travelerCount"]
```

页面应在表单上方显示：

```text
还需要补充：departureCity、travelerCount
```

### 4.4 规划结果展示区

生成成功后展示：

```text
planId
currentVersion
remainingCredits
完整 Markdown 答案
```

第一版可以用 `<pre>` 或简单 `div white-space: pre-wrap` 显示 Markdown，不接 Markdown 渲染库。

按钮：

```text
刷新当前计划
查看 v1
查看当前版本
复制结果
```

对应接口：

```http
GET /api/v1/plans/{planId}
GET /api/v1/plans/{planId}/versions/{version}
```

### 4.5 自然语言修改区

控件：

```text
textarea: 修改指令
button: 修改计划
button: 使用局部修改示例
button: 使用核心需求变更示例
```

局部修改示例：

```text
第三天太赶了，少安排一个城市，多留一点自由时间
```

核心需求变更示例：

```text
预算改成900欧，不想住青旅，改成经济型酒店
```

调用接口：

```http
POST /api/v1/plans/{planId}/modify
```

请求：

```json
{
  "sessionId": "test-006",
  "message": "第三天太赶了，少安排一个城市，多留一点自由时间"
}
```

返回状态处理：

| status | 页面行为 |
|---|---|
| `UPDATED` | 更新当前答案、版本号和计划展示区 |
| `REQUIREMENT_NEEDS_CONFIRMATION` | 显示更新后的需求表，让用户重新确认 |
| `REQUIREMENT_NEEDS_USER_INPUT` | 显示缺失字段，让用户补全需求表 |
| `NEEDS_CLARIFICATION` | 显示追问问题 |
| `DIRECT_COMMENT` | 显示普通提示，不改版本 |
| `REVISION_FAILED` | 显示错误，保留旧版本 |

## 5. 前端状态设计

第一版可以使用一个全局 JS 对象：

```javascript
const state = {
  sessionId: "test-006",
  requirementId: null,
  planId: null,
  currentVersion: null,
  remainingCredits: null,
  spec: null,
  validation: null,
  graphResult: null,
  currentAnswer: null,
  lastRawResponse: null,
  loading: false
};
```

状态来源：

- `requirementId` 来自 draft 响应。
- `spec` 来自 draft / update / confirm / modify 返回。
- `planId` 来自 generate 响应。
- `currentVersion` 来自 generate 后 plan 查询，或 modify 返回。
- `remainingCredits` 来自 generate 响应。
- `currentAnswer` 来自 graphResult.answer 或 plan 查询响应。

第一版暂时不需要复杂状态管理库。

## 6. API 调用顺序

### 6.1 标准成功链路

```text
1. POST /api/v1/requirements/draft
2. 用户编辑表单
3. PUT /api/v1/requirements/{requirementId}
4. POST /api/v1/requirements/{requirementId}/confirm
5. POST /api/v1/requirements/{requirementId}/generate
6. GET /api/v1/plans/{planId}
7. POST /api/v1/plans/{planId}/modify
```

### 6.2 信息不足链路

```text
用户输入：我想下个月去欧洲玩，帮我安排
-> draft
-> 页面显示缺失字段
-> 不允许或不建议点击生成
```

### 6.3 核心需求变更链路

```text
已有 planId
-> 用户输入：预算改成900欧
-> modify 返回 REQUIREMENT_NEEDS_CONFIRMATION
-> 页面更新需求表
-> 用户确认需求表
-> 用户重新 generate
-> 后端生成新的 planId 或后续扩展为同 planId 新版本
```

注意：当前第六阶段第一版中，核心需求变更返回更新后的需求表；重新 generate 可能创建新的 `planId`。后续可以优化为同一个 `planId` 下追加新版本。

## 7. Loading 和错误处理

页面至少需要三个 loading 状态：

```text
整理需求表中...
确认需求表中...
生成完整规划中，这一步可能需要 1-2 分钟...
修改计划中...
```

错误展示规则：

- HTTP 400：展示后端返回的 assistantMessage 或 graphResult.answer。
- HTTP 404：提示 requirementId / planId 不存在，可能是后端重启导致内存状态丢失。
- HTTP 402：提示额度不足。
- HTTP 500：提示服务异常，并展示原始错误 JSON。

由于当前仓库是内存版，页面顶部应显示一个提示：

```text
开发调试模式：后端重启后 requirementId、planId 和版本记录会丢失。
```

## 8. 页面样式建议

调试台不是正式产品，但要清晰。

布局建议：

```text
两列布局：
左侧：输入和需求表
右侧：结果、修改和原始 JSON
```

颜色建议：

- 背景：浅灰或白色。
- 卡片：简单边框，不做复杂装饰。
- 成功：绿色小标签。
- 警告：黄色小标签。
- 错误：红色文本。
- Loading：按钮禁用 + 文案变化。

避免：

- 大面积渐变。
- 装饰性图形。
- 复杂动画。
- 过多营销文案。

## 9. 推荐文件结构

第一版单文件：

```text
AgentProjectTrip/src/main/resources/static/agent-lab.html
```

文件内部结构：

```html
<style>
  页面样式
</style>

<body>
  顶部状态栏
  自然语言输入区
  需求表编辑区
  规划结果区
  修改区
  原始 JSON 区
</body>

<script>
  state
  api()
  render()
  draftRequirement()
  saveRequirement()
  confirmRequirement()
  generatePlan()
  loadPlan()
  modifyPlan()
</script>
```

后续如果页面变复杂，再拆成：

```text
static/agent-lab/index.html
static/agent-lab/app.js
static/agent-lab/style.css
```

## 10. 测试计划

### 10.1 完整成功链路

输入：

```text
国庆去法国和意大利玩10天，预算1200欧，不含国际机票，2个人，从上海出发，想避开人多的地方
```

期望：

1. 点击“整理需求表”后显示结构化需求。
2. `status` 为 `READY_TO_CONFIRM` 或可确认状态。
3. 点击“确认需求表”成功。
4. 点击“生成完整规划”后返回完整答案。
5. 页面显示 `planId`。
6. 页面显示剩余额度。

### 10.2 信息不足链路

输入：

```text
我想下个月去欧洲玩，帮我安排
```

期望：

1. 页面显示缺失字段。
2. 不直接生成完整规划。
3. 用户可以手动补全目的地、天数、预算等字段。

### 10.3 局部修改链路

修改输入：

```text
第三天太赶了，少安排一个城市，多留一点自由时间
```

期望：

1. 返回 `UPDATED`。
2. 当前版本变为 `v2`。
3. 结果区显示修改后的答案。

### 10.4 核心需求变更链路

修改输入：

```text
预算改成900欧，不想住青旅，改成经济型酒店
```

期望：

1. 返回 `REQUIREMENT_NEEDS_CONFIRMATION`。
2. 页面需求表中预算变成 900。
3. 住宿偏好变成经济型酒店。
4. 页面提示需要重新确认需求表。

### 10.5 普通评论链路

修改输入：

```text
这个方案不错，我先看看
```

期望：

1. 返回 `DIRECT_COMMENT`。
2. 不新增版本。
3. 页面显示普通回复。

## 11. 验收标准

阶段 6.5 完成后必须满足：

1. 能通过 `http://localhost:8080/agent-lab.html` 打开页面。
2. 页面能完成 draft -> confirm -> generate 的第五阶段链路。
3. 页面能显示并保存 `requirementId`。
4. 页面能显示并保存 `planId`。
5. 页面能调用 modify 接口完成局部修改。
6. 页面能识别核心需求变更返回，并展示更新后的需求表。
7. 页面有 loading 状态，完整生成时不会看起来像卡死。
8. 页面能展示原始 JSON，方便调试。
9. 后端重启导致 ID 失效时，页面能显示友好错误。
10. 不引入新的前端构建依赖。

## 12. 后续正式前端预告

调试台稳定后，可以进入正式前端阶段：

- React / Vue 项目。
- 用户登录。
- 需求表更好的组件化编辑。
- Markdown 渲染。
- 计划版本列表和对比。
- 计划导出 PDF。
- 支付和额度页。
- 异步生成进度条。
- 响应式移动端。
- 地图、日历和每日行程卡片。
