# 第11阶段开发：异步生成进度和结果页完善

创建日期：2026-06-14

对应总控文档：

```text
阶段9-17开发规划与维护清单.md
```

当前状态：

```text
已完成，人工测试通过
```

## 0.1 当前开发记录

### 2026-06-14：人工测试通过，阶段完成

用户已在正式前端页面完成手动测试，并确认第 11 阶段测试通过。

本阶段正式归档文档：

```text
阶段 11 异步生成进度和结果页完善.md
```

### 2026-06-14：第 11 阶段代码第一版

本轮已按人工确认的方向完成第一版代码：

```text
GET /api/v1/jobs 列表接口从裸 List 改为 GenerationJobListResponse
GenerationJobResponse 新增 progressPercent / stageLabel / actionHint 等展示字段
前端新增最近任务列表
前端新增刷新页面恢复任务逻辑
本阶段暂不新增取消任务接口，只兼容 CANCELLED 展示
```

后端完成内容：

```text
GenerationJobResponse 新增状态中文名、阶段中文名、阶段说明、进度百分比、操作建议、耗时、终态、可恢复字段
GenerationJobListResponse 新增，用于包装 sessionId 最近任务列表和恢复摘要
GenerationJobController.listJobs(...) 改为返回 GenerationJobListResponse
GenerationJobController 注释明确任务查询接口只读、不扣费、不触发 Graph
GenerationJobResponseTest 新增，覆盖展示字段和列表摘要
```

前端完成内容：

```text
travel-planner.html 生成进度区新增 jobMeta、jobTimeline、jobAdvice、recentJobs 和最近任务按钮
travel-planner.html 静态资源版本更新为 20260614-5
agent-app.js 新增 jobDetail、recentJobs、job 时间字段、actionHint 等状态
agent-app.js 新增 loadRecentJobs、recoverLatestJob、applyJobResponse、applyJobListResponse、selectRecentJob
agent-app.js 增强 loadJobStatus，终态后刷新最近任务，成功后自动加载 plan，失败后展示建议
agent-app.js 新增任务时间线、失败建议、最近任务列表渲染
app.css 新增任务状态徽标、任务摘要、时间线、失败建议、最近任务列表样式
```

已执行验证：

```text
node --check AgentProjectTrip/src/main/resources/static/agent-app.js
通过

HTML / JS id 绑定检查
通过，绑定的 54 个 id 全部存在

mvn test
通过，Tests run: 105, Failures: 0, Errors: 0, Skipped: 0

mvn process-resources
通过，静态资源已同步到 target/classes

node --check AgentProjectTrip/target/classes/static/agent-app.js
通过

target/classes 静态资源检查
通过，travel-planner.html 已包含 20260614-5、jobMeta、jobTimeline、jobAdvice、recentJobs、reloadRecentJobsButton
```

下一步：

```text
已完成正式归档。
下一步进入阶段 12：真实接入航班分支。
```

## 0. 本阶段一句话目标

第 11 阶段的目标是：把“后台异步生成任务”变成用户能看懂、能等待、能恢复、能处理失败的正式产品体验。

用户应该可以：

```text
确认需求 -> 点击生成 -> 立刻看到任务创建成功
 -> 看到当前生成阶段和解释
 -> 刷新页面后能找回正在运行的任务
 -> 成功后自动展示旅行方案
 -> 失败后知道失败原因，并能回到需求表修正后重试
```

系统应该避免：

```text
用户点击生成后不知道系统有没有开始工作
页面刷新后丢失 jobId / planId
任务失败时只显示生硬异常
生成成功后用户还要手动找 planId
同一个 requirementId 在运行中重复点击导致重复创建任务或重复扣费
```

## 1. 本阶段核心思想

前 10 个阶段已经让系统具备了两个重要基础：

```text
第 9 阶段：正式前端 MVP 页面
第 10 阶段：生成前需求表单和确认门控
```

现在用户已经可以比较稳定地把旅行需求整理成结构化表单，但完整生成仍然是长耗时任务。

因此第 11 阶段不急着增加新 Agent，也不急着接航班分支。  
本阶段先把“生成过程”这条用户体验链路打磨好。

核心判断：

```text
Agent 系统不是只要最后能吐出答案就算完成。
对于需要几十秒甚至几分钟的任务，用户必须知道系统正在做什么、为什么还没结束、失败后该怎么办。
```

所以本阶段的重点是：

```text
GenerationJob 可读化
前端进度体验增强
失败恢复路径增强
刷新页面恢复任务
最近任务列表和结果找回
```

## 2. 本阶段不做什么

为了控制边界，第 11 阶段明确不做：

```text
不接入真实航班分支
不把 BranchDispatch 改成模型驱动
不新增酒店 / 签证 / 预算等分支 Agent
不接真实支付
不做登录注册
不迁移 LangGraph4j
不大改 Graph 核心规划逻辑
不改第 10 阶段需求表单的核心字段规则，除非发现影响异步流程的 bug
```

本阶段允许做少量后端 DTO / 接口增强，但必须围绕“异步任务和结果页体验”服务。

## 3. 当前代码现状

### 3.1 后端已有能力

当前已经存在异步生成任务模型和接口：

```text
GenerationJob
GenerationJobStatus
GenerationJobStage
GenerationJobStore
InMemoryGenerationJobStore
JdbcGenerationJobStore
AsyncPlanGenerationService
GenerationJobController
GenerationJobResponse
```

当前已有接口：

```text
GET /api/v1/jobs/{jobId}
按 jobId 查询任务详情。

GET /api/v1/jobs?sessionId=xxx&limit=5
按 sessionId 查询最近任务。
```

当前已有阶段：

```text
CREATED
VALIDATING_REQUIREMENT
CHARGING_CREDIT
RUNNING_GRAPH
SAVING_PLAN
REFUNDING_CREDIT
FINISHED
```

当前已有状态：

```text
PENDING
RUNNING
SUCCEEDED
FAILED
CANCELLED
```

### 3.2 前端已有能力

当前正式页面已经有：

```text
travel-planner.html
app.css
agent-app.js
```

`agent-app.js` 已经具备：

```text
generatePlanAsync()
startJobPolling()
stopJobPolling()
loadJobStatus(showErrors)
loadPlan(showErrors)
renderJobProgress()
renderPlan()
persistState()
restoreState()
isJobActive(status)
```

当前前端状态字段已经有：

```text
jobId
jobStatus
jobStage
planId
remainingCredits
planRecord
pollingTimer
lastAssistantMessage
lastRawResponse
```

### 3.3 当前不足

虽然异步任务已经能跑通，但用户体验仍然偏“工程调试”：

```text
进度阶段只有简单文本，解释不够具体
缺少任务阶段时间线
缺少最近任务列表
刷新页面后只依赖 localStorage，不能主动从后端找回最近任务
失败原因没有分层展示，用户不知道是需求问题、额度问题、工具问题还是模型问题
成功后虽然能自动 loadPlan，但结果区域可以更明确地展示“已完成、planId、版本”
正在运行时的按钮禁用和状态提示还可以更清楚
开发者 JSON 有，但用户侧缺少更友好的任务摘要
```

## 4. 本阶段解决的问题

### 4.1 解决“用户不知道系统在做什么”

页面需要把后端阶段翻译成人话：

```text
CREATED -> 已创建生成任务，正在排队准备
VALIDATING_REQUIREMENT -> 正在检查需求表是否满足生成条件
CHARGING_CREDIT -> 正在扣除一次生成额度
RUNNING_GRAPH -> 正在调用核心 Agent 工作流生成方案
SAVING_PLAN -> 正在保存最终旅行方案
REFUNDING_CREDIT -> 生成失败，正在退回额度
FINISHED -> 任务已结束
```

### 4.2 解决“刷新页面后任务丢失”

页面加载时应该优先恢复本地状态；如果本地没有活跃任务，但有 sessionId，则调用：

```text
GET /api/v1/jobs?sessionId={sessionId}&limit=5
```

从后端找最近任务。  
如果最近任务中有 PENDING / RUNNING，则自动恢复轮询。  
如果最近任务中有 SUCCEEDED 且有 planId，则允许用户一键恢复方案。

### 4.3 解决“失败不知道怎么处理”

失败时页面不应该只显示异常文本。  
需要根据任务状态和 errorMessage 给出操作建议：

```text
需求缺失或未确认 -> 回到需求表修改
额度不足 -> 提示额度不足，后续阶段接支付
Graph 失败 -> 可以重试，保留需求表
工具失败 -> 提醒部分外部服务不可用，可稍后重试
未知异常 -> 展示简短失败原因，并在 JSON 面板保留完整响应
```

### 4.4 解决“成功后进入结果不够明确”

成功后页面应该：

```text
停止轮询
自动读取 plan
显示“方案已生成”
显示 planId 简写和当前版本
允许复制方案
允许继续自然语言修改
保留最近任务摘要
```

## 5. 本阶段总体流程设计

第 11 阶段完成后的用户流程：

```text
用户确认需求表
  ↓
点击“生成完整方案”
  ↓
POST 生成接口创建 GenerationJob
  ↓
前端获得 jobId，立即显示任务卡片
  ↓
前端定时 GET /api/v1/jobs/{jobId}
  ↓
任务处于 PENDING / RUNNING
  ↓
显示阶段解释、进度条、运行时长、当前动作
  ↓
任务 SUCCEEDED
  ↓
停止轮询，自动 GET plan，展示旅行方案
  ↓
任务 FAILED
  ↓
停止轮询，展示失败原因和下一步建议
```

刷新恢复流程：

```text
页面打开
  ↓
restoreState() 从 localStorage 恢复
  ↓
如果有 active jobId，继续轮询
  ↓
如果没有 active jobId，但有 sessionId
  ↓
GET /api/v1/jobs?sessionId={sessionId}&limit=5
  ↓
渲染最近任务列表
  ↓
如有运行中任务，提示或自动恢复
  ↓
如有成功任务，允许一键加载方案
```

## 6. 后端计划

### 6.1 GenerationJobResponse 增强

计划在 `GenerationJobResponse` 中增加面向前端展示的派生字段：

```text
stageLabel
stageDescription
statusLabel
progressPercent
actionHint
durationSeconds
terminal
recoverable
```

字段用途：

```text
stageLabel：阶段短标题，例如“执行核心规划”
stageDescription：阶段解释，例如“正在调用核心 Agent 工作流生成初稿、评估风险并保存结果”
statusLabel：状态短标题，例如“生成中”“已完成”“失败”
progressPercent：前端进度条使用的建议百分比
actionHint：当前用户可以做什么，例如“请保持页面打开，或稍后回来查看”
durationSeconds：任务已运行或总耗时
terminal：是否已经进入终态
recoverable：失败后是否建议用户修改需求后重试
```

说明：  
进度百分比只是用户体验辅助，不代表真实模型执行百分比。后端提供它，是为了让前端不散落一堆阶段映射硬编码。

### 6.2 新增任务列表响应 DTO

计划新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/dto/GenerationJobListResponse.java
```

用途：

```text
包装最近任务列表
返回 sessionId
返回 count
返回 jobs
后续可以追加 hasActiveJob / latestPlanId 等摘要
```

预计接口变化：

```text
GET /api/v1/jobs?sessionId=xxx&limit=5
```

从直接返回：

```text
List<GenerationJobResponse>
```

改为返回：

```text
GenerationJobListResponse
```

如果担心前端兼容，也可以保留旧结构，但当前项目还在开发期，推荐直接改成包装 DTO，让接口语义更清晰。

### 6.3 GenerationJobController 增强

计划修改：

```text
AgentProjectTrip/src/main/java/com/travel/agent/web/GenerationJobController.java
```

新增或调整：

```text
listJobs(...)
返回 GenerationJobListResponse

getJob(...)
继续返回 GenerationJobResponse
```

Controller 注释需要说明：

```text
GET /api/v1/jobs/{jobId} 用于轮询单个任务
GET /api/v1/jobs 用于刷新页面后恢复最近任务
两个接口都不触发 Graph，不扣费
```

### 6.4 任务阶段展示规则集中

推荐把阶段到展示信息的映射放在后端 DTO 里，优先不新增类。

如果 `GenerationJobResponse` 变得太重，再考虑新增：

```text
GenerationJobPresentation
```

第一版建议不新增，先保持简单。

### 6.5 Store 暂不大改

当前已有：

```text
GenerationJobStore.findRecentBySessionId(sessionId, limit)
```

本阶段先复用它。  
如果测试发现排序不稳定，再修复：

```text
InMemoryGenerationJobStore
JdbcGenerationJobStore
```

要求：

```text
最近任务按 createdAt 或 updatedAt 倒序
limit 做安全限制
返回同一个 sessionId 下的任务
```

## 7. 前端计划

### 7.1 页面结构增强

修改：

```text
AgentProjectTrip/src/main/resources/static/travel-planner.html
```

计划在“生成进度”区域增加：

```text
任务摘要行：jobId 简写、运行时长、更新时间
阶段时间线：创建任务、校验需求、扣费、核心生成、保存方案、完成
失败建议区：失败时展示下一步按钮和提示
最近任务区：展示最近 3-5 个任务
```

保持原则：

```text
正式页面不要变成开发日志页面
用户看到的是清楚的状态和下一步动作
完整 JSON 继续放在默认折叠的调试面板里
```

### 7.2 agent-app.js 状态增强

修改：

```text
AgentProjectTrip/src/main/resources/static/agent-app.js
```

计划新增状态字段：

```text
jobDetail
recentJobs
jobStartedAt
jobUpdatedAt
jobFinishedAt
jobErrorMessage
jobActionHint
```

用途：

```text
jobDetail：保存最近一次任务详情响应
recentJobs：保存 sessionId 下最近任务
jobStartedAt / jobUpdatedAt / jobFinishedAt：用于显示运行时长和更新时间
jobErrorMessage：失败时用户区展示
jobActionHint：后端给出的下一步建议
```

### 7.3 任务列表加载

计划新增方法：

```text
loadRecentJobs(showErrors)
applyJobResponse(data)
applyJobListResponse(data)
selectRecentJob(jobId)
recoverLatestJob()
```

职责：

```text
loadRecentJobs(showErrors)
调用 GET /api/v1/jobs?sessionId=xxx&limit=5，页面打开和点击刷新时使用。

applyJobResponse(data)
统一处理单个任务响应，避免 generatePlanAsync 和 loadJobStatus 各自写状态。

applyJobListResponse(data)
保存最近任务列表，寻找可恢复任务。

selectRecentJob(jobId)
用户从最近任务列表中选择某个任务，恢复对应 jobId。

recoverLatestJob()
页面初始化时自动寻找最近运行中任务或最近成功任务。
```

### 7.4 轮询逻辑增强

当前轮询：

```text
每 2500ms 调一次 GET /api/v1/jobs/{jobId}
终态停止
成功后 loadPlan(false)
```

本阶段计划增强：

```text
轮询前先检查 jobId 是否存在
终态后刷新最近任务列表
任务成功后只自动 loadPlan 一次，避免重复闪烁
任务失败后显示失败建议，允许用户返回需求表
如果连续请求失败，不立刻清空任务，展示网络错误并允许手动刷新
```

### 7.5 进度展示增强

计划修改：

```text
renderJobProgress()
```

新增或增强：

```text
renderJobTimeline()
renderRecentJobs()
renderJobFailureAdvice()
formatJobTime(...)
formatDuration(...)
jobStatusClass(...)
```

展示内容：

```text
状态：生成中 / 已完成 / 失败 / 已取消
阶段：中文标题
说明：当前系统正在做什么
进度条：使用 progressPercent
运行时长：例如“已运行 42 秒”
任务更新时间：例如“刚刚更新”
失败建议：例如“请回到需求表补充时间或预算后重试”
最近任务：显示 jobId、状态、阶段、planId
```

### 7.6 按钮状态增强

本阶段需要明确按钮状态：

```text
生成中：
禁用“生成完整方案”
允许“刷新任务”
允许查看最近任务
禁用会破坏当前任务的重复生成动作

成功后：
允许“重新加载方案”
允许“复制”
允许“继续修改方案”

失败后：
允许“继续编辑需求”
允许“重新生成”
允许“刷新任务”
```

### 7.7 样式增强

修改：

```text
AgentProjectTrip/src/main/resources/static/app.css
```

计划新增：

```text
job-summary
job-meta
job-timeline
job-step
job-step.done
job-step.active
job-step.failed
job-advice
recent-jobs
recent-job
recent-job.active
```

样式原则：

```text
进度区域紧凑，不做夸张大屏动画
状态颜色克制，成功绿色、失败红色、运行中蓝色或中性色
移动端不溢出，任务列表可换行
```

## 8. 修改文件清单

预计修改：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/dto/GenerationJobResponse.java
AgentProjectTrip/src/main/java/com/travel/agent/web/GenerationJobController.java
AgentProjectTrip/src/main/resources/static/travel-planner.html
AgentProjectTrip/src/main/resources/static/agent-app.js
AgentProjectTrip/src/main/resources/static/app.css
阶段9-17开发规划与维护清单.md
第11阶段开发.md
```

可能修改：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/store/GenerationJobStore.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/store/InMemoryGenerationJobStore.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/store/JdbcGenerationJobStore.java
```

预计新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/dto/GenerationJobListResponse.java
```

预计不删除任何类。

## 9. 预计新增方法

### 9.1 后端新增方法

`GenerationJobResponse`：

```text
getStageLabel()
setStageLabel(...)
getStageDescription()
setStageDescription(...)
getStatusLabel()
setStatusLabel(...)
getProgressPercent()
setProgressPercent(...)
getActionHint()
setActionHint(...)
getDurationSeconds()
setDurationSeconds(...)
isTerminal()
setTerminal(...)
isRecoverable()
setRecoverable(...)
```

也可能不暴露所有 setter，而是在 `from(...)` 里统一赋值。

`GenerationJobListResponse`：

```text
of(sessionId, jobs)
getSessionId()
getCount()
getJobs()
getHasActiveJob()
getLatestJobId()
getLatestPlanId()
```

### 9.2 前端新增方法

`agent-app.js`：

```text
loadRecentJobs(showErrors)
applyJobResponse(data)
applyJobListResponse(data)
selectRecentJob(jobId)
recoverLatestJob()
renderJobTimeline()
renderRecentJobs()
renderJobFailureAdvice()
formatJobTime(value)
formatDuration(seconds)
jobStatusClass(status)
jobTerminal(status)
```

## 10. 业务规则

### 10.1 任务恢复规则

页面初始化时：

```text
如果 localStorage 里有 PENDING / RUNNING jobId：
    继续轮询该任务
否则：
    查询最近任务列表
    如果存在 PENDING / RUNNING：
        自动恢复最新运行中任务并开始轮询
    如果存在 SUCCEEDED 且有 planId：
        显示可恢复方案入口，不强制覆盖当前页面
```

### 10.2 任务终态规则

```text
SUCCEEDED：
停止轮询，自动加载 plan，保留 jobDetail。

FAILED：
停止轮询，展示 errorMessage 和 actionHint，保留 requirementId。

CANCELLED：
停止轮询，提示任务已取消。本阶段不新增取消按钮，只兼容状态展示。
```

### 10.3 重试规则

第 11 阶段先不新增独立“重试任务”后端接口。  
失败后用户可以：

```text
点击“继续编辑需求”
保存并重新确认需求
再次点击“生成完整方案”
```

如果当前 requirementId 没有运行中任务，`AsyncPlanGenerationService.createJob(...)` 会创建新任务。  
如果已有运行中任务，则继续返回旧任务，避免重复扣费。

## 11. 接口设计

### 11.1 查询单个任务

请求：

```http
GET /api/v1/jobs/{jobId}
```

响应字段计划：

```json
{
  "jobId": "job-xxx",
  "sessionId": "test-001",
  "requirementId": "req-xxx",
  "planId": "plan-xxx",
  "status": "RUNNING",
  "statusLabel": "生成中",
  "currentStage": "RUNNING_GRAPH",
  "stageLabel": "执行核心规划",
  "stageDescription": "正在调用核心 Agent 工作流生成方案。",
  "progressPercent": 70,
  "actionHint": "请稍等，生成完成后会自动展示方案。",
  "terminal": false,
  "recoverable": false,
  "durationSeconds": 42,
  "errorMessage": null,
  "assistantMessage": "旅行方案生成任务正在执行，请稍等。"
}
```

### 11.2 查询最近任务

请求：

```http
GET /api/v1/jobs?sessionId=test-001&limit=5
```

响应字段计划：

```json
{
  "sessionId": "test-001",
  "count": 2,
  "hasActiveJob": true,
  "latestJobId": "job-xxx",
  "latestPlanId": "plan-xxx",
  "jobs": []
}
```

## 12. 前端页面草图

生成进度区域大致结构：

```text
生成进度

[状态徽标] 生成中
任务：job-5158...    已运行：42 秒    更新时间：刚刚

当前阶段：执行核心规划
正在调用核心 Agent 工作流生成方案，可能需要几十秒。

[==============      ] 70%

时间线：
✓ 创建任务
✓ 检查需求
✓ 扣除额度
● 执行核心规划
○ 保存方案
○ 完成

最近任务：
job-xxx  生成中  执行核心规划
job-yyy  已完成  plan-yyy
```

失败状态大致结构：

```text
生成失败

失败原因：
完整规划生成失败，外部工具暂时不可用。

建议：
可以稍后重试。如果需求本身不完整，请回到需求表补充后重新生成。

[继续编辑需求] [刷新任务] [重新生成]
```

## 13. 注释规范要求

本阶段新增或修改注释必须遵守：

```text
C:\Users\DJI\Desktop\agent spring ai\注释规范.md
```

重点要求：

```text
GenerationJobListResponse 必须写清楚它是列表响应 DTO，不执行查询。
GenerationJobResponse 新增展示字段时，必须说明这些字段是前端展示派生字段，不是核心状态源。
GenerationJobController 必须说明两个 GET 接口都只读、不扣费、不触发 Graph。
agent-app.js 新增的恢复和轮询函数必须说明状态来源、停止条件、失败策略。
HTML / CSS 不需要写大量注释，但新增结构的 id 必须和 JS 绑定一致。
```

## 14. 测试计划

### 14.1 前端静态检查

```powershell
node --check AgentProjectTrip\src\main\resources\static\agent-app.js
```

### 14.2 HTML / JS id 绑定检查

用脚本检查：

```text
agent-app.js bindElements 中声明的 id
必须全部存在于 travel-planner.html
```

### 14.3 Java 测试

如果修改 Java：

```powershell
cd AgentProjectTrip
mvn test
```

### 14.4 静态资源同步

如果用户正在运行的是已编译资源，需要执行：

```powershell
cd AgentProjectTrip
mvn process-resources
```

### 14.5 手动测试

浏览器访问：

```text
http://localhost:8080/travel-planner.html
```

推荐测试内容：

```text
国庆去法国和意大利玩10天，预算1200欧，不含国际机票，想避开人多的地方
```

手动测试重点：

```text
1. 自动整理需求表。
2. 保存并确认需求。
3. 点击生成完整方案。
4. 观察任务进度是否显示阶段解释和进度条。
5. 刷新页面，看是否能恢复任务或最近任务列表。
6. 任务成功后是否自动展示旅行方案。
7. 最近任务列表是否显示最新任务。
8. 若任务失败，是否显示失败原因和可操作建议。
9. 开发者 JSON 面板仍默认折叠，展开后能看到原始响应。
```

## 15. 验收标准

第 11 阶段完成时必须满足：

```text
点击生成后立即拿到 jobId，并显示任务摘要。
页面显示当前阶段的中文解释，而不只是枚举名。
页面显示稳定的进度条和阶段时间线。
刷新页面后可以恢复运行中任务或最近成功任务。
任务成功后自动展示 plan。
任务失败后展示失败原因和下一步建议。
最近任务列表可以显示 sessionId 下的最近任务。
运行中不会重复创建任务或重复扣费。
JSON 调试面板保留，且默认折叠。
```

## 16. 开发步骤

### Step 1：后端 DTO 增强

```text
修改 GenerationJobResponse
新增展示字段
集中编写 stage/status 到展示文案和 progressPercent 的映射
新增 GenerationJobListResponse
```

### Step 2：Controller 列表接口增强

```text
修改 GenerationJobController.listJobs(...)
返回 GenerationJobListResponse
保留 getJob(...) 单任务查询
补充注释
```

### Step 3：前端 HTML 结构增强

```text
修改 travel-planner.html
在生成进度区增加任务摘要、时间线、失败建议、最近任务容器
更新静态资源版本号
```

### Step 4：前端 JS 状态和轮询增强

```text
修改 agent-app.js
新增 jobDetail / recentJobs 等状态
统一 applyJobResponse
新增 loadRecentJobs
新增恢复最近任务逻辑
增强 renderJobProgress
新增 renderJobTimeline / renderRecentJobs / renderJobFailureAdvice
```

### Step 5：前端 CSS 样式增强

```text
修改 app.css
增加任务摘要、时间线、最近任务、失败建议样式
检查移动端不溢出
```

### Step 6：测试和文档同步

```text
执行 node --check
执行 HTML / JS id 绑定检查
执行 mvn test
执行 mvn process-resources
更新第11阶段开发.md 的当前开发记录
用户手动测试通过后，新增 阶段 11 异步生成进度和结果页完善.md
更新阶段9-17开发规划与维护清单.md
```

## 17. 完成后的归档要求

用户手动测试通过后，需要新建：

```text
阶段 11 异步生成进度和结果页完善.md
```

归档文档必须记录：

```text
本阶段核心思想
本阶段解决的问题
新增功能
修改的业务逻辑
修改的前端内容
修改的后端接口
修改的代码文件
新增的类
删除或废弃的类
新增的方法
删除或废弃的方法
关键代码细节和用途
注释规范执行情况
测试方式和测试结果
遗留问题
下一阶段衔接点
```

同时更新：

```text
阶段9-17开发规划与维护清单.md
```

把阶段 11 标记为：

```text
已完成
```

并写入完成日期和归档文档路径。

## 18. 人工审核点

开始写代码前，需要用户确认以下方向：

```text
1. 是否同意 GET /api/v1/jobs 列表接口从 List<GenerationJobResponse> 改成 GenerationJobListResponse。
2. 是否同意后端提供 progressPercent / stageLabel / actionHint，前端只负责展示。
3. 是否同意本阶段新增“最近任务列表”和“刷新页面恢复任务”。
4. 是否同意本阶段不新增取消任务接口，只做 CANCELLED 状态兼容展示。
```

如果以上方向通过，再开始第 11 阶段代码实现。
