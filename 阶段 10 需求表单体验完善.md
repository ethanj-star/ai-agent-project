# 阶段 10 需求表单体验完善

完成日期：2026-06-14

## 1. 阶段结论

第 10 阶段已完成。

本阶段把第 9 阶段已经跑通的正式前端页面，进一步升级为“生成前需求质量门控”。

现在用户可以：

```text
自然语言输入 -> 自动整理需求表 -> 手动修正 -> 保存 -> 确认 -> 生成完整方案
```

也可以：

```text
不输入自然语言 -> 直接填写需求表 -> 保存创建草稿 -> 确认 -> 生成完整方案
```

## 2. 本阶段核心思想

本阶段没有继续扩展 Agent 分支，也没有迁移 LangGraph4j。

本阶段重点解决的是：

```text
在进入高成本 Graph / Planner 之前，先保证用户需求表足够清晰。
```

原因：

```text
需求表质量越高，后续 Planner、RiskReasoning 和分支工具越不容易跑偏。
```

## 3. 本阶段解决的问题

第 9 阶段页面已经能完整跑通，但仍有这些问题：

```text
用户必须先输入自然语言，不能直接填表创建需求
特殊要求没有独立字段，只能塞进偏好或避开项
预算是否包含国际机票原来会阻塞确认，产品体验偏硬
用户修改表单后，如果忘记保存，可能以为后端会用新值生成
字段缺失只在顶部整体提示，不够直观
确认按钮禁用时，用户不容易知道原因
```

第 10 阶段已解决：

```text
新增手动填表创建需求草稿接口
新增 specialNotes 特殊要求字段
预算金额和币种继续作为硬阻塞
出发地缺失改为 warning
预算是否包含国际机票为空改为 warning
前端新增未保存修改保护
前端新增字段级缺失 / warning 提示
前端新增确认原因提示
```

## 4. 产品策略确认

用户已确认以下策略，并已按此实现：

```text
新增 POST /api/v1/requirements：同意
新增 specialNotes：同意
预算金额缺失：blocking
出发地缺失：warning
预算是否包含国际机票为空：warning
```

## 5. 新增功能

### 5.1 手动创建需求草稿

新增接口：

```text
POST /api/v1/requirements
```

用途：

```text
用户不输入自然语言时，可以直接填写表单并保存为一张需求草稿。
```

特点：

```text
不调用 LLM
不扣费
不进入 Graph
只补齐 requirementId / sessionId / status
复用 RequirementValidationNode 校验
复用 RequirementStore 保存
```

### 5.2 specialNotes 特殊要求字段

新增字段：

```text
TravelRequirementSpec.specialNotes
```

用途：

```text
保存无法归入固定字段的要求。
```

例子：

```text
带老人
带孩子
不想开车
少走路
每天不要太累
需要无障碍
```

该字段已进入：

```text
需求抽取
规则兜底
前端表单
需求保存
Graph 初始化关键词
Planner prompt
需求变更复制逻辑
```

### 5.3 校验策略调整

硬阻塞：

```text
目的地缺失或过宽
旅行天数缺失或无效
预算金额缺失或无效
预算币种缺失
```

非阻塞 warning：

```text
出发地缺失
出行时间缺失
旅行人数缺失
预算是否包含国际机票未确认
预算明显偏紧
目的地数量和天数可能不匹配
```

### 5.4 前端字段分组

需求表被分为：

```text
基础信息
预算信息
旅行偏好
```

新增字段：

```text
特殊要求 specialNotesInput
```

### 5.5 未保存修改保护

新增状态：

```text
requirementDirty
```

含义：

```text
用户修改了表单，但还没有保存。
```

效果：

```text
未保存时不能确认需求
页面显示“表单有未保存修改，请先保存”
保存成功后 requirementDirty=false
```

### 5.6 字段级提示

新增字段提示容器：

```text
data-field-hint
```

显示效果：

```text
必填缺失：红色提示
建议补充：黄色提示
```

## 6. 修改文件清单

### 6.1 后端文件

```text
AgentProjectTrip/src/main/java/com/travel/agent/web/RequirementController.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelRequirementSpec.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/RequirementExtractionAgent.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/RequirementValidationNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/InitStateNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/PlanDraftNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/RequirementPatchNode.java
```

### 6.2 前端文件

```text
AgentProjectTrip/src/main/resources/static/travel-planner.html
AgentProjectTrip/src/main/resources/static/agent-app.js
AgentProjectTrip/src/main/resources/static/app.css
```

### 6.3 测试文件

```text
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/node/RequirementValidationNodeTest.java
```

### 6.4 文档文件

```text
第10阶段开发.md
阶段9-17开发规划与维护清单.md
阶段 10 需求表单体验完善.md
```

## 7. 新增 Java 类

本阶段没有新增 Java 类。

## 8. 删除 Java 类

本阶段没有删除 Java 类。

## 9. 新增和修改的方法

### 9.1 RequirementController.createManualDraft(...)

新增。

接口：

```text
POST /api/v1/requirements
```

用途：

```text
创建手动填写的需求草稿。
```

### 9.2 RequirementController.prepareManualDraft(...)

新增私有方法。

用途：

```text
补齐手动需求草稿的 requirementId、originalMessage 和 DRAFT 状态。
```

### 9.3 TravelRequirementSpec.getSpecialNotes / setSpecialNotes

新增。

用途：

```text
读写特殊要求。
setter 会把空字符串和纯空白转换为 null。
```

### 9.4 RequirementExtractionAgent.extractSpecialNotes(...)

新增。

用途：

```text
规则兜底抽取带老人、带孩子、少走路、不想开车、不要太累等特殊要求。
```

### 9.5 RequirementValidationNode.validateFlightBudgetBoundary(...)

修改。

变化：

```text
从 blocking 改为 warning。
```

### 9.6 InitStateNode.buildRequirementKeywords(...)

修改。

变化：

```text
把 specialNotes 写入关键词，兼容后续分支派发和旧逻辑。
```

### 9.7 PlanDraftNode.formatRequirementSpec(...)

修改。

变化：

```text
把 specialNotes 注入 Planner prompt。
```

### 9.8 RequirementPatchNode.copy(...)

修改。

变化：

```text
复制需求表时保留 specialNotes。
```

## 10. 前端新增和修改函数

### 10.1 saveRequirement()

修改。

变化：

```text
没有 requirementId 时 POST /api/v1/requirements。
已有 requirementId 时 PUT /api/v1/requirements/{requirementId}。
```

### 10.2 confirmRequirement()

修改。

变化：

```text
如果 requirementDirty=true，则阻止确认并提示先保存。
```

### 10.3 fillRequirementForm()

修改。

变化：

```text
回填 specialNotesInput。
```

### 10.4 readSpecFromForm()

修改。

变化：

```text
读取 specialNotes。
预算币种统一转大写。
```

### 10.5 renderFieldHints()

新增。

用途：

```text
根据 missingFields 和 warnings 渲染字段级提示。
```

### 10.6 renderConfirmReason()

新增。

用途：

```text
解释确认按钮为什么可用或不可用。
```

### 10.7 markRequirementDirty()

新增。

用途：

```text
监听表单 input/change，标记未保存修改。
```

### 10.8 validateFormLocally()

新增。

用途：

```text
做前端轻量校验。
后端仍然是最终门控。
```

## 11. 测试结果

已通过：

```text
node --check AgentProjectTrip/src/main/resources/static/agent-app.js
```

已通过：

```text
HTML / JS id 绑定检查
绑定的 49 个 id 全部存在
progressBox 是纯样式容器，不需要绑定
```

已通过：

```text
mvn test
Tests run: 103, Failures: 0, Errors: 0, Skipped: 0
```

人工测试：

```text
用户已确认测试通过
```

## 12. 手动验收内容

已验证：

```text
直接手动填表可以创建需求草稿
预算缺失会阻塞确认
出发地缺失只 warning
预算是否包含国际机票为空只 warning
未保存修改会阻止确认
特殊要求可保存并进入生成上下文
```

## 13. 遗留问题

第 10 阶段完成后仍然遗留：

```text
字段级 warning 仍是轻量文本匹配，不是完整结构化 warning 映射
偏好和避开项仍是逗号输入，还没有做标签组件
specialNotes 还不是结构化旅客画像
没有正式用户账户系统
没有正式支付系统
航班分支仍未真正接入 Graph 分支执行链路
BranchDispatch 仍未改成模型驱动
```

## 14. 下一阶段衔接

下一阶段建议进入：

```text
阶段 11：异步生成进度和结果页完善
```

原因：

```text
现在正式页面和需求表单门控都已经可用。
下一步应继续优化生成任务、失败恢复、结果展示和历史任务体验。
```

