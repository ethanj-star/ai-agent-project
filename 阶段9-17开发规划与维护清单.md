# 阶段9-17开发规划与维护清单

更新时间：2026-06-21

本文档用于维护第 9 阶段到第 17 阶段的后续开发计划。前 8 阶段已经归档，后续所有阶段都以本文档为总控入口。

## 0. 维护规则

本文档由 Codex 持续维护。每开始、推进、完成一个阶段，都要同步更新本文档。

阶段状态统一使用：

```text
未开始
进行中
已完成
暂停
废弃
```

每个阶段完成后，必须做两件事：

```text
1. 在本文档的阶段总览表里把状态改为“已完成”，写入完成日期和阶段归档文档路径。
2. 新建一个阶段归档文档，例如：
   阶段 9 正式前端 MVP 页面.md
```

每个阶段归档文档必须完整记录：

```text
本阶段核心思想
本阶段解决了什么问题
新增了哪些功能
修改了哪些业务逻辑
修改了哪些前端内容
修改了哪些后端接口
修改了哪些代码文件
新增了哪些类
删除了哪些类
新增了哪些方法
删除或废弃了哪些方法
每个关键代码的用途
测试方式和测试结果
仍然遗留的问题
下一阶段衔接点
```

代码注释必须遵守：

```text
C:\Users\DJI\Desktop\agent spring ai\注释规范.md
```

每次新增或修改 Java / HTML / JavaScript 代码时，都要检查：

```text
类级 Javadoc 是否说明系统位置、职责和边界
公开方法是否说明输入、输出、流程和失败策略
Graph / Agent 代码是否说明读写哪些状态字段
Controller 是否说明 HTTP 语义和状态码含义
Service 是否说明业务事务、失败回滚和异步边界
前端异步流程是否说明状态映射、轮询和错误处理
```

## 1. 阶段总览

| 阶段 | 名称 | 状态 | 目标一句话 | 完成日期 | 阶段归档文档 |
|---|---|---|---|---|---|
| 9 | 正式前端 MVP 页面 | 已完成 | 把开发调试台升级成用户能完整体验的正式页面 | 2026-06-14 | 阶段 9 正式前端 MVP 页面.md |
| 10 | 需求表单体验完善 | 已完成 | 把结构化需求表做成产品核心，降低无效输入和 token 浪费 | 2026-06-14 | 阶段 10 需求表单体验完善.md |
| 11 | 异步生成进度和结果页完善 | 已完成 | 让用户清楚看到生成进度、失败原因和最终结果 | 2026-06-14 | 阶段 11 异步生成进度和结果页完善.md |
| 12 | 航班与酒店真实分支接入 | 已完成 | 把已有 FlightTools 与酒店查询工具真正接入 Graph 分支执行链路 | 2026-06-14 | 阶段 12 航班与酒店真实分支接入.md |
| 13 | 模型驱动 BranchDispatch | 已完成 | 让核心模型判断该调用哪些分支任务，Java 负责安全兜底 | 2026-06-14 | 阶段 13 模型驱动 BranchDispatch.md |
| 14 | RAG 知识库运营与 MediaCrawler 采集 Agent | 未开始 | 用少量定期采集和景点主数据维护 Pinecone 旅行知识库 | - | - |
| 15 | 标准 LangGraph4j 状态图迁移 | 未开始 | 把当前手写 Graph 迁移为边界明确的标准 LangGraph4j Agent 状态图 | - | - |
| 16 | 联网事实核查与输出前强校验 | 未开始 | 接入 OpenAI 联网搜索或自研 OnlineFactCheckAgent，对最终方案做事实核查和风险重排 | - | - |
| 17 | Agent Eval 自动评估体系 | 未开始 | 参考 OpenAI Agent Eval 思路建立旅行规划测试集、评分指标和回归评估流程 | - | - |

## 2. 总体开发原则

后续阶段按“产品闭环优先，Agent 智能逐步增强”的顺序推进。

不要一开始就追求最复杂的 LangGraph4j 架构。当前最重要的是先让用户能完整使用：

```text
输入需求
 -> 自动整理表单
 -> 用户确认
 -> 异步生成
 -> 显示进度
 -> 展示方案
 -> 自然语言修改
 -> 保存版本
```

RAG 知识库运营、标准 LangGraph4j 迁移、联网事实核查和 Agent Eval，都要建立在这个产品闭环稳定的基础上。

## 2.1 未真实工具接入 TODO 清单

下面这些能力目前还没有真实工具或真实数据源接入。它们可以被模型或 RAG 文本提及，但不能被系统当成已经验证过的实时事实使用。后续阶段开发时，需要根据产品优先级逐步拆成独立工具、分支 Agent 或风险审查能力。

```text
火车 / 城际交通 / 地铁换乘：
目前主要依赖模型常识和 RAG，没有接入真实交通 API、路线 API 或铁路票价/班次服务。

签证：
目前没有签证规则服务或工具，只有模型/RAG文本判断。后续需要明确数据来源、更新时间和免责声明边界。

预算精算：
目前没有真实价格聚合器，预算主要来自需求字段、工具片段和模型估算。后续需要按住宿、机票、城际交通、餐饮、门票、备用金拆分。

餐厅 / 美食：
目前没有餐厅 API 或真实店铺推荐工具。后续可考虑 SerpApi Local、Google Places、OpenTable、TheFork 或本地静态知识库。

门票 / 预约 / 开放时间：
RiskIssueType 里已经有 OPERATING_HOURS，但还没有真实开放时间、闭馆日、门票预约或排队数据工具。

未来天气预报：
现在 WeatherTools 使用的是 current weather，只适合当前天气查询，不适合未来日期行程判断。后续需要接入 forecast 或历史气候均值。

实时安全提醒 / 罢工 / 灾害 / 活动人流：
目前没有真实数据源。后续可按安全提醒、交通罢工、大型活动、人流风险分别设计工具。
```

## 3. 阶段 9：正式前端 MVP 页面

状态：已完成

阶段归档文档：

```text
阶段 9 正式前端 MVP 页面.md
```

### 核心思想

把当前偏开发调试性质的 `agent-lab.html` 升级为一个用户能完整操作的正式 MVP 页面。

现在的问题是：后端功能已经很多，但用户还需要理解很多开发字段，例如 `requirementId`、`jobId`、`planId`。阶段 9 要把这些技术细节藏起来，让用户像使用普通网站一样完成一次旅行规划。

### 解决的问题

```text
用户不需要看后端日志也能测试系统
用户不需要手动理解 jobId / planId
页面能引导用户从需求输入走到最终方案
页面能展示生成中、成功、失败、可修改等状态
```

### 主要功能

```text
正式首页 / 需求输入区
需求表单区
确认需求区
生成进度区
旅行方案展示区
自然语言修改区
版本历史区
记忆偏好区
错误提示和空状态
```

### 预计修改文件

```text
AgentProjectTrip/src/main/resources/static/agent-lab.html
```

可能新增：

```text
AgentProjectTrip/src/main/resources/static/app.css
AgentProjectTrip/src/main/resources/static/agent-app.js
```

如果继续单文件实现，则不新增 CSS / JS 文件，但必须在阶段归档文档中说明原因。

### 验收标准

```text
打开页面后，用户可以从一句自然语言开始，完整生成一份旅行计划。
生成过程中页面不会假死。
生成成功后自动展示方案。
生成失败时页面展示用户可理解的错误。
用户可以继续输入修改意见并得到新版本。
```

## 4. 阶段 10：需求表单体验完善

状态：已完成

阶段归档文档：

```text
阶段 10 需求表单体验完善.md
```

### 核心思想

把结构化需求表变成产品核心。用户可以先自然语言输入，系统自动填表；用户也可以直接手动填写表单。

这一步是为了节省 token，也为了避免用户一句话含糊不清导致 Graph 生成无效方案。

### 解决的问题

```text
用户自然语言表达不稳定
模型直接规划容易误解时间、预算、人数、目的地
每次无效生成都会浪费 token 和额度
正式产品需要在生成前确认关键字段
```

### 主要功能

```text
目的地字段优化
出发地字段优化
出行时间字段优化
旅行天数字段优化
人数和人群字段
预算金额和币种
预算是否包含国际机票
住宿偏好
交通偏好
旅行风格
想体验的内容
想避开的内容
特殊要求
缺失项提示
警告项提示
确认后锁定表单
重新编辑需求表
```

### 预计修改文件

```text
AgentProjectTrip/src/main/resources/static/agent-lab.html
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelRequirementSpec.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/RequirementExtractionAgent.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/RequirementValidationNode.java
AgentProjectTrip/src/main/java/com/travel/agent/web/RequirementController.java
```

可能新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelerProfile.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BudgetPreference.java
```

是否新增取决于字段复杂度。如果字段仍能清晰放在 `TravelRequirementSpec` 中，就不提前拆类。

### 验收标准

```text
用户输入一句自然语言后，页面能自动填出大部分表单。
缺少目的地、时间、天数、预算等关键字段时，页面明确提示。
用户确认前不会扣费。
用户确认后才能触发完整生成。
```

## 5. 阶段 11：异步生成进度和结果页完善

状态：已完成

阶段归档文档：

```text
阶段 11 异步生成进度和结果页完善.md
```

### 核心思想

把 `GenerationJob` 从后端状态对象变成用户能理解的进度体验。

现在后端已经有异步任务，但前端需要更清楚地展示“正在做什么”和“失败在哪里”。

### 解决的问题

```text
大模型生成时间较长，用户容易以为页面卡住
刷新页面后需要找回当前任务
任务失败时需要展示具体原因
用户需要知道什么时候可以查看 plan
```

### 主要功能

```text
生成任务创建成功提示
任务阶段文案映射
轮询任务状态
刷新页面恢复最近任务
失败原因展示
成功后自动加载 plan
生成按钮防重复点击
任务运行中禁止重复扣费
```

### 预计修改文件

```text
AgentProjectTrip/src/main/resources/static/agent-lab.html
AgentProjectTrip/src/main/java/com/travel/agent/web/GenerationJobController.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/dto/GenerationJobResponse.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/generation/AsyncPlanGenerationService.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/GenerationJobStage.java
```

可能新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/dto/GenerationJobListResponse.java
```

### 验收标准

```text
点击生成后立即拿到 jobId。
页面显示当前阶段。
任务成功后自动展示 plan。
任务失败后展示原因并允许用户返回编辑需求。
刷新页面后可以通过 sessionId 找回最近任务。
```

## 6. 阶段 12：航班与酒店真实分支接入

状态：已完成

阶段归档文档：

```text
阶段 12 航班与酒店真实分支接入.md
```

阶段开发过程文档：

```text
第12阶段开发.md
```

### 核心思想

把已经存在的 `FlightTools`、`FlightServiceImpl.searchFlights(...)`、`PlacesTools.searchHotels(...)` 和 `PlacesServiceImpl.searchHotels(...)` 真正接入 Graph 分支任务链路。

现在系统有航班和酒店查询底层代码，但它们还没有稳定进入正式 Graph 工作流：

```text
FLIGHT 分支已经有枚举和派发可能，但 BranchAgentFacade 遇到 FLIGHT 时仍然返回 fallback。
酒店查询已有 PlacesTools.searchHotels(...)，但 BranchTaskType 没有 HOTEL，BranchDispatchNode 也不会派发酒店任务。
```

阶段 12 要把这两个“底层已存在但 Graph 未真接入”的能力补成正式分支，让 Planner 可以拿到真实查询结果或明确失败原因。

### 解决的问题

```text
Graph 可以派发 FLIGHT 任务，但不能真的查航班
用户明确问航班时，系统只能返回占位提示
旅行规划无法引用真实航班参考
酒店底层工具存在，但 Graph 不会派发 HOTEL 任务
住宿预算和酒店建议主要靠模型估算，缺少真实酒店价格参考
```

### 主要功能

```text
新增 executeFlight(...) 分支执行逻辑
从 BranchTask 中解析 origin / destination / date
中文城市和国家映射为机场或城市参数
缺少出发地或日期时返回可解释失败，不伪造航班
把航班结果整理成 BranchResult
新增 HOTEL 分支类型
新增酒店分支派发规则
新增 executeHotel(...) 分支执行逻辑
从需求表解析目的地、入住日期、退房日期或停留天数
缺少明确日期时返回可解释失败或降级为住宿预算提醒
把酒店结果整理成 BranchResult
Planner prompt 中注入航班参考
Planner prompt 中注入酒店参考
```

### 预计修改文件

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/BranchAgentFacade.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/tools/FlightTools.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/tools/PlacesTools.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/FlightService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/FlightServiceImpl.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/PlacesService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/PlacesServiceImpl.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTask.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTaskType.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/BranchDispatchNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/PlanDraftNode.java
```

可能新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/FlightSearchRequest.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/HotelSearchRequest.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/FlightSearchParamResolver.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/HotelSearchParamResolver.java
```

### 验收标准

```text
用户给出出发地、目的地和日期时，FLIGHT 分支能真实调用 FlightTools。
缺少日期或出发地时，FLIGHT 分支明确返回不可查询原因。
Planner 不再伪造具体航班号和价格。
航班 API 失败不会中断整个 Graph。
用户给出目的地、日期和住宿需求时，HOTEL 分支能真实调用 PlacesTools.searchHotels。
缺少入住 / 退房日期时，HOTEL 分支明确返回不可查询原因或住宿预算降级提示。
Planner 能引用真实酒店价格范围、评分和住宿提醒。
酒店 API 失败不会中断整个 Graph。
```

## 7. 阶段 13：模型驱动 BranchDispatch

状态：已完成

阶段归档文档：

```text
阶段 13 模型驱动 BranchDispatch.md
```

阶段开发过程文档：

```text
第13阶段开发.md
```

### 核心思想

把当前 Java 硬规则派发升级为“模型建议 + Java 守卫”的任务派发机制。

成熟 Agent 不应该只靠写死规则判断是否查天气、景点、航班。核心模型应该根据上下文判断需要哪些分支任务，但 Java 必须控制边界，防止模型乱调用工具。

### 解决的问题

```text
BranchDispatchNode 当前主要依赖 Java 硬规则
复杂需求下无法灵活判断工具调用
模型无法主动规划下一步需要什么信息
完全交给模型又容易乱派发、超预算、调用不存在工具
```

### 主要功能

```text
新增模型驱动任务派发节点
DeepSeek Pro 输出结构化 BranchTask 建议
Java Guard 校验任务类型、数量、参数和预算
非法任务降级或丢弃
保留原 BranchDispatchNode 作为兜底
任务派发原因写入 BranchTask
```

### 预计修改文件

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/LangGraphPlannerFacade.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/BranchDispatchNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTask.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTaskType.java
```

可能新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/ModelBranchDispatchNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/BranchDispatchGuardNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchDispatchDecision.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchDispatchPolicy.java
```

### 验收标准

```text
模型可以根据用户需求建议 WEATHER / PLACES / FLIGHT / KNOWLEDGE 任务。
Java Guard 限制最大任务数和允许类型。
模型输出非法 JSON 时自动回退到旧 BranchDispatchNode。
单元测试覆盖合法任务、非法任务、超数量任务、模型失败 fallback。
```

### 完成记录

```text
已新增 ModelBranchDispatchNode，让 DeepSeek Pro 输出结构化分支任务建议。
已新增 BranchDispatchGuardNode，用 Java 校验模型建议并兜底旧 BranchDispatchNode。
已新增 BranchDispatchDecision / BranchTaskSuggestion / BranchDispatchIssue / BranchDispatchPolicy。
已把派发记录写入 TravelPlanState，并注入 PlanDraftNode，供 Planner 判断工具可用边界。
已修复前端生成进度本地计时和失败步骤误标绿的问题。
mvn test 通过。
node --check agent-app.js 通过。
mvn process-resources 通过。
正式前端人工测试通过。
```

## 8. 阶段 14：RAG 知识库运营与 MediaCrawler 采集 Agent

状态：进行中

计划阶段归档文档：

```text
阶段 14 RAG 知识库运营与 MediaCrawler 采集 Agent.md
```

### 核心思想

把 Pinecone 从一次性导入的静态知识库，升级为可以持续运营、定期补充、按景点覆盖度维护的旅行知识系统。

阶段 14 使用本地 MediaCrawler 项目作为第一版内容采集工具：

```text
C:\Users\DJI\Desktop\red note mediacrawler
```

核心策略是少量、定期、可审计地采集内容，而不是无限制抓取。系统在数据库里维护法国、意大利、瑞士的景点表单，记录每个国家有哪些可游玩景点、每个景点当前攻略覆盖情况、是否需要补充内容。后续采集 Agent 根据这张表动态生成或修改 MediaCrawler 的 `base_config.py` 中 `KEYWORDS` 关键词配置，按不同旅游风格定期补充小红书内容，并把清洗后的内容写入 MySQL 和 Pinecone。

阶段 14 的目标不是立刻做全世界全量景点，而是先把“法国 / 意大利 / 瑞士”三个核心国家做成可持续更新的 RAG 样板。

同时，阶段 14 引入 Adaptive RAG（自适应检索增强生成）。MediaCrawler 解决“知识库内容如何持续变新”，Adaptive RAG 解决“每次查询如何选择正确的检索方式”。系统不应把所有问题都固定成一次 Pinecone similarity search，而应该根据用户问题类型、复杂度、目的地数量、是否需要比较、是否需要操作指导，动态选择检索策略、知识来源和生成逻辑。

### 解决的问题

```text
当前 Pinecone 知识库缺少持续维护机制
RAG 内容是否过期、是否覆盖热门景点，目前缺少数据库记录
不同旅行风格需要不同攻略材料，例如小众、亲子、情侣、穷游、深度游、拍照、美食、徒步
Planner 现在只能被动读取已有 RAG，不能主动发现某个景点攻略缺失
景点知识来源缺少 raw -> clean -> chunk -> embedding 的可追踪链路
当前 RetrieveKnowledgeNode 更接近固定检索流程，不会根据事实问答、探索推荐、路线比较、操作教程、多跳行程推理切换检索策略
简单问题不应该浪费大量 token 做多阶段检索，复杂多国行程也不应该只拿一次相似度检索结果
```

### 主要功能

```text
建立国家表：France / Italy / Switzerland
建立景点主数据表：记录景点名称、国家、城市、标签、热度、是否启用、RAG 覆盖状态
建立采集任务表：记录本次要采集哪些景点、哪些风格、生成了哪些关键词、执行状态和错误信息
建立原始内容表：保存 MediaCrawler 采集到的原始内容摘要、来源、发布时间、采集时间
建立清洗内容表：保存去重、过滤、总结后的攻略内容
建立向量切片表：记录哪些 clean document 已经切片并写入 Pinecone
根据景点表和旅游风格动态生成 KEYWORDS
定期修改或生成 MediaCrawler base_config.py / KEYWORDS 配置
少量定期运行 MediaCrawler，先覆盖法国、意大利、瑞士热门景点
检测景点攻略覆盖缺口，如果某景点没有小红书攻略，则标记为需要联网搜索或只保留官方介绍
将清洗后的攻略内容 embedding 后 upsert 到 Pinecone
为每条 Pinecone 记录保留 MySQL 审计信息，方便删除、重建和版本追踪
识别 RAG 查询类型：FACT_BASED / EXPLORATORY / COMPARATIVE / INSTRUCTIONAL / MULTI_HOP
根据查询类型选择检索策略：关键词精确检索、语义扩展检索、多文档对比、结构化数据优先、多阶段检索
根据查询类型选择知识来源：Pinecone UGC 攻略、MySQL 景点主数据、结构化 FAQ、官方资料摘要、后续联网事实核查结果
记录 Adaptive RAG 决策轨迹：queryType、retrievalStrategy、sourceType、topK、metadata filter、命中文档和 rerank 结果
把 Adaptive RAG 检索结果写回 TravelPlanState，供分支 Agent、核心规划 Agent 和风险审查节点使用
```

### 预计修改文件

```text
AgentProjectTrip/src/main/resources/schema-mysql.sql
AgentProjectTrip/src/main/java/com/travel/agent/web/KnowledgeController.java
AgentProjectTrip/src/main/java/com/travel/agent/core/etl/*
AgentProjectTrip/src/main/java/com/travel/agent/core/service/*
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/*
AgentProjectTrip/src/main/java/com/travel/agent/ai/tools/KnowledgeTools.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/RetrieveKnowledgeNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelPlanState.java
```

可能新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/core/service/PoiCatalogService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/CrawlTaskService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/RagKnowledgeMaintenanceService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/JdbcPoiCatalogService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/JdbcCrawlTaskService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/PineconeKnowledgeMaintenanceService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/etl/MediacrawlerKeywordBuilder.java
AgentProjectTrip/src/main/java/com/travel/agent/core/etl/MediacrawlerConfigWriter.java
AgentProjectTrip/src/main/java/com/travel/agent/core/etl/TravelContentCleaner.java
AgentProjectTrip/src/main/java/com/travel/agent/core/etl/TravelContentChunker.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/AdaptiveRagNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/RagQueryType.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/RagRetrievalStrategy.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/AdaptiveRagDecision.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/RagRetrievalResult.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/KnowledgeSourceType.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/RagQueryClassifierAgent.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/AdaptiveRagService.java
```

可能新增数据库表：

```text
travel_countries
travel_pois
poi_collection_profiles
crawl_tasks
crawl_task_keywords
raw_travel_documents
clean_travel_documents
rag_knowledge_chunks
pinecone_upsert_records
rag_source_profiles
rag_query_logs
rag_retrieval_traces
```

### 验收标准

```text
数据库中维护法国、意大利、瑞士的国家和景点主数据。
可以根据景点表和风格标签生成 MediaCrawler KEYWORDS。
可以少量定期运行 MediaCrawler，并记录每次采集任务状态。
可以把采集内容清洗、切片、写入 Pinecone，并在 MySQL 中记录可追踪元数据。
RetrieveKnowledgeNode 检索到的内容能体现更新后的景点攻略信息。
不同查询类型会选择不同 Adaptive RAG 策略，并在日志或状态中显示决策结果。
事实型问题可以走精确检索和结构化数据优先，探索型问题可以走语义扩展检索，多城市复杂行程可以走多阶段检索。
固定 similarity search 不再是唯一 RAG 路径。
小红书 UGC 只作为经验攻略来源，不直接承担实时强事实判断；开放时间、闭馆日、极端天气、签证变更等仍交给阶段 16 的联网事实核查。
采集失败或某景点攻略缺失时，有明确状态记录，不影响主规划流程。
采集逻辑必须限频、少量、可关闭，并保留合规边界说明。
```

## 9. 阶段 15：标准 LangGraph4j 状态图迁移

状态：未开始

计划阶段归档文档：

```text
阶段 15 标准 LangGraph4j 状态图迁移.md
```

### 核心思想

把当前手写的 `LangGraphPlannerFacade` 顺序流程，迁移为标准化 LangGraph4j Agent 状态图。

这一阶段要求“绝对标准化”：明确每个节点的输入、输出、状态字段、失败边界、条件边、循环边和暂停恢复策略。外部 Controller 仍然调用稳定门面，但内部执行必须从手写 Java if/else 编排升级为标准状态图。

### 解决的问题

```text
LangGraphPlannerFacade 会随着节点增加越来越膨胀
条件分支、风险修订和澄清暂停目前主要靠手写 if 实现
分支任务还不是标准图结构，后续难以扩展多轮 agent 交互
RAG 维护 Agent、事实核查 Agent、Eval Agent 加入后，手写流程会变得难维护
缺少标准化状态图后，边界控制、循环控制、失败恢复和 Trace 都难以统一
```

### 主要功能

```text
引入 LangGraph4j 依赖
固定 TravelPlanState 图状态 schema
把 InitStateNode 包装为标准图节点
把 PreClarifyCheckNode / ClarifyQuestionNode / MergeClarificationNode 包装为澄清子流程
把 RetrieveKnowledgeNode 包装为 RAG 节点
把 ModelBranchDispatchNode / BranchDispatchGuardNode / BranchExecuteNode 包装为分支任务子图
把 PlanDraftNode / ValidateDraftNode / TripRiskReasoningNode / PlanRevisionNode / FinalizeAnswerNode 包装为规划与修正子图
实现条件边：是否需要澄清、是否可以继续、是否需要修订、是否执行完成
实现循环边：RiskReasoning -> PlanRevision -> Validate -> RiskReasoning
实现循环上限：读取 revisionCount / maxRevisionCount，防止无限修正
实现暂停恢复：pending 状态继续由 sessionId 找回，恢复后回到正确图节点
保留 LangGraphPlannerFacade 作为外部门面，避免 Web 层和生成服务大改
```

### 预计修改文件

```text
AgentProjectTrip/pom.xml
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/LangGraphPlannerFacade.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelPlanState.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/*.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/LangGraphPlannerFacadeTest.java
```

可能新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/TravelPlanGraphFactory.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/TravelPlanGraphRunner.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/TravelPlanGraphEdges.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/TravelPlanGraphStateSchema.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/TravelPlanGraphStateSerializer.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/TravelPlanGraphConfig.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/TravelPlanGraphFactoryTest.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/TravelPlanGraphRunnerTest.java
```

### 验收标准

```text
外部仍然调用 LangGraphPlannerFacade.plan(...)。
内部执行变成标准 LangGraph4j 状态图。
每个节点有明确输入、输出、状态字段和失败策略。
澄清暂停、用户补充恢复、RAG 检索、模型派发、分支执行、风险修订、最终输出都能在图中运行。
循环修订有明确上限，不会无限执行。
旧 Graph 单元测试通过，并新增条件边、循环边、暂停恢复测试。
```

## 10. 阶段 16：联网事实核查与输出前强校验

状态：未开始

计划阶段归档文档：

```text
阶段 16 联网事实核查与输出前强校验.md
```

### 核心思想

在最终版本输出给用户之前，引入联网事实核查能力，对行程中的开放时间、闭馆日、门票预约、交通风险、天气风险、安全事件等进行强校验。

阶段 16 到来时，先指导用户手动获取 OpenAI / ChatGPT API 权限，并以官方最新文档为准确认可用的联网搜索模型或 Responses / Tools 调用方式。第一版可以使用 OpenAI 联网搜索能力，也可以用我们自己的 `OnlineFactCheckAgent` 抽象封装搜索、抓取和官方来源核查。

核心目标是：最终方案不要只“看起来合理”，还要在关键事实上经得起输出前检查。

### 解决的问题

```text
当前 RiskReasoning 主要基于已有 RAG、工具结果和模型判断，缺少输出前联网事实核查
Planner 可能安排用户在景点关闭时间参观，例如周四晚上 8 点去卢浮宫
门票预约、闭馆日、开放时间、临时关闭、罢工、安全提醒等信息可能快速变化
模型可能把未核查的常识当成事实写入最终方案
用户需要知道哪些信息已经被核查，哪些只是建议或假设
```

### 主要功能

```text
手动获取 OpenAI API 权限和联网搜索能力配置说明
新增 OnlineFactCheckAgent 或 OnlineFactCheckNode
对最终草案中的景点开放时间进行联网核查
对门票预约、闭馆日、季节性关闭进行联网核查
对极端天气、罢工、安全提醒、大型活动进行联网核查或降级说明
优先使用官方来源，例如博物馆官网、旅游局、交通运营商、天气服务
把核查结果转成 RiskIssue 或 FactCheckIssue
如果冲突可自动修正，则触发 PlanRevision 重新安排
如果冲突需要用户决定，则输出明确提醒或进入澄清
在最终答案中标注“已联网核查 / 未能核查 / 需出发前再次确认”的边界
```

### 预计修改文件

```text
AgentProjectTrip/src/main/resources/application.properties
AgentProjectTrip/src/main/java/com/travel/agent/ai/config/*
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/LangGraphPlannerFacade.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/TripRiskReasoningNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/PlanRevisionNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/FinalizeAnswerNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/RiskAssessment.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/RiskIssue.java
```

可能新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/OnlineFactCheckAgent.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/OnlineFactCheckNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/FactCheckAssessment.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/FactCheckIssue.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/FactCheckSource.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/tools/OnlineSearchTools.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/OnlineFactCheckService.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/OpenAiOnlineFactCheckService.java
```

可能新增数据库表或审计记录：

```text
fact_check_runs
fact_check_issues
fact_check_sources
```

### 验收标准

```text
完成 OpenAI API / 联网搜索能力接入说明，并能在本地配置运行。
最终方案输出前会执行 OnlineFactCheckNode。
能识别至少一种开放时间冲突，例如“卢浮宫关闭时间仍安排参观”。
能把事实冲突转成结构化 FactCheckIssue / RiskIssue。
可自动修正的问题会触发 PlanRevision。
无法自动修正的问题会在最终答案中明确提醒用户。
联网搜索失败时系统降级说明，不伪造已核查事实。
```

## 11. 阶段 17：Agent Eval 自动评估体系

状态：未开始

计划阶段归档文档：

```text
阶段 17 Agent Eval 自动评估体系.md
```

### 核心思想

参考 OpenAI Agent Eval 的思路，建立本项目自己的旅行规划 Agent 测试集、评分指标和回归评估流程。

到阶段 17 时，系统已经具备结构化需求、RAG 维护、标准 Graph、分支工具、联网事实核查等能力。此时不能只靠肉眼判断“结果看起来不错”，而要用固定测试集和自动评分指标持续评估每次改动是否让系统变好。

### 解决的问题

```text
当前生成质量主要靠人工看结果
Agent 工作流改动后，缺少回归测试来判断是否退化
RAG、工具调用、事实核查、风险修订是否真的提升结果，目前缺少量化指标
不同类型旅行需求的成功标准不一样，需要建立分场景评估集
后续优化模型 prompt、工具策略和 Graph 边界时，需要可重复评估
```

### 主要功能

```text
建立 eval_cases 数据集，覆盖典型旅行规划场景
建立 expected_constraints，记录每个测试用例必须满足的硬约束
建立评分维度：需求遵守度、事实准确率、预算一致性、行程可执行性、工具使用正确性、风险发现率、输出清晰度、成本和延迟
建立自动运行入口，对同一批 eval cases 批量生成方案
建立 LLM-as-judge 或规则评分器，对输出进行结构化评分
建立事实核查对照项，例如开放时间冲突、预算超限、天气冲突
保存每次评估的模型版本、prompt 版本、Graph 版本、RAG 版本和工具配置
输出评估报告，支持比较两次代码修改前后的得分差异
```

### 预计修改文件

```text
AgentProjectTrip/src/main/resources/schema-mysql.sql
AgentProjectTrip/src/test/java/com/travel/agent/*
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/GraphResult.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/generation/PlanGenerationService.java
```

可能新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/eval/AgentEvalRunner.java
AgentProjectTrip/src/main/java/com/travel/agent/eval/AgentEvalCase.java
AgentProjectTrip/src/main/java/com/travel/agent/eval/AgentEvalResult.java
AgentProjectTrip/src/main/java/com/travel/agent/eval/AgentEvalMetric.java
AgentProjectTrip/src/main/java/com/travel/agent/eval/AgentEvalScorer.java
AgentProjectTrip/src/main/java/com/travel/agent/eval/RuleBasedTravelPlanScorer.java
AgentProjectTrip/src/main/java/com/travel/agent/eval/LlmJudgeTravelPlanScorer.java
AgentProjectTrip/src/main/resources/eval/travel_eval_cases.json
```

可能新增数据库表：

```text
agent_eval_cases
agent_eval_runs
agent_eval_results
agent_eval_scores
```

### 验收标准

```text
至少建立 20 个核心旅行规划 eval cases。
测试集覆盖预算紧张、开放时间冲突、未来天气冲突、缺少关键信息、航班酒店约束、热门景点完整游览、小众避峰等场景。
每个 eval case 都有明确 expected constraints。
可以一键运行一批 eval cases 并生成评分报告。
评分报告至少包含需求遵守度、事实准确率、预算一致性、行程可执行性、风险发现率。
每次 Graph / prompt / RAG / 工具策略改动后，可以通过 eval 对比是否退化。
```

## 12. 阶段完成归档模板

每完成一个阶段，新建对应阶段文档时使用下面模板。

```text
# 阶段 N 阶段名称

完成日期：YYYY-MM-DD
对应总控文档：阶段9-17开发规划与维护清单.md

## 1. 本阶段核心思想

## 2. 本阶段解决的问题

## 3. 新增功能

## 4. 修改的业务逻辑

## 5. 修改的前端内容

## 6. 修改的后端接口

## 7. 修改的代码文件

## 8. 新增的类

## 9. 删除或废弃的类

## 10. 新增的方法

## 11. 删除或废弃的方法

## 12. 关键代码细节和用途

## 13. 注释规范执行情况

## 14. 测试方式和测试结果

## 15. 遗留问题

## 16. 下一阶段衔接点
```

## 13. 当前推荐下一步

下一步从阶段 14 开始。

阶段 9 到阶段 13 已完成。当前系统已经具备从需求整理、确认、异步生成、任务恢复、方案展示、真实航班 / 酒店工具调用，到模型驱动分支派发与 Java Guard 兜底的基础产品闭环。

阶段 14 应开始建设 RAG 知识库运营与 MediaCrawler 采集 Agent。第一版重点不是大规模爬取，而是建立法国、意大利、瑞士景点主数据表，按景点和旅行风格生成 MediaCrawler `KEYWORDS`，少量定期采集小红书内容，清洗后写入 MySQL 和 Pinecone，让 RAG 能持续更新。同时加入 Adaptive RAG，让系统根据用户查询类型动态选择检索策略、知识来源和生成逻辑，避免所有问题都走固定 similarity search。

阶段 15 再进行标准 LangGraph4j 状态图迁移，把当前手写 Graph 改成边界明确、可暂停恢复、可循环修正、可观测的标准 Agent 状态图。

阶段 16 接入 OpenAI 联网搜索能力或自研 OnlineFactCheckAgent，在最终答案输出前做事实强校验，例如开放时间、闭馆日、门票预约、天气风险和安全事件。

阶段 17 参考 OpenAI Agent Eval 思路，建立旅行规划测试集、评分指标和回归评估流程，避免后续修改 Graph、prompt、RAG 和工具策略时只靠人工肉眼判断效果。
