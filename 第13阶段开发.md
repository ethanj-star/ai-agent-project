# 第13阶段开发：模型驱动 BranchDispatch

创建日期：2026-06-14

对应总控文档：

```text
阶段9-17开发规划与维护清单.md
```

当前状态：

```text
已完成，人工测试通过，已归档
```

计划阶段归档文档：

```text
阶段 13 模型驱动 BranchDispatch.md
```

## 0.1 当前开发记录

### 2026-06-14：第 13 阶段代码第一版

本轮已完成“模型建议 + Java Guard”的分支派发第一版代码：

```text
新增 BranchTaskSuggestion：承载模型建议的单个分支任务，type 使用 String 接住未知工具类型。
新增 BranchDispatchDecision：承载模型整体派发建议、notes 和 fallback 原因。
新增 BranchDispatchIssue：记录 Guard 接受、拒绝、裁剪或 fallback 的原因。
新增 BranchDispatchPolicy：集中定义当前允许的任务类型和最大任务数。
新增 ModelBranchDispatchNode：调用 DeepSeek Pro 输出结构化分支任务建议。
新增 BranchDispatchGuardNode：校验模型建议，拒绝未知工具、重复任务、参数不足任务和未来天气误用。
TravelPlanState 新增 branchDispatchDecision 和 branchDispatchIssues。
BranchTask 新增 dispatchReason 和 dispatchPriority。
LangGraphPlannerFacade 优先使用 ModelBranchDispatchNode -> BranchDispatchGuardNode，未注入时回退旧 BranchDispatchNode。
PlanDraftNode 注入分支派发记录，让 Planner 知道哪些工具建议被拒绝或降级。
BranchDispatchNode 作为旧规则 fallback 保留，并给规则派发任务写入 dispatchReason。
```

本轮安全边界：

```text
模型调用失败、模型输出非法 JSON、模型输出空任务时，Guard 回退旧 BranchDispatchNode。
模型成功输出任务但任务被 Guard 拒绝时，不再回退旧规则把它偷偷加回来。
未知工具类型全部拒绝，例如 VISA / TRAIN / RESTAURANT / FORECAST_WEATHER。
WEATHER 只接受当前/今天/实时天气诉求，不接受未来旅行天气预报。
FLIGHT 必须有目的地、出发地和明确 startDate。
HOTEL 必须有目的地、startDate 和 durationDays。
```

已执行验证：

```text
mvn test
通过，Tests run: 123, Failures: 0, Errors: 0, Skipped: 0
```

等待人工测试：

```text
需要用户在正式前端页面手动测试模型派发日志、Guard 拒绝日志，以及最终方案是否仍能正常生成。
人工测试通过后，再生成正式归档文档：阶段 13 模型驱动 BranchDispatch.md。
```

### 2026-06-14：人工测试通过与前端体验修复

人工测试过程中补充修复了两个前端可视化问题：

```text
生成任务在 RUNNING_GRAPH 阶段停留较久时，已运行秒数现在由前端本地时钟每秒刷新，不再只依赖后端轮询返回的 durationSeconds。
生成失败时，进度步骤不再把未实际执行过的后续步骤误标为绿色完成，避免误判 Graph 已跑完。
```

最终验证：

```text
node --check AgentProjectTrip/src/main/resources/static/agent-app.js
通过。

mvn process-resources
通过。

正式前端页面人工测试通过。
```

阶段归档文档：

```text
阶段 13 模型驱动 BranchDispatch.md
```

## 0. 本阶段一句话目标

第 13 阶段的目标是：把当前 `BranchDispatchNode` 里写死的 Java 派发规则，升级为“核心模型提出分支任务建议，Java Guard 负责安全校验和兜底”的任务派发机制。

完成后，系统的分支派发应从：

```text
Java 规则判断：看到关键词就派发 WEATHER / PLACES / FLIGHT / HOTEL
```

升级为：

```text
DeepSeek Pro 读懂用户需求和结构化需求表
 -> 输出需要调用哪些分支工具
 -> Java Guard 检查是否允许、参数是否足够、数量是否超限
 -> 合法任务进入 BranchExecute
 -> 非法任务被拒绝或降级
 -> 模型失败时回退旧 BranchDispatchNode
```

## 1. 为什么现在要做这一阶段

第 12 阶段已经让系统具备真实分支执行能力：

```text
KNOWLEDGE
WEATHER
PLACES
FLIGHT
HOTEL
```

但是当前“派发哪些分支任务”仍然主要由 `BranchDispatchNode` 的 Java 硬逻辑决定。

这会带来几个问题：

```text
规则越来越多，代码会变得臃肿。
复杂需求下，Java 很难判断用户真正需要哪些工具。
模型没有机会根据上下文主动计划“下一步应该查什么”。
如果完全交给模型，又可能乱调用不存在的工具、调用太多工具或传入不完整参数。
```

所以第 13 阶段要采用成熟 agent 系统常用的折中方案：

```text
模型负责理解和建议。
Java 负责边界、安全、成本和兜底。
```

## 2. 本阶段要解决的问题

本阶段解决以下问题：

```text
BranchDispatchNode 目前主要是硬编码规则。
核心模型不能根据完整上下文灵活决定分支任务。
工具越多，纯 Java if/keyword 规则越难维护。
模型如果直接控制工具调用，容易产生不存在的任务类型或过量调用。
当前分支派发缺少“为什么派发这个任务”的原因记录。
```

## 3. 本阶段不做什么

为了控制复杂度，第 13 阶段暂时不做：

```text
不新增火车、签证、餐厅、门票、未来天气、安全提醒等真实工具。
不把所有分支 Agent 改成千问模型执行。
不迁移 LangGraph4j。
不做完整多轮模型反思循环。
不让模型直接调用外部 API。
不让模型决定扣费。
```

本阶段只改“派发任务的决策方式”，不扩大真实工具接入范围。

## 4. 目标工作流

第 13 阶段完成后的 Graph 主流程应变成：

```text
Gatekeeper
 -> Requirement / Clarify
 -> RAG
 -> ModelBranchDispatch
 -> BranchDispatchGuard
 -> BranchExecute
 -> PlanDraft
 -> Validate
 -> RiskReasoning
 -> PlanRevision
 -> Finalize
 -> Persist Plan / Job
```

其中：

```text
ModelBranchDispatch：让 DeepSeek Pro 根据上下文提出分支任务建议。
BranchDispatchGuard：用 Java 检查建议是否安全、可执行、不过量。
BranchExecute：只执行 Guard 接受后的任务。
```

如果模型调用失败、输出非法 JSON 或建议为空，则回退到旧的 `BranchDispatchNode`。

## 5. 模型派发输入

`ModelBranchDispatchNode` 应向核心模型提供这些信息：

```text
用户原始输入
结构化 TravelRequirementSpec
Gatekeeper 实体
RAG 检索摘要
已知目的地
出行时间与天数
预算金额、币种、是否包含国际机票
住宿偏好
当前系统可用的分支任务类型
每种分支任务的使用边界
```

可用工具清单必须明确写入提示词，例如：

```text
KNOWLEDGE：补充目的地常识、路线建议、签证/预算等非实时背景文本。
WEATHER：只能查当前天气，不适合未来日期预报。
PLACES：查询代表城市热门景点。
FLIGHT：需要出发地、目的地和明确日期。
HOTEL：需要目的地、入住日期和旅行天数。
```

## 6. 模型派发输出格式

模型必须输出严格 JSON，不要输出 Markdown。

建议结构：

```json
{
  "tasks": [
    {
      "type": "HOTEL",
      "priority": "HIGH",
      "reason": "用户提供了明确日期、天数和住宿偏好，需要真实酒店价格参考。"
    }
  ],
  "notes": [
    "当前天气工具只能查询实时天气，不应用于 2026 年 10 月具体天气预报。"
  ]
}
```

第一版只接受这些 `type`：

```text
KNOWLEDGE
WEATHER
PLACES
FLIGHT
HOTEL
```

模型输出任何其他类型，例如：

```text
VISA
TRAIN
RESTAURANT
FORECAST_WEATHER
OPENING_HOURS
```

都必须由 Java Guard 拒绝，并写入拒绝原因。

## 7. Java Guard 规则

`BranchDispatchGuardNode` 或同等 Guard 服务负责兜底。

第一版建议规则：

```text
最多允许 5 个分支任务。
每种任务类型最多 1 个任务。
只允许 KNOWLEDGE / WEATHER / PLACES / FLIGHT / HOTEL。
未知任务类型全部拒绝。
任务缺少必要上下文时拒绝或降级。
模型输出非法 JSON 时回退旧 BranchDispatchNode。
模型输出空任务时回退旧 BranchDispatchNode。
模型建议过多任务时按 priority 和白名单排序裁剪。
所有被接受和拒绝的任务都要记录日志。
```

更细规则：

```text
KNOWLEDGE：有目的地或复杂规划需求时可以接受。
PLACES：有目的地，且需要景点、路线、避峰、游玩内容时可以接受。
WEATHER：只用于当前天气或用户明确问当前天气；未来日期不应当作精准预报。
FLIGHT：需要明确出发地、目的地和 startDate；预算不含国际机票不能单独触发航班。
HOTEL：需要目的地、startDate、durationDays；有住宿偏好或预算规划需求时优先接受。
```

## 8. 旧 BranchDispatchNode 的角色

旧 `BranchDispatchNode` 不应立即删除。

它在第 13 阶段有两个用途：

```text
1. 模型派发失败时的 fallback。
2. 单元测试和线上稳定性的安全兜底。
```

等模型派发稳定后，后续阶段再决定是否把旧规则改名为：

```text
RuleBasedBranchDispatchNode
```

或者只作为 Guard 的 fallback 方法保留。

## 9. 预计修改的代码文件

预计修改：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/LangGraphPlannerFacade.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/BranchDispatchNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTask.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTaskType.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/TravelPlanState.java
```

预计新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/ModelBranchDispatchNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/BranchDispatchGuardNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchDispatchDecision.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTaskSuggestion.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchDispatchPolicy.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchDispatchIssue.java
```

预计新增或更新测试：

```text
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/node/ModelBranchDispatchNodeTest.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/node/BranchDispatchGuardNodeTest.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/LangGraphPlannerFacadeTest.java
AgentProjectTrip/src/test/java/com/travel/agent/ai/graph/node/BranchDispatchNodeTest.java
```

## 10. 预计新增类说明

### 10.1 ModelBranchDispatchNode

职责：

```text
读取 TravelPlanState。
组织模型派发提示词。
调用核心模型 DeepSeek Pro。
解析模型输出 JSON。
产出 BranchDispatchDecision。
模型失败时返回 fallback 标记。
```

它只负责“让模型提出建议”，不直接执行工具。

### 10.2 BranchDispatchGuardNode

职责：

```text
接收模型建议。
校验任务类型、任务数量、必要参数、工具可用性和成本边界。
把合法建议转换成 BranchTask。
把非法建议记录为 BranchDispatchIssue。
模型失败时调用旧 BranchDispatchNode 兜底。
```

它是第 13 阶段的安全核心。

### 10.3 BranchDispatchDecision

职责：

```text
承载模型输出的整体派发决策。
包括任务建议、模型备注、解析状态和失败原因。
```

### 10.4 BranchTaskSuggestion

职责：

```text
承载模型建议的单个任务。
字段包括 type、priority、reason。
```

第一版不让模型直接填复杂工具参数，参数仍由 Java 从 `TravelRequirementSpec` 和 `BranchTask` 里推导。

### 10.5 BranchDispatchPolicy

职责：

```text
集中定义 Guard 规则。
包括允许任务类型、最大任务数、每类任务上限、是否允许 fallback。
```

### 10.6 BranchDispatchIssue

职责：

```text
记录某个模型建议为什么被拒绝、降级或裁剪。
```

后续可以进入 Trace 面板，帮助我们调试模型派发质量。

## 11. 预计新增方法

`ModelBranchDispatchNode`：

```text
dispatch(TravelPlanState state)
buildPrompt(TravelPlanState state)
parseDecision(String rawModelOutput)
fallbackDecision(String reason)
```

`BranchDispatchGuardNode`：

```text
guard(TravelPlanState state, BranchDispatchDecision decision)
validateSuggestion(...)
toBranchTask(...)
fallbackToRuleBasedDispatch(...)
```

`BranchDispatchPolicy`：

```text
defaultPolicy()
isAllowedType(...)
maxTaskCount()
```

`BranchTaskSuggestion`：

```text
normalizedType()
```

具体方法名以代码实现时与现有风格匹配为准。

## 12. 关键实现策略

### 12.1 模型只建议，不执行

模型输出的只是：

```text
应该查航班
应该查酒店
应该查景点
为什么
优先级
```

它不直接调用 `FlightTools` / `PlacesTools`，也不决定扣费。

### 12.2 Java 继续掌握工具边界

Java Guard 负责：

```text
不存在的工具不能调用。
参数不足的工具不能调用。
未来天气不能伪装成 forecast。
外部 API 调用数量不能超限。
预算边界必须遵守。
```

### 12.3 保留旧规则 fallback

如果模型不可用，系统仍然可以继续生成方案。

这点很重要，因为第 13 阶段不能为了“更智能”牺牲稳定性。

### 12.4 分支派发原因可追踪

每个被接受的 `BranchTask` 应尽量携带 reason，或者在 `TravelPlanState` 中保留本轮派发摘要。

这样后续第 15 阶段做 Trace 日志时，可以清楚看到：

```text
模型建议了什么
Java 接受了什么
Java 拒绝了什么
最终执行了什么
```

## 13. 注释规范要求

本阶段新增和修改 Java 代码时，必须继续遵守：

```text
C:\Users\DJI\Desktop\agent spring ai\注释规范.md
```

重点要求：

```text
ModelBranchDispatchNode 的类注释必须说明它在 Graph 中的位置，以及它只负责模型建议、不负责工具执行。
BranchDispatchGuardNode 的类注释必须说明 Guard 是安全边界。
公开方法必须说明输入、输出、失败策略和 fallback 行为。
模型 JSON 解析失败时必须解释为什么回退旧 BranchDispatchNode。
涉及 TravelPlanState 的方法要说明读取和写入哪些字段。
```

## 14. 自动化测试计划

需要覆盖：

```text
模型返回合法 KNOWLEDGE / HOTEL / FLIGHT 建议时，Guard 生成对应 BranchTask。
模型返回未知类型时，Guard 拒绝该任务。
模型返回过多任务时，Guard 按策略裁剪。
模型返回非法 JSON 时，系统回退旧 BranchDispatchNode。
模型返回空任务时，系统回退旧 BranchDispatchNode。
FLIGHT 缺少出发地或日期时，Guard 不应接受真实航班任务。
HOTEL 缺少 startDate 或 durationDays 时，Guard 不应接受真实酒店任务。
WEATHER 面对未来日期时，不能当成精准天气预报工具使用。
```

代码完成后需要执行：

```powershell
cd AgentProjectTrip
mvn test
```

如果修改静态资源，再执行：

```powershell
cd AgentProjectTrip
mvn process-resources
```

## 15. 手动测试计划

代码完成后，建议用正式页面测试：

```text
http://localhost:8080/travel-planner.html
```

测试 1：需要航班和酒店

```text
从上海出发，2026年10月1日去法国和意大利玩10天，预算2000欧，预算包含国际机票，想住经济型酒店，避开人多的地方。
```

预期：

```text
日志出现 ModelBranchDispatch。
模型建议 FLIGHT / HOTEL / PLACES / KNOWLEDGE。
Guard 接受合法任务。
BranchExecute 执行被接受的任务。
```

测试 2：预算不含国际机票

```text
从上海出发，2026年10月1日去法国和意大利玩10天，预算1200欧，不含国际机票，想住便宜酒店。
```

预期：

```text
FLIGHT 不应仅因为“不含国际机票”被强制派发。
如果模型建议 FLIGHT，Guard 应根据出发地、日期、预算边界判断是否接受。
最终预算不应把国际机票计入 1200 欧。
```

测试 3：未知工具诉求

```text
帮我规划法国和意大利10天，还要查签证、火车票、餐厅预约和未来天气。
```

预期：

```text
模型可能建议签证、火车、餐厅、未来天气。
Guard 必须拒绝当前不存在的工具类型。
最终方案可以说明这些能力暂未真实接入，不能伪造实时结果。
```

测试 4：模型失败 fallback

```text
可以通过单元测试模拟模型输出非法 JSON。
```

预期：

```text
系统回退旧 BranchDispatchNode。
Graph 不失败。
```

## 16. 验收标准

第 13 阶段完成时必须满足：

```text
Graph 主流程接入 ModelBranchDispatch 和 BranchDispatchGuard。
核心模型可以输出结构化分支任务建议。
Java Guard 可以拒绝未知类型、过量任务和参数不足任务。
模型失败时旧 BranchDispatchNode 可以兜底。
BranchExecute 只执行 Guard 接受后的任务。
日志中能看到模型建议数量、接受数量和拒绝数量。
mvn test 通过。
用户手动测试通过后，再生成阶段 13 正式归档文档。
```

## 17. 人工审核点

开始写代码前，请确认以下策略：

```text
1. 是否同意第 13 阶段只做 DeepSeek Pro 模型派发，不把千问接成分支执行模型？
2. 是否同意旧 BranchDispatchNode 保留为 fallback？
3. 是否同意模型输出未知工具类型时全部拒绝，而不是自动创建新工具？
4. 是否同意本阶段最多允许 5 个分支任务？
5. 是否同意本阶段不新增外部工具，只调度已有 KNOWLEDGE / WEATHER / PLACES / FLIGHT / HOTEL？
```

审核通过后，再开始第 13 阶段代码实现。
