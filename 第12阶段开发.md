# 第12阶段开发：航班与酒店真实分支接入

创建日期：2026-06-14

对应总控文档：

```text
阶段9-17开发规划与维护清单.md
```

当前状态：

```text
已完成，人工测试通过，已归档
```

## 0.1 当前开发记录

### 2026-06-14：第 12 阶段代码第一版

本轮已完成航班和酒店真实分支接入的第一版代码：

```text
BranchTaskType 新增 HOTEL 分支类型。
BranchTask 新增 startDate、durationDays、departureCity、accommodationPreference、budgetIncludesInternationalFlight 字段。
FlightSearchRequest 新增，用于表达航班工具可查询参数和失败原因。
HotelSearchRequest 新增，用于表达酒店工具可查询参数和失败原因。
FlightSearchParamResolver 新增，负责把出发地、目的地和日期解析成 IATA 航班参数。
HotelSearchParamResolver 新增，负责把目的地、日期和天数解析成酒店查询参数。
BranchDispatchNode 新增 HOTEL 派发规则，并让结构化需求表完整时可以派发 FLIGHT / HOTEL。
BranchAgentFacade 注入 FlightTools，FLIGHT 分支从 fallback 改为真实调用 searchFlights。
BranchAgentFacade 新增 executeHotel，真实调用 PlacesTools.searchHotels。
PlanDraftNode 提示词新增“不要编造实时酒店价格、酒店库存”约束。
```

已执行验证：

```text
mvn test
通过，Tests run: 114, Failures: 0, Errors: 0, Skipped: 0
```

等待人工测试：

```text
需要用户在前端页面或接口里手动测试真实 SerpApi 航班 / 酒店查询是否符合预期。
人工测试通过后，再生成正式归档文档：阶段 12 航班与酒店真实分支接入.md。
```

### 2026-06-14：人工测试通过并完成归档

用户已确认第 12 阶段航班和酒店真实接入的手动测试通过。

本阶段正式归档为：

```text
阶段 12 航班与酒店真实分支接入.md
```

同时进入第 13 阶段计划：

```text
第13阶段开发.md
```

## 0. 本阶段一句话目标

第 12 阶段的目标是：把“底层已经有，但 Graph 没真正用起来”的航班和酒店能力接入正式工作流。

完成后，系统应该能做到：

```text
用户在需求表里给出出发地、目的地、日期、天数、预算后
  -> Graph 可以派发 FLIGHT / HOTEL 分支
  -> BranchAgentFacade 调用真实 FlightTools / PlacesTools
  -> 分支结果以 BranchResult 形式返回
  -> Planner 在生成方案时引用真实航班和酒店参考
  -> 如果参数不足或外部 API 失败，系统明确说明原因，不伪造数据
```

## 1. 本阶段核心思想

现在项目里航班和酒店都不是“完全没写”，而是处于不同程度的半接入状态。

航班当前状态：

```text
FlightServiceImpl.searchFlights(...) 已经能调用 SerpApi Google Flights
FlightTools.searchFlights(...) 已经存在
BranchTaskType 已经有 FLIGHT
BranchDispatchNode 有机会派发 FLIGHT
但是 BranchAgentFacade 遇到 FLIGHT 时仍然调用 executeFlightFallback(...)
所以正式 Graph 工作流没有真实查航班
```

酒店当前状态：

```text
PlacesServiceImpl.searchHotels(...) 已经能调用 SerpApi Google Hotels
PlacesTools.searchHotels(...) 已经存在
HotelDTO 已经存在
但是 BranchTaskType 没有 HOTEL
BranchDispatchNode 不会派发 HOTEL
BranchAgentFacade 只在 PLACES 分支里查景点，不查酒店
所以正式 Graph 工作流没有真实查酒店
```

第 12 阶段不追求“所有旅行数据源一次性补齐”，而是先把最接近可用的两条链路补完整。

## 2. 本阶段解决的问题

```text
问题 1：用户明确需要机票参考时，Graph 不能真实调用航班工具。
问题 2：Planner 容易用常识估算机票，不够可验证。
问题 3：酒店底层 API 已写好，但正式规划流完全没有派发酒店任务。
问题 4：住宿预算、住宿区域、酒店价格目前主要靠模型估算。
问题 5：外部工具失败时，Planner 需要拿到明确失败原因，而不是继续编造实时数据。
```

## 3. 本阶段不做什么

为了控制边界，第 12 阶段明确不做：

```text
不做模型驱动 BranchDispatch，那是第 13 阶段。
不做火车、地铁、城际交通真实 API。
不做签证真实规则库。
不做餐厅、美食真实 API。
不做门票、预约、开放时间真实 API。
不做未来天气预报工具。
不做实时安全提醒、罢工、灾害、活动人流工具。
不做完整支付系统和真实登录系统。
不迁移 LangGraph4j。
```

本阶段只补齐航班和酒店两个已经有底层基础的分支。

## 4. 目标工作流

第 12 阶段完成后的 Graph 分支大致如下：

```text
Requirement / Clarify
  -> RAG
  -> BranchDispatch
       -> KNOWLEDGE
       -> PLACES
       -> WEATHER
       -> FLIGHT
       -> HOTEL
  -> BranchExecute
       -> executeKnowledge(...)
       -> executePlaces(...)
       -> executeWeather(...)
       -> executeFlight(...)
       -> executeHotel(...)
  -> PlanDraft
  -> Validate
  -> RiskReasoning
  -> PlanRevision
  -> Finalize
  -> Persist Plan / Job
```

## 5. 航班分支设计

### 5.1 输入来源

航班分支优先从结构化需求表读取：

```text
departureCity
destinations
startDate
startDateText
durationDays
budgetIncludesInternationalFlight
```

如果结构化需求表不完整，再从 `BranchTask.query`、`BranchTask.destinations`、`BranchTask.time` 做轻量兜底解析。

### 5.2 查询参数

`FlightTools.searchFlights(...)` 当前需要：

```text
origin：IATA 三字码，例如 DUB、PEK、PVG
destination：IATA 三字码，例如 CDG、FCO
date：YYYY-MM-DD
```

第 12 阶段要新增一层参数解析，把用户输入转成工具可用参数。

建议第一版支持有限映射：

```text
中国主要城市：北京 -> PEK，上海 -> PVG，广州 -> CAN，深圳 -> SZX
欧洲常用城市：巴黎 -> CDG，罗马 -> FCO，米兰 -> MXP，伦敦 -> LHR，阿姆斯特丹 -> AMS，都柏林 -> DUB
国家默认入口：法国 -> CDG，意大利 -> FCO，英国 -> LHR，爱尔兰 -> DUB
```

### 5.3 失败策略

航班分支不能为了“看起来完整”而伪造航班。

如果缺少参数，返回失败型 `BranchResult`：

```text
缺少出发地：请用户补充 departureCity
缺少明确日期：提示需要 YYYY-MM-DD 或可解析的出发日期
目的地无法映射机场：提示该目的地暂不支持真实航班查询
外部 API 失败：提示航班工具暂不可用，Planner 不应生成实时票价
```

### 5.4 输出摘要

成功时 `BranchResult.summary` 应简洁提供：

```text
航线
日期
航司
最低或代表性价格
最多 3 条候选
```

失败时 `BranchResult.summary` 应明确：

```text
航班分支未获得可用实时结果，Planner 不应编造航班号、实时票价或可售状态。
```

## 6. 酒店分支设计

### 6.1 输入来源

酒店分支优先从结构化需求表读取：

```text
destinations
startDate
durationDays
travelerCount
budgetAmount
budgetCurrency
accommodationPreference
```

如果用户没有明确住宿偏好，但目的地、日期、天数齐全，也可以查询代表城市酒店作为预算参考。

### 6.2 查询参数

`PlacesTools.searchHotels(...)` 当前需要：

```text
city：英文城市名，例如 Paris、Rome、Milan
checkInDate：YYYY-MM-DD
checkOutDate：YYYY-MM-DD
```

第一版建议：

```text
只查询最多 2 个代表城市，避免外部 API 调用太多。
国家默认代表城市：法国 -> Paris，意大利 -> Rome，英国 -> London。
如果用户给的是具体城市，优先使用具体城市。
checkOutDate = startDate + durationDays。
如果 durationDays 缺失，但 startDate 存在，酒店分支返回可解释失败。
```

### 6.3 HOTEL 分支类型

需要在 `BranchTaskType` 中新增：

```text
HOTEL
```

`BranchDispatchNode` 第一版仍然可以用 Java 守卫规则派发：

```text
如果有目的地，并且满足以下任意条件：
  用户提到酒店 / 住宿 / 民宿 / 青旅 / 住哪里 / accommodation / hotel
  或者需求表中 accommodationPreference 不为空
  或者预算规划需要住宿价格参考
则派发 HOTEL 分支
```

注意：第 13 阶段才把这个派发逻辑升级成模型驱动。

### 6.4 失败策略

酒店分支也不能伪造实时价格。

如果缺少参数，返回失败型 `BranchResult`：

```text
缺少明确入住日期：提示需要 startDate
缺少行程天数：提示需要 durationDays 或 checkOutDate
城市无法映射：提示当前城市暂不支持酒店查询
外部 API 失败：提示酒店工具暂不可用，Planner 只能给常规住宿建议
```

### 6.5 输出摘要

成功时 `BranchResult.summary` 应包含：

```text
城市
入住日期
退房日期
酒店名称
每晚价格
评分
最多 3 条候选
```

失败时应明确告诉 Planner：

```text
酒店分支未获得可用实时结果，Planner 不应编造酒店价格、评分或库存状态。
```

## 7. 预计修改文件

### 7.1 后端 Java 文件

预计修改：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTaskType.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/BranchTask.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/agents/BranchAgentFacade.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/BranchDispatchNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/PlanDraftNode.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/tools/FlightTools.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/tools/PlacesTools.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/FlightServiceImpl.java
AgentProjectTrip/src/main/java/com/travel/agent/core/service/impl/PlacesServiceImpl.java
```

可能新增：

```text
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/FlightSearchRequest.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/model/HotelSearchRequest.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/FlightSearchParamResolver.java
AgentProjectTrip/src/main/java/com/travel/agent/ai/graph/node/HotelSearchParamResolver.java
```

是否新增 resolver 类取决于实现复杂度。如果参数解析只需要少量静态方法，可以先放在 `BranchAgentFacade` 内部；如果映射、日期推导和失败原因较多，就拆成独立 resolver，保持 `BranchAgentFacade` 不膨胀。

### 7.2 前端文件

本阶段原则上不改前端主流程。

如果需要让用户更容易测试酒店和航班分支，可以少量修改：

```text
AgentProjectTrip/src/main/resources/static/travel-planner.html
AgentProjectTrip/src/main/resources/static/agent-app.js
```

但第一版建议先通过现有需求表字段测试，不新增新的 UI 复杂度。

## 8. 预计新增类

### 8.1 FlightSearchRequest

用途：

```text
封装航班查询所需参数。
避免 BranchAgentFacade 在多个局部变量之间传递 origin、destination、date 和失败原因。
```

建议字段：

```text
originCode
destinationCode
departureDate
sourceDescription
missingReason
queryable
```

### 8.2 HotelSearchRequest

用途：

```text
封装酒店查询所需参数。
让酒店分支能清楚知道是否可以查询、查哪个城市、查哪段日期。
```

建议字段：

```text
city
checkInDate
checkOutDate
sourceDescription
missingReason
queryable
```

### 8.3 FlightSearchParamResolver

用途：

```text
负责把 TravelRequirementSpec / BranchTask 中的自然语言地点和时间转成航班工具参数。
```

### 8.4 HotelSearchParamResolver

用途：

```text
负责把 TravelRequirementSpec / BranchTask 中的目的地和行程天数转成酒店工具参数。
```

## 9. 预计新增方法

`BranchAgentFacade`：

```text
executeFlight(BranchTask task)
executeHotel(BranchTask task)
formatFlightResults(...)
formatHotelResults(...)
```

`BranchDispatchNode`：

```text
looksLikeHotelNeed(...)
shouldDispatchHotel(...)
```

`FlightSearchParamResolver`：

```text
resolve(TravelPlanState state, BranchTask task)
resolveAirportCode(String place)
resolveDepartureDate(...)
```

`HotelSearchParamResolver`：

```text
resolve(TravelPlanState state, BranchTask task)
resolveHotelCity(String place)
resolveCheckOutDate(LocalDate checkInDate, Integer durationDays)
```

实际写代码时，如果现有 `TravelPlanState` 暂时没有把 `TravelRequirementSpec` 传到 `BranchTask`，需要优先检查状态对象已有字段，再决定是扩展 `BranchTask` 还是在 `BranchAgentFacade` 内保留兼容解析。

## 10. 业务规则

### 10.1 外部 API 调用上限

为了避免一次规划触发过多外部请求：

```text
航班分支第一版最多查 1 条主要入境路线。
酒店分支第一版最多查 2 个代表城市。
每个城市酒店最多取 3 条结果。
每条航线航班最多取 3 条结果。
```

### 10.2 不伪造实时数据

任何真实工具失败时，最终回答必须体现：

```text
该部分实时数据未成功获取
下面内容是常规建议或预算估算
不要给出具体航班号、实时票价、酒店库存、实时酒店价格
```

### 10.3 国际机票预算边界

如果需求表里：

```text
budgetIncludesInternationalFlight = false
```

Planner 可以引用航班作为路线参考，但预算部分不能把国际机票计入用户预算。

如果：

```text
budgetIncludesInternationalFlight = true
```

Planner 应把航班价格作为预算压力来源之一。

### 10.4 日期缺失

航班和酒店都高度依赖日期。

```text
如果 startDate 缺失，不应调用真实航班 / 酒店价格工具。
如果只有“国庆”“下个月”这类自然语言时间，但没有被解析成明确日期，第一版先返回可解释失败。
后续可以在需求表阶段加强日期解析。
```

## 11. 注释规范要求

本阶段所有新增或修改的 Java 注释必须遵守：

```text
C:\Users\DJI\Desktop\agent spring ai\注释规范.md
```

重点要求：

```text
BranchTaskType 新增 HOTEL 时，要说明系统位置、职责和后续扩展边界。
BranchAgentFacade 新增 executeFlight / executeHotel 时，要说明失败策略和不伪造实时数据原则。
参数解析类如果新增，类级 Javadoc 必须说明它只负责“参数推导”，不负责真实 API 调用。
公开方法必须说明输入、输出、失败返回和为什么不抛异常中断 Graph。
涉及外部 API 的 Service / Tool 注释要说明数据来源、缓存和失败兜底。
```

## 12. 测试计划

### 12.1 单元测试

建议新增或更新：

```text
BranchAgentFacadeTest
BranchDispatchNodeTest
FlightSearchParamResolverTest
HotelSearchParamResolverTest
PlanDraftNodeTest
```

覆盖场景：

```text
给出出发地、目的地、日期时，FLIGHT 分支生成成功结果。
缺少出发地时，FLIGHT 分支返回失败 BranchResult。
缺少明确日期时，FLIGHT 分支返回失败 BranchResult。
给出目的地、日期、天数时，HOTEL 分支生成成功结果。
缺少日期或天数时，HOTEL 分支返回失败 BranchResult。
外部工具返回空列表时，分支失败但 Graph 不崩溃。
Planner prompt 能看到 FLIGHT / HOTEL 分支结果。
```

### 12.2 后端验证命令

代码完成后需要执行：

```powershell
cd AgentProjectTrip
mvn test
```

如果修改了静态资源，还需要执行：

```powershell
cd AgentProjectTrip
mvn process-resources
```

### 12.3 手动测试

代码完成后，会给用户明确的手动测试步骤。预期测试内容如下：

```text
测试 1：航班和酒店都可查询
从上海出发，2026年10月1日去法国和意大利玩10天，预算2000欧，预算包含国际机票，想住经济型酒店，避开人多的地方。

预期：
Graph 日志出现 FLIGHT 和 HOTEL 分支。
FLIGHT 分支真实调用 FlightTools。
HOTEL 分支真实调用 PlacesTools.searchHotels。
最终方案中航班和酒店部分有真实查询参考或明确失败说明。

测试 2：缺少日期
从上海去法国和意大利玩10天，预算2000欧，想住经济型酒店。

预期：
FLIGHT / HOTEL 不应伪造实时价格。
分支结果或最终方案提示缺少明确日期。

测试 3：预算不含国际机票
从上海出发，2026年10月1日去法国和意大利玩10天，预算1200欧，不含国际机票，想住青旅或便宜酒店。

预期：
航班可作为路线参考，但预算部分不能把国际机票算进 1200 欧。
酒店价格可作为住宿预算参考。
```

## 13. 验收标准

第 12 阶段完成时必须满足：

```text
BranchTaskType 支持 HOTEL。
BranchDispatchNode 可以派发 HOTEL，并继续正确派发 FLIGHT。
BranchAgentFacade 不再对 FLIGHT 只返回 fallback，而是能调用 FlightTools。
BranchAgentFacade 能调用 PlacesTools.searchHotels。
工具参数不足时返回可解释失败，不抛异常打断 Graph。
工具 API 失败时返回失败 BranchResult，不影响整体规划。
PlanDraftNode 能把航班和酒店分支结果注入 Planner prompt。
最终方案不会伪造未查询到的实时机票和酒店价格。
mvn test 通过。
用户手动测试通过后，再归档本阶段。
```

## 14. 完成后的归档要求

用户手动测试通过后，需要新建：

```text
阶段 12 航班与酒店真实分支接入.md
```

归档文档必须记录：

```text
本阶段核心思想
本阶段解决的问题
新增功能
修改的业务逻辑
修改的前端内容
修改的后端接口
修改的代码文件
新增的类
删除或废弃的类
新增的方法
删除或废弃的方法
关键代码细节和用途
注释规范执行情况
测试方式和测试结果
遗留问题
下一阶段衔接点
```

同时更新：

```text
阶段9-17开发规划与维护清单.md
```

把阶段 12 标记为：

```text
已完成
```

并写入完成日期和阶段归档文档路径。

## 15. 人工审核点

开始写代码前，需要用户确认以下方向：

```text
1. 是否同意第 12 阶段同时接入航班和酒店，而不是只做航班。
2. 是否同意 HOTEL 作为独立 BranchTaskType，而不是继续塞进 PLACES。
3. 是否同意第一版只支持有限城市 / 国家到机场和酒店城市的映射。
4. 是否同意日期缺失时不查询真实价格，而是返回可解释失败。
5. 是否同意本阶段原则上不改前端，只通过现有需求表触发测试。
```

如果以上方向通过，再开始第 12 阶段代码实现。
